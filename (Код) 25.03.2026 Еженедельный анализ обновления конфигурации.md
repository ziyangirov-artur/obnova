# (Код) 25.03.2026 Еженедельный анализ обновления конфигурации

**Источник:** сравнение MyConfig1 ↔ New\2503  
**Дата:** 25.03.2026  
**Изменённых файлов (по содержимому):** 24, из них модулей .bsl — 6.

---

## CommonModules\Odin_Общий\Ext\Module.bsl

### 1. №12247 — НеБлокирует1С (обновление задачи)

**Код До (MyConfig1):**
```bsl
Если ТекЗадача.НеБлокирует1С Тогда
	НовЗадача = Задачи.ЗадачаИсполнителя.СоздатьЗадачу();
	...
	НовЗадача.Записать();
Иначе
	НовЗадача = Задачи.ЗадачаИсполнителя.СоздатьЗадачу();
	...
	НовЗадача.Записать();
КонецЕсли;
```

**Код После (New\2503):**
```bsl
Если ТекЗадача.НеБлокирует1С Тогда
	НовЗадача = Задачи.ЗадачаИсполнителя.СоздатьЗадачу();
	...
	НовЗадача.Записать();
Иначе
	НовЗадача = Задачи.ЗадачаИсполнителя.СоздатьЗадачу();
	...
	НовЗадача.НеБлокирует1С = ТекЗадача.НеБлокирует1С;  // ← исправление
	НовЗадача.Записать();
КонецЕсли;
```

**Данный участок кода:** в ветке `Иначе` добавлено присвоение `НовЗадача.НеБлокирует1С = ТекЗадача.НеБлокирует1С` перед записью.

---

### 2. btrx#14002 — график работы (time_to, URL без dealerId)

**Код До:**
```bsl
СтруктураДанных_ГрафикРаботы.Вставить("time_to", "20:00");
...
СтруктураДанных_ГрафикРаботы.Вставить("dealerId", Строка(ТекЗапись.Организация.УникальныйИдентификатор()));
```

**Код После:**
```bsl
СтруктураДанных_ГрафикРаботы.Вставить("time_to", "22:00");// +Фомин Сергей 24.03.2026 btrx#14002
// строка dealerId удалена
```

**Данный участок кода:** конец рабочего дня 20:00 → 22:00; параметр `dealerId` из структуры графика убран.

---

### 3. №12516 — тест-драйв (телемаркетолог, НикТГ, CRM)

Фрагменты: проверка `СтруктураДанных = Неопределено`; для тест-драйва — `Телемаркетолог`, `НикТГ`, вызовы `CRM_ПолучитьСписокЗвонковПоНомеруТелефона` / `CRM_ПолучитьСписокЗвонковПоНомеруТелефона_ТестДрайв`; условие `ЗначениеСвойства = "Тест драйв"` и блок с `Содержание`.

**Данный участок кода:** расширена логика по тест-драйву и защита от неинициализированной структуры.

---

### 4. №11555 — исключения подразделений

**Код До:** условие возврата только при `ЗначениеСвойства = "Звонок заботы"`.

**Код После:** добавлены ветки `Рекомендация 1`, `Рекомендация 2`, `Дружеские отношения`, `Входящий трафик` (комментарий 16.03.2026 №11555).

**Данный участок кода:** расширен список значений, при которых для подразделений из исключений выполняется ранний `Возврат`.

---

### 5. №13294 / №12823 — СтруктураДанных

В нескольких местах добавлены проверки вида `Если СтруктураДанных = Неопределено Тогда ... КонецЕсли` перед использованием результата.

**Данный участок кода:** защита от обращения к неинициализированной структуре.

---

## DataProcessors\ALLMaintenanceSchedule_ВыгрузкаДанных\Ext\ObjectModule.bsl

**Контекст:** процедура выгрузки Lada, очередь `ОчередьНаВыгрузкуВALLMaintenanceSchedule`.

**Код До:**
```bsl
Если НЕ ЗначениеЗаполнено(СтрокаТЧ.СсылкаНаОбъектВнешнейСистемы) Тогда
	НоваяЗапись = РегистрыСведений.ОчередьНаВыгрузкуВALLMaintenanceSchedule.СоздатьМенеджерЗаписи();
	...
КонецЕсли;
```

**Код После:**
```bsl
//+Фомин Сергей 24.03.2026 btrx#14002
Если НЕ ЗначениеЗаполнено(СтрокаТЧ.СсылкаНаОбъектВнешнейСистемы) И НЕ СтрокаТЧ.СтатусВыгрузки = Перечисления.СтатусыВыгрузкиВALLMaintenanceSchedule.Ошибка Тогда
	НоваяЗапись = РегистрыСведений.ОчередьНаВыгрузкуВALLMaintenanceSchedule.СоздатьМенеджерЗаписи();
	...
КонецЕсли;
```

**Данный участок кода:** повторная постановка в очередь не выполняется, если статус выгрузки уже «Ошибка».

---

## DataProcessors\Б24_ВыгрузкаКонтрагентов\Ext\ObjectModule.bsl

**Автор в коде:** Луковников 24.03.2026.

**Код До:** функция `ПолучитьСписокДоговоровКонтрагентов(Контрагент)` без параметра договора; в запросе отбор только по контрагенту.

**Код После:**  
- сигнатура `ПолучитьСписокДоговоровКонтрагентов(Контрагент, ВыбранныйДоговор = Неопределено)`;  
- в запрос добавлено условие `И (&ВыбранныйДоговор = НЕОПРЕДЕЛЕНО ИЛИ ДоговорыКонтрагентов.Ссылка = &ВыбранныйДоговор)` и установка параметра `ВыбранныйДоговор`.

**Данный участок кода:** возможность ограничить список договоров выбранным договором.

---

## DataProcessors\ЗаявкиРДС_Битрикс\Ext\ObjectModule.bsl

Крупное обновление интеграции с Битрикс24:

- банковский счёт контрагента, длина полей (`СтрДлина`), лимит `total=1000`;
- поля заявки: `ЗаявкаId`, `UF_CRM`, договор, категории, платежи;
- правки текстов запросов и формирования тела запроса.

**Данный участок кода:** см. полный diff в `diff_zayavki_25.03.txt`.

---

## DataProcessors\Обмен_с_ОЗОН\Forms\ФормаОбмена\Ext\Form\Module.bsl

**Код До:** процедуры `ЗапросЗаказов` / сервер без массовой выгрузки по площадкам.

**Код После:**
```bsl
&НаКлиенте
Процедура ПоВсемПлощадкам(Команда)
	ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда)
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ ТорговыеПлощадки.Ссылка ... ГДЕ ТорговыеПлощадки.УчаствуетВАвтообмене И НЕ ТорговыеПлощадки.ПометкаУдаления";
	ТР = Запрос.Выполнить().Выгрузить();
	Для каждого СТР из ТР Цикл
		Сценарий = СТР.Ссылка;
		ЗаполнитьОбразцыВыгрузки();
		ВыгрузитьВыбранноеНаСервере();
	КонецЦикла;
КонецПроцедуры
```

**Данный участок кода:** команда «по всем площадкам» — запрос к `Справочник.ТорговыеПлощадки` и цикл выгрузки.

---

## InformationRegisters\ОчередьНаПолучениеСертификатаВыкупаИзWeb\Ext\RecordSetModule.bsl

**Код До (MyConfig1):** полный модуль с областями `ПередЗаписью`, `ПриЗаписи` и т.д.

**Код После (New\2503):** только комментарий-заголовок (модуль фактически пустой по коду).

**Данный участок кода:** удалены процедуры событий набора записей; остался заголовок `odin+ (11.03.26) ARM-120`.

---

## Сводная таблица

| № | Модуль | Автор/метка | Суть |
|---|--------|-------------|------|
| 1 | Odin_Общий | №12247 | НеБлокирует1С при копировании задачи |
| 2 | Odin_Общий | btrx#14002 | time_to 22:00, без dealerId в графике |
| 3 | Odin_Общий | №12516, №11555, №13294 | тест-драйв, исключения подразделений, проверки |
| 4 | ALLMaintenanceSchedule | btrx#14002 | не дублировать очередь при ошибке Lada |
| 5 | Б24_ВыгрузкаКонтрагентов | Луковников 24.03 | параметр ВыбранныйДоговор |
| 6 | ЗаявкиРДС_Битрикс | — | расширение полей и запросов к API |
| 7 | Обмен_с_ОЗОН (форма) | — | ПоВсемПлощадкам |
| 8 | ОчередьНаПолучениеСертификатаВыкупаИзWeb | — | очистка RecordSetModule |

---

*Полный перечень «Данный участок кода» по строкам — в файлах `diff_*_25.03.txt` в корне MyConfig1.*
