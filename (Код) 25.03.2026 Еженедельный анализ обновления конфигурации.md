# (Код) 25.03.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `0c71db6a` `(Обновление 25.03.2026)` в `MyConfig1`.

---

## 1) `CommonModules/Odin_Общий/Ext/Module.bsl`

**Что изменено**
- Исправлена логика обновления задач по блоку №12247 (`НеБлокирует1С` в ветке создания новой задачи).
- Доработан интеграционный сценарий графика (`btrx#14002`): изменен `time_to` (`20:00` -> `22:00`), скорректирован состав параметров URL (без `dealerId`).
- Расширена логика по кейсам тест-драйва и обработке CRM-данных.
- Добавлены защитные проверки `СтруктураДанных = Неопределено` в рабочих ветках.

**Эффект**
- Уменьшение ошибок при создании/обновлении задач и повышение устойчивости интеграционных сценариев.

---

## 2) `DataProcessors/ALLMaintenanceSchedule_ВыгрузкаДанных/Ext/ObjectModule.bsl`

**Что изменено**
- Скорректирована логика постановки в очередь выгрузки (Lada, `btrx#14002`) для исключения повторной постановки записи при ошибочном статусе.

**Эффект**
- Меньше дублирующих записей в очереди и лишних повторных попыток выгрузки.

---

## 3) `DataProcessors/Б24_ВыгрузкаКонтрагентов/Ext/ObjectModule.bsl`

**Что изменено**
- Доработана функция получения договоров контрагента: добавлен параметр выбора конкретного договора (`ВыбранныйДоговор`) и отбор в тексте запроса.

**Эффект**
- Более точная выгрузка договорных данных в Б24.

---

## 4) `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`

**Что изменено**
- Расширена обработка данных для интеграции с Битрикс24: поля заявки, UF-реквизиты, детали по договору/платежам и служебные доработки формирования запроса.

**Эффект**
- Повышена полнота и предсказуемость выгрузки заявок в CRM.

---

## 5) `DataProcessors/Обмен_с_ОЗОН/Forms/ФормаОбмена/Ext/Form/Module.bsl`

**Что изменено**
- Добавлен сценарий выгрузки по всем торговым площадкам (клиентская команда + серверный цикл по `Справочник.ТорговыеПлощадки` с последовательным вызовом выгрузки).

**Эффект**
- Ускорение массовой выгрузки и снижение ручных операций.

---

## 6) `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/RecordSetModule.bsl`

**Что изменено**
- Модуль упрощен: удалены рабочие блоки, оставлен заголовок.

**Эффект**
- Снижен объем локальной логики в модуле регистра, упрощено сопровождение.

---

## Сводная таблица

| № | Модуль | Суть |
|---|---|---|
| 1 | `Odin_Общий` | Исправления логики задач и интеграции |
| 2 | `ALLMaintenanceSchedule_ВыгрузкаДанных` | Очередь выгрузки без лишних дублей |
| 3 | `Б24_ВыгрузкаКонтрагентов` | Отбор по выбранному договору |
| 4 | `ЗаявкиРДС_Битрикс` | Расширение состава CRM-данных |
| 5 | `Обмен_с_ОЗОН` | Массовая выгрузка по площадкам |
| 6 | `ОчередьНаПолучениеСертификата...` | Упрощение RecordSetModule |
