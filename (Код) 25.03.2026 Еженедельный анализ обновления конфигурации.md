# (Код) 25.03.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `0c71db6a` `(Обновление 25.03.2026)` в `MyConfig1`.  
**Формат:** для каждого изменения показаны блоки **Код до / Код после / Что добавлено по коду**.

---

## 1) `CommonModules/Odin_Общий/Ext/Module.bsl`

### Участок 1.1: график работы мастера (`time_to`)

**Код до**
```bsl
СтруктураДанных_ГрафикРаботы.Вставить("user_id", idМастераЧислом);
СтруктураДанных_ГрафикРаботы.Вставить("date", Формат(ТекЗапись.ЗаявкаНаРемонт.ДатаНачала, "ДФ=yyyy-MM-dd"));
СтруктураДанных_ГрафикРаботы.Вставить("time_from", "8:00");
СтруктураДанных_ГрафикРаботы.Вставить("time_to", "20:00");
СтруктураДанных_ГрафикРаботы.Вставить("status", 1);
```

**Код после**
```bsl
СтруктураДанных_ГрафикРаботы.Вставить("user_id", idМастераЧислом);
СтруктураДанных_ГрафикРаботы.Вставить("date", Формат(ТекЗапись.ЗаявкаНаРемонт.ДатаНачала, "ДФ=yyyy-MM-dd"));
СтруктураДанных_ГрафикРаботы.Вставить("time_from", "8:00");
СтруктураДанных_ГрафикРаботы.Вставить("time_to", "22:00");// Конец рабочего дня +Фомин Сергей 24.03.2026 btrx#14002
СтруктураДанных_ГрафикРаботы.Вставить("status", 1);
```

**Что добавлено по коду**
- Изменено значение параметра: `"time_to"` с `20:00` на `22:00`.
- Добавлен служебный комментарий с привязкой к `btrx#14002`.

### Участок 1.2: формирование URL проверки графика (убран `dealerId`)

**Код до**
```bsl
АдресПроверки = СтрШаблон("%1?year=%2&month=%3&dealerId=%4&day=%5&userId=%6", 
							Адрес, 
							массивПодСтрок[0],
							СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[1]),
							СтруктураДанныхДляПодключения.БрендID,
							СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[2]),
							Формат(СтруктураДанных.user_id, "ЧГ=0"));
```

**Код после**
```bsl
// было %1?year=%2&month=%3&dealerId=%4&day=%5&userId=%6
АдресПроверки = СтрШаблон("%1?year=%2&month=%3&day=%4&userId=%5", 
							Адрес, 
							массивПодСтрок[0],
							СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[1]),
							//СтруктураДанныхДляПодключения.БрендID, //dealerId
							СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[2]),
							Формат(СтруктураДанных.user_id, "ЧГ=0"));
```

**Что добавлено по коду**
- Убран параметр `dealerId` из шаблона URL.
- Добавлен комментарий с фиксированием прежнего формата URL.

---

## 2) `DataProcessors/ALLMaintenanceSchedule_ВыгрузкаДанных/Ext/ObjectModule.bsl`

### Участок 2.1: повторная постановка в очередь для Lada

**Код до**
```bsl
Odin_Общий.DSF_ПолучитьIDДокумента(...);
Запись.ВОчереди = ?(Запись.ТекстОшибки = "" И Запись.IDКлиентаDNM <> "" И Запись.IDДокументаDNM <> "" , Ложь, Истина);
НаборПротокола.Записать();
```

**Код после**
```bsl
Odin_Общий.DSF_ПолучитьIDДокумента(...);
Запись.ВОчереди = ?(Запись.ТекстОшибки = "" И Запись.IDКлиентаDNM <> "" И Запись.IDДокументаDNM <> "" , Ложь, Истина);

//Один раз пытаемся выгрузить. Повторно при ошибке не ставим на выгрузку. Только для Лады(можно расширить)
Если ТипБренда = Справочники.ТипБренда.Lada Тогда
	Запись.ВОчереди = Ложь;
КонецЕсли;

НаборПротокола.Записать();
```

**Что добавлено по коду**
- Добавлен отдельный блок с условием по бренду `Lada`.
- При `Lada` флаг `Запись.ВОчереди` принудительно сбрасывается в `Ложь`.

---

## 3) `DataProcessors/Б24_ВыгрузкаКонтрагентов/Ext/ObjectModule.bsl`

### Участок 3.1: параметр выбора одного договора

**Код до**
```bsl
Функция ПолучитьСписокДоговоровКонтрагентов(Контрагент, Таблица = Неопределено) Экспорт
...
|ГДЕ
|	Контрагенты.Ссылка = &ВыбранныйКонтрагент";
...
Запрос.УстановитьПараметр("ВыбранныйКонтрагент", Контрагент);
```

**Код после**
```bsl
Функция ПолучитьСписокДоговоровКонтрагентов(Контрагент, Таблица = Неопределено, ВыбранныйДоговор=0) Экспорт
//Луковников 24.03.2026г Добавлен отбор по одному договору
...
|ГДЕ
|	Контрагенты.Ссылка = &ВыбранныйКонтрагент
|	И (ДоговорыВзаиморасчетов.Ссылка = &ВыбранныйДоговор
|			ИЛИ &ВыбранныйДоговор = 0)";
...
Запрос.УстановитьПараметр("ВыбранныйКонтрагент", Контрагент);
Запрос.УстановитьПараметр("ВыбранныйДоговор", ВыбранныйДоговор);
```

**Что добавлено по коду**
- Новый параметр функции: `ВыбранныйДоговор`.
- Новый отбор в `ГДЕ` по `ДоговорыВзаиморасчетов.Ссылка`.
- Новый параметр запроса: `Запрос.УстановитьПараметр("ВыбранныйДоговор", ...)`.

---

## 4) `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`

### Участок 4.1: исправление расчета строки и лимита списка

**Код до**
```bsl
ВсеКромеПС=Лев(РабочийКод,СтрДлина(РабочийКод-1));
...
СтрокаТабТекст= СтрокаТабТекст+"&total=1000";
```

**Код после**
```bsl
ВсеКромеПС=Лев(РабочийКод,СтрДлина(РабочийКод)-1);
...
//СтрокаТабТекст= СтрокаТабТекст+"&total=1000";
```

**Что добавлено по коду**
- Исправлена скобка в вычислении длины `СтрДлина(РабочийКод)-1`.
- Лимит `total=1000` переведен в комментарий (отключен).

### Участок 4.2: форматирование `id` заявки и динамика категорий

**Код до**
```bsl
Если xmlId=НоваяСтрокаУИД и (Обрtitle="" или  title=Обрtitle) Тогда  
	ЗаявкаId=Заявка.Получить("id");
КонецЕсли;
...
+ "&fields[UF_CRM_49_CATEGORY][0]=1483"
+ "&fields[UF_CRM_49_CATEGORY][1]=2567"
```

**Код после**
```bsl
Если xmlId=НоваяСтрокаУИД и (Обрtitle="" или  title=Обрtitle) Тогда  
	ЗаявкаId=Формат(Заявка.Получить("id"),"ЧГ=0");
КонецЕсли;
...
ТекстКатегорий="";
Если НЕ (СписокКатегорий="" или СписокКатегорий=неопределено) Тогда
	СчетКатегорий=0;
	Для каждого СамаКватегория из СписокКатегорий цикл
		ТекстКатегорий=ТекстКатегорий+"&fields[UF_CRM_49_CATEGORY]["+Формат(СчетКатегорий,"ЧГ-0")+"]="+СокрЛП(СамаКватегория.Значение);
		СчетКатегорий=СчетКатегорий+1;
	КонецЦикла;
КонецЕсли;
```

**Что добавлено по коду**
- Добавлено форматирование `id` через `Формат(...,"ЧГ=0")`.
- Статические категории заменены на динамическое формирование по `СписокКатегорий`.

---

## 5) `DataProcessors/Обмен_с_ОЗОН/Forms/ФормаОбмена/Ext/Form/Module.bsl`

### Участок 5.1: массовая выгрузка по всем площадкам

**Код до**
```bsl
&НаКлиенте
Процедура ЗапросЗаказов(Команда)
	ЗапросЗаказовНаСервере();
КонецПроцедуры
```

**Код после**
```bsl
&НаКлиенте
Процедура ПоВсемПлощадкам(Команда)
	ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда)
	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ
	|	ТорговыеПлощадки.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ТорговыеПлощадки КАК ТорговыеПлощадки
	|ГДЕ
	|	ТорговыеПлощадки.УчаствуетВАвтообмене
	|	И НЕ ТорговыеПлощадки.ПометкаУдаления";

	ТР=Запрос.Выполнить().Выгрузить();
	Для каждого СТР из ТР цикл
		Сценарий=СТР.Ссылка;
		ЗаполнитьОбразцыВыгрузки();
		ВыгрузитьВыбранноеНаСервере();
	КонецЦикла;
КонецПроцедуры
```

**Что добавлено по коду**
- Новая клиентская команда `ПоВсемПлощадкам`.
- Новая серверная процедура с выборкой активных площадок.
- Цикл по площадкам с вызовом `ВыгрузитьВыбранноеНаСервере()`.

---

## 6) `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/RecordSetModule.bsl`

### Участок 6.1: упрощение структуры модуля

**Код до**
```bsl
////////////////////////////////////////////////////////////////////////////////
// odin+ (11.03.26) ARM-120: Оценка автомобиля на выкуп после машинозаезда
////////////////////////////////////////////////////////////////////////////////
#Область ОписаниеПеременных
...
#Область ОбработчикиСобытий
...
#Область Инициализация
...
```

**Код после**
```bsl
////////////////////////////////////////////////////////////////////////////////
// odin+ (11.03.26) ARM-120: Оценка автомобиля на выкуп после машинозаезда
////////////////////////////////////////////////////////////////////////////////
```

**Что добавлено по коду**
- Нового кода не добавлено; выполнено сокращение шаблонных областей модуля.
