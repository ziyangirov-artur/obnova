# (Код) 19.03.2026 Еженедельный анализ обновления конфигурации

- Детальный разбор по изменённому коду: `analis/анализ кода 19.03.md`

## 1. Новые объекты (добавлены в обновлении)

### Константы
- `ИнтервалПроверкиНовыхСообщенийПользователю` — интервал проверки новых сообщений пользователю
- `ТаймаутФормыСообщениеПользователюЛичное` — таймаут для формы личного сообщения пользователю
- `ТаймаутФормыСообщениеПользователюОбщее` — таймаут для формы общего сообщения пользователю

### Обработки (DataProcessors)
- `ЗаявкиРДС_Битрикс` — обработка интеграции заявок РДС с Битрикс
- `ObjectModule.bsl` — расширение/модуль обработки «ЗаявкиРДС_Битрикс»
- `ОбъединениеРабочихЛистовОП` — форма для объединения рабочих листов ОП при переносе клиентов CRM
- `Form.bin` — макет/расширение формы «ОбъединениеРабочихЛистовОП»

### Перечисления (Enums)
- `СтатусыЗаявокВВеб` — перечисление статусов заявок в веб
- `ManagerModule.bsl` — модуль управления перечислением «СтатусыЗаявокВВеб»

### Регистры сведений (InformationRegisters)
- `ОчередьНаПолучениеСертификатаВыкупаИзWeb` — очередь/регистр сведений для получения сертификата выкупа из Web
- `ManagerModule.bsl` — Manager-модуль регистра очереди «ОчередьНаПолучениеСертификатаВыкупаИзWeb»
- `RecordSetModule.bsl` — RecordSet-модуль регистра очереди «ОчередьНаПолучениеСертификатаВыкупаИзWeb»
- `Выбор.xml` — форма выбора записей регистра очереди «ОчередьНаПолучениеСертификатаВыкупаИзWeb»
- `Form.bin` — макет формы выбора записей регистра очереди «ОчередьНаПолучениеСертификатаВыкупаИзWeb»

## 2. Изменённые объекты

- Всего изменено (файлы/объекты): 65

Подробный разбор логики, где именно внесены изменения, приведён в разделе «Изменённые .bsl модули».

## 3. Изменённые .bsl модули

## CommonModules/Odin_Общий/Ext/Module.bsl

### №12247 (12.03.2026)

**Изменение в Процедура ОбновитьЗадачиПоДолгами:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлен/расширен признак `НеБлокирует1С` с учетом списка исключений пользователей.

**Код До:**

```bsl
		НовЗадача.Тема 						= пТема;
		НовЗадача.Описание 					= пОписание;
		//+Бардашов Н.А. 19.02.2026 №12247  
		ПользователиИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьПараметрыСписком("ПользователиБезБлокировкиСДолгомПоСделкам"); 
		Если ПользователиИсключения.Найти(пПользователь) <> Неопределено Тогда 
			НовЗадача.НеБлокирует1С = Истина;
		КонецЕсли;	
		//-Бардашов Н.А. 19.02.2026 №12247
		НаборЗадач.Записать();
			
	ИначеЕсли НаборЗадач.Количество() <> 0 И СуммаДолга <> 0 Тогда
		// Обновим описание задачи
		ТекЗадача = НаборЗадач[0];
		ТекЗадача.Пользователь 				= пПользователь; // odin+ (30.04.19) Пользователя тоже обновим, вдруг в сделке сотрудник поменялся
		ТекЗадача.Тема 						= пТема;
		ТекЗадача.Описание 					= пОписание;
		НаборЗадач.Записать();
	КонецЕсли;
КонецПроцедуры	
```

**Код После:**

```bsl
		НовЗадача.Описание 					= пОписание;
		//+Бардашов Н.А. 19.02.2026 №12247  
		ПользователиИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьПараметрыСписком("ПользователиБезБлокировкиСДолгомПоСделкам"); 
		Если ПользователиИсключения.Найти(пПользователь) <> Неопределено Тогда 
			НовЗадача.НеБлокирует1С = Истина;
		КонецЕсли;	
		//-Бардашов Н.А. 19.02.2026 №12247
		НаборЗадач.Записать();
			
	ИначеЕсли НаборЗадач.Количество() <> 0 И СуммаДолга <> 0 Тогда
		// Обновим описание задачи
		ТекЗадача = НаборЗадач[0];
		ТекЗадача.Пользователь 				= пПользователь; // odin+ (30.04.19) Пользователя тоже обновим, вдруг в сделке сотрудник поменялся
		ТекЗадача.Тема 						= пТема;
		ТекЗадача.Описание 					= пОписание;
		//+Бардашов Н.А. 12.03.2026 №12247  
		ПользователиИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьПараметрыСписком("ПользователиБезБлокировкиСДолгомПоСделкам"); 
		Если ПользователиИсключения.Найти(пПользователь) <> Неопределено Тогда 
			НовЗадача.НеБлокирует1С = Истина;
		КонецЕсли;	
		//-Бардашов Н.А. 12.03.2026 №12247
		НаборЗадач.Записать();
	КонецЕсли;
КонецПроцедуры	
```

**Данный участок кода:** (изменённый/добавленный фрагмент)

- **Удалено:** строка `НовЗадача.Тема = пТема;` в блоке создания новой задачи.
- **Добавлено:** в ветке обновления существующей задачи (ИначеЕсли НаборЗадач.Количество() <> 0...) — проверка исключений и установка `НеБлокирует1С`:

```bsl
		ПользователиИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьПараметрыСписком("ПользователиБезБлокировкиСДолгомПоСделкам"); 
		Если ПользователиИсключения.Найти(пПользователь) <> Неопределено Тогда 
			НовЗадача.НеБлокирует1С = Истина;
		КонецЕсли;	
```

---

### №11555 (16.03.2026)

**Изменение в Процедура СоздатьОбновитьДокументСобытие:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Расширено условие обработки по `Входящий трафик` (влияние на ветвление/ранний возврат).

**Код До:**

```bsl
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
			
		КонецЕсли;
		
	КонецЕсли;
	
	//+Бардашов Н.А. 30.01.2026 №11555 
	ПодразделенияИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьНастройки("ПодразделениеНеСоздаватьЗадачи");  
	Если ПодразделенияИсключения.Количество() <> 0 Тогда 
		ПодразделениеДокумента = ДокументСсылка.ПодразделениеКомпании;
		ЗначениеНастройки = ПодразделенияИсключения.Найти(ПодразделениеДокумента,"Параметр");
		Если ЗначениеНастройки <> Неопределено И (ЗначениеСвойства = "Звонок заботы" 
			Или ЗначениеСвойства = "Рекомендация 1"
			Или ЗначениеСвойства = "Рекомендация 2"
			Или ДопПараметр = "Дружеские отношения") Тогда
			Возврат;
		КонецЕсли;                                           
	КонецЕсли;	
	//-Бардашов Н.А. 30.01.2026 №11555

	Если ДопПараметр = "Пригласить на ТО" ИЛИ  ДопПараметр = "Дружеские отношения" Тогда
		Содержание		 = ?(ДопПараметр = "Пригласить на ТО", "Необходимо связаться с клиентом для записи на ТО ", "Необходимо связаться с клиентом для поддержания дружеских отношений ")
							+ ДокументСсылка.ДокументОснование.Контрагент + " клиент приобрел " + ДокументСсылка.ДокументОснование.Автомобили.Выгрузить()[0].Автомобиль + ". Дата покупки " + ДокументСсылка.ДокументОснование.Дата; 
		ДатаСобытия      = ?(ДопПараметр = "Пригласить на ТО", ДокументСсылка.ДокументОснование.Дата + 2592000, 
						   ?(Свойство = "Пригласить на ТО", ДокументСсылка.ДокументОснование.Дата + 7776000, ДокументСсылка.ДатаНачала + 7776000)); // Если пригласить на ТО, то через 30 дней иначе каждые 90 дней
```

**Код После:**

```bsl
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
			
		КонецЕсли;
		
	КонецЕсли;
	
	//+Бардашов Н.А. 30.01.2026 №11555 
	ПодразделенияИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьНастройки("ПодразделениеНеСоздаватьЗадачи");  
	Если ПодразделенияИсключения.Количество() <> 0 Тогда 
		ПодразделениеДокумента = ДокументСсылка.ПодразделениеКомпании;
		ЗначениеНастройки = ПодразделенияИсключения.Найти(ПодразделениеДокумента,"Параметр");
		Если ЗначениеНастройки <> Неопределено И (ЗначениеСвойства = "Звонок заботы" 
			Или ЗначениеСвойства = "Рекомендация 1"
			Или ЗначениеСвойства = "Рекомендация 2"
			Или ДопПараметр = "Дружеские отношения") 
			Или Свойство = "Входящий трафик" Тогда //+Бардашов Н.А. 16.03.2026 №11555
			Возврат;
		КонецЕсли;                                           
	КонецЕсли;	
	//-Бардашов Н.А. 30.01.2026 №11555

	Если ДопПараметр = "Пригласить на ТО" ИЛИ  ДопПараметр = "Дружеские отношения" Тогда
		Содержание		 = ?(ДопПараметр = "Пригласить на ТО", "Необходимо связаться с клиентом для записи на ТО ", "Необходимо связаться с клиентом для поддержания дружеских отношений ")
							+ ДокументСсылка.ДокументОснование.Контрагент + " клиент приобрел " + ДокументСсылка.ДокументОснование.Автомобили.Выгрузить()[0].Автомобиль + ". Дата покупки " + ДокументСсылка.ДокументОснование.Дата; 
		ДатаСобытия      = ?(ДопПараметр = "Пригласить на ТО", ДокументСсылка.ДокументОснование.Дата + 2592000, 
```

**Данный участок кода:** в условии для раннего возврата добавлено «Или Свойство = "Входящий трафик"» — при входящем трафике задача не создаётся.

```bsl
			Или Свойство = "Входящий трафик" Тогда //+Бардашов Н.А. 16.03.2026 №11555
			Возврат;
```

---

### №13294 (13.03.2026)

**Изменение в Процедура СоздатьОбновитьДокументСобытие:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Изменена логика определения `ВидСобытия` для телемаркетолога.

**Код До:**

```bsl
		//+Бардашов Н.А. 13.10.2025 №4511 Добавлена Попытка
		Попытка
			НовыйДокументСобытие.ДокументОснование = ?(ЕстьПредыдущееСобытие, ПоследнееСобытие.Ссылка, ДокументСсылка);
		Исключение
			НовыйДокументСобытие.ДокументОснование = ДокументСсылка;
			ЕстьПредыдущееСобытие = Ложь;
		КонецПопытки;
		// Вафина-
		НовыйДокументСобытие.Заполнить(НовыйДокументСобытие.ДокументОснование);
		
		НовыйДокументСобытие.Дата			 			  = ТекущаяДата(); // Каргина+ (04.06.2024) - теперь записываем события текущей датой
		НовыйДокументСобытие.Тема			 			  = ?(Свойство = "Входящий трафик", "Повторный контакт по входящему трафику", ЗначениеСвойства);
		НовыйДокументСобытие.Приоритет 	     			  = Перечисления.Важность.Высокая;
		НовыйДокументСобытие.Состояние 	      			  = Перечисления.СостоянияСобытий.Запланировано;
		НовыйДокументСобытие.ТипСообщения      			  = ?(ЭтоРабочийЛистОП, Перечисления.ТипыСообщений.Входящее, Перечисления.ТипыСообщений.Исходящее);
		НовыйДокументСобытие.ВидСобытия        			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);
		НовыйДокументСобытие.ДатаНачала 	   			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ДатаПришел, ДатаСобытия);
		НовыйДокументСобытие.Результат					  = "";
		//+Бардашов Н.А. №5992 26.09.2025
		Попытка
			НовыйДокументСобытие.Маркер 				  =	СтруктураДанных.Маркер;
		Исключение
		КонецПопытки;
		//-Бардашов Н.А. №5992 26.09.2025
		Если ЗначениеЗаполнено(НовыйДокументСобытие.ДатаОкончания) Тогда
			НовыйДокументСобытие.ДатаОкончания = "";
```

**Код После:**

```bsl
		//+Бардашов Н.А. 13.10.2025 №4511 Добавлена Попытка
		Попытка
			НовыйДокументСобытие.ДокументОснование = ?(ЕстьПредыдущееСобытие, ПоследнееСобытие.Ссылка, ДокументСсылка);
		Исключение
			НовыйДокументСобытие.ДокументОснование = ДокументСсылка;
			ЕстьПредыдущееСобытие = Ложь;
		КонецПопытки;
		// Вафина-
		НовыйДокументСобытие.Заполнить(НовыйДокументСобытие.ДокументОснование);
		
		НовыйДокументСобытие.Дата			 			  = ТекущаяДата(); // Каргина+ (04.06.2024) - теперь записываем события текущей датой
		НовыйДокументСобытие.Тема			 			  = ?(Свойство = "Входящий трафик", "Повторный контакт по входящему трафику", ЗначениеСвойства);
		НовыйДокументСобытие.Приоритет 	     			  = Перечисления.Важность.Высокая;
		НовыйДокументСобытие.Состояние 	      			  = Перечисления.СостоянияСобытий.Запланировано;
		НовыйДокументСобытие.ТипСообщения      			  = ?(ЭтоРабочийЛистОП, Перечисления.ТипыСообщений.Входящее, Перечисления.ТипыСообщений.Исходящее);
		//+Бардашов Н.А. №13294 13.03.2026
		Если СтруктураДанных.Свойство("Телемаркетолог") Тогда
			НовыйДокументСобытие.ВидСобытия        		  = СтруктураДанных.ВидКонтакта;
		Иначе
			НовыйДокументСобытие.ВидСобытия        		  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);		
		КонецЕсли;
		//НовыйДокументСобытие.ВидСобытия        		  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);
		//-Бардашов Н.А. №13294 13.03.2026
		НовыйДокументСобытие.ДатаНачала 	   			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ДатаПришел, ДатаСобытия);
		НовыйДокументСобытие.Результат					  = "";
		//+Бардашов Н.А. №5992 26.09.2025
```

**Данный участок кода:** вместо одной строки присвоения `ВидСобытия` — ветвление по телемаркетологу.

- **Удалено:** `НовыйДокументСобытие.ВидСобытия = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);`
- **Добавлено:**

```bsl
		//+Бардашов Н.А. №13294 13.03.2026
		Если СтруктураДанных.Свойство("Телемаркетолог") Тогда
			НовыйДокументСобытие.ВидСобытия        		  = СтруктураДанных.ВидКонтакта;
		Иначе
			НовыйДокументСобытие.ВидСобытия        		  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);		
		КонецЕсли;
		//НовыйДокументСобытие.ВидСобытия ...
		//-Бардашов Н.А. №13294 13.03.2026
```

---

### №12823 (10.03.2026)

**Изменение в Процедура СоздатьОбновитьДокументСобытие:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлена/расширена работа с `НикТГ`: сохранение на событии и запись в регистр `КонтактнаяИнформация`.

**Код До:**

```bsl
		НовыйДокументСобытие.Заполнить(НовыйДокументСобытие.ДокументОснование);
		
		НовыйДокументСобытие.Дата			 			  = ТекущаяДата(); // Каргина+ (04.06.2024) - теперь записываем события текущей датой
		НовыйДокументСобытие.Тема			 			  = ?(Свойство = "Входящий трафик", "Повторный контакт по входящему трафику", ЗначениеСвойства);
		НовыйДокументСобытие.Приоритет 	     			  = Перечисления.Важность.Высокая;
		НовыйДокументСобытие.Состояние 	      			  = Перечисления.СостоянияСобытий.Запланировано;
		НовыйДокументСобытие.ТипСообщения      			  = ?(ЭтоРабочийЛистОП, Перечисления.ТипыСообщений.Входящее, Перечисления.ТипыСообщений.Исходящее);
		НовыйДокументСобытие.ВидСобытия        			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);
		НовыйДокументСобытие.ДатаНачала 	   			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ДатаПришел, ДатаСобытия);
		НовыйДокументСобытие.Результат					  = "";
		//+Бардашов Н.А. №5992 26.09.2025
		Попытка
			НовыйДокументСобытие.Маркер 				  =	СтруктураДанных.Маркер;
		Исключение
		КонецПопытки;
		//-Бардашов Н.А. №5992 26.09.2025
		Если ЗначениеЗаполнено(НовыйДокументСобытие.ДатаОкончания) Тогда
			НовыйДокументСобытие.ДатаОкончания = "";
		КонецЕсли;
		Если ЭтоРабочийЛистОП Тогда
			НовыйДокументСобытие.ДатаОкончания = СтруктураДанных.ДатаУшел;
			НовыйДокументСобытие.Контрагент    = СтруктураДанных.Контрагент;
		КонецЕсли;
		НовыйДокументСобытие.КонтактноеЛицо    			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.Контрагент, ДокументСсылка.Контрагент);
		НовыйДокументСобытие.Пользователи[0].Пользователь = ПолучитьПользователяПоСотруднику(ДокументСсылка.Менеджер);
		НовыйДокументСобытие.ФорматТекста	 			  = Перечисления.ФорматТекста.ПростойТекст;
```

**Код После:**

```bsl
		Если СтруктураДанных.Свойство("Телемаркетолог") Тогда
			НовыйДокументСобытие.ВидСобытия        		  = СтруктураДанных.ВидКонтакта;
		Иначе
			НовыйДокументСобытие.ВидСобытия        		  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);		
		КонецЕсли;
		//НовыйДокументСобытие.ВидСобытия        		  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ВидКонтакта, Перечисления.ВидыСобытий.ТелефонныйЗвонок);
		//-Бардашов Н.А. №13294 13.03.2026
		НовыйДокументСобытие.ДатаНачала 	   			  = ?(ЭтоРабочийЛистОП, СтруктураДанных.ДатаПришел, ДатаСобытия);
		НовыйДокументСобытие.Результат					  = "";
		//+Бардашов Н.А. №5992 26.09.2025
		Попытка
			НовыйДокументСобытие.Маркер 				  =	СтруктураДанных.Маркер;
		Исключение
		КонецПопытки;
		//-Бардашов Н.А. №5992 26.09.2025 
		//+Бардашов Н.А. №12823 10.03.2026 
		Если СтруктураДанных.Свойство("НикТГ") Тогда
			НовыйДокументСобытие.НикТГ = СтруктураДанных.НикТГ;
		КонецЕсли;
		
		Если СтруктураДанных.Свойство("Контрагент") 
			И ТипЗнч(СтруктураДанных.Контрагент) = Тип("СправочникСсылка.Контрагенты") 
			И СтруктураДанных.Свойство("НикТГ") 
			И ЗначениеЗаполнено(СтруктураДанных.НикТГ) Тогда
		
			ЗаписьКИ = РегистрыСведений.КонтактнаяИнформация.СоздатьМенеджерЗаписи();
```

**Данный участок кода:** добавлены сохранение НикТГ на событии и запись в регистр КонтактнаяИнформация.

- **Добавлено:** блок проверки «НикТГ» и присвоение `НовыйДокументСобытие.НикТГ`; условие по Контрагенту + НикТГ и создание менеджера записи `КонтактнаяИнформация` (начало блока).

```bsl
		//+Бардашов Н.А. №12823 10.03.2026 
		Если СтруктураДанных.Свойство("НикТГ") Тогда
			НовыйДокументСобытие.НикТГ = СтруктураДанных.НикТГ;
		КонецЕсли;
		
		Если СтруктураДанных.Свойство("Контрагент") 
			И ТипЗнч(СтруктураДанных.Контрагент) = Тип("СправочникСсылка.Контрагенты") 
			И СтруктураДанных.Свойство("НикТГ") 
			И ЗначениеЗаполнено(СтруктураДанных.НикТГ) Тогда
			ЗаписьКИ = РегистрыСведений.КонтактнаяИнформация.СоздатьМенеджерЗаписи();
```

---

### Изменение кода

**Изменение в Процедура СоздатьОбновитьДокументСобытие:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Изменено состояние события по тест-драйву на `Запланировано`.

**Код До:**

```bsl
		ОбновитьСвойствоДокументаСобытие(СобытиеПоРекомендации.Ссылка, "Полученная рекомендация");
		
	ИначеЕсли ДопПараметр = "Тест драйв" Тогда
		СобытиеПоТестДрайву = Документы.Событие.СоздатьДокумент();
		СобытиеПоТестДрайву.ДокументОснование = ДокументСсылка;
		СобытиеПоТестДрайву.Заполнить(СобытиеПоТестДрайву.ДокументОснование);
		
		СобытиеПоТестДрайву.Дата			 			 = ТекущаяДата(); // Каргина+ (04.06.2024) - теперь записываем события текущей датой
		СобытиеПоТестДрайву.Тема			   			 = "Тест драйв";
		СобытиеПоТестДрайву.ДатаНачала 	   				 = СтруктураДанных.ДатаПришел;
		СобытиеПоТестДрайву.ДатаОкончания				 = СтруктураДанных.ДатаУшел;
		СобытиеПоТестДрайву.Контрагент    				 = СтруктураДанных.Контрагент;
		СобытиеПоТестДрайву.КонтактноеЛицо				 = СтруктураДанных.Контрагент;
		СобытиеПоТестДрайву.Приоритет					 = Перечисления.Важность.Высокая;
		СобытиеПоТестДрайву.ТипСообщения      			 = Перечисления.ТипыСообщений.Входящее;
		СобытиеПоТестДрайву.Состояние 	      			 = Перечисления.СостоянияСобытий.Завершено;
		СобытиеПоТестДрайву.ВидСобытия					 = Перечисления.ВидыСобытий.ЛичнаяВстреча;
		СобытиеПоТестДрайву.Пользователи[0].Пользователь = ПолучитьПользователяПоСотруднику(ДокументСсылка.Менеджер);
		СобытиеПоТестДрайву.ФорматТекста 				 = Перечисления.ФорматТекста.ПростойТекст;
		СобытиеПоТестДрайву.Содержание					 = "Клиент воспользовался услугой тест драйв";
		
		// Каргина+ (02.06.2024)
		#Если Клиент Тогда
		глПрава[обПолучитьСсылкуПВХПравИНастроек("Проведение задним числом")]		    = Истина;
		глПрава[обПолучитьСсылкуПВХПравИНастроек("Редактировать в заданном интервале")] = Ложь;
		глПрава[обПолучитьСсылкуПВХПравИНастроек("Проведение документа раньше документа основания")] = Истина; // Каргина+ (04.06.2024) - добавила еще одно право
```

**Код После:**

```bsl
		ОбновитьСвойствоДокументаСобытие(СобытиеПоРекомендации.Ссылка, "Полученная рекомендация");
		
	ИначеЕсли ДопПараметр = "Тест драйв" Тогда
		СобытиеПоТестДрайву = Документы.Событие.СоздатьДокумент();
		СобытиеПоТестДрайву.ДокументОснование = ДокументСсылка;
		СобытиеПоТестДрайву.Заполнить(СобытиеПоТестДрайву.ДокументОснование);
		
		СобытиеПоТестДрайву.Дата			 			 = ТекущаяДата(); // Каргина+ (04.06.2024) - теперь записываем события текущей датой
		СобытиеПоТестДрайву.Тема			   			 = "Тест драйв";
		СобытиеПоТестДрайву.ДатаНачала 	   				 = СтруктураДанных.ДатаПришел;
		СобытиеПоТестДрайву.ДатаОкончания				 = СтруктураДанных.ДатаУшел;
		СобытиеПоТестДрайву.Контрагент    				 = СтруктураДанных.Контрагент;
		СобытиеПоТестДрайву.КонтактноеЛицо				 = СтруктураДанных.Контрагент;
		СобытиеПоТестДрайву.Приоритет					 = Перечисления.Важность.Высокая;
		СобытиеПоТестДрайву.ТипСообщения      			 = Перечисления.ТипыСообщений.Входящее;        
		//+Бардашов
		СобытиеПоТестДрайву.Состояние 	      			 = Перечисления.СостоянияСобытий.Запланировано;
		//СобытиеПоТестДрайву.Состояние 	      			 = Перечисления.СостоянияСобытий.Завершено;
        //-Бардашов
		СобытиеПоТестДрайву.ВидСобытия					 = Перечисления.ВидыСобытий.ЛичнаяВстреча;
		СобытиеПоТестДрайву.Пользователи[0].Пользователь = ПолучитьПользователяПоСотруднику(ДокументСсылка.Менеджер);
		СобытиеПоТестДрайву.ФорматТекста 				 = Перечисления.ФорматТекста.ПростойТекст;
		СобытиеПоТестДрайву.Содержание					 = "Клиент воспользовался услугой тест драйв";
		
		// Каргина+ (02.06.2024)
		#Если Клиент Тогда
```

**Данный участок кода:** состояние события по тест-драйву изменено с «Завершено» на «Запланировано».

- **Удалено:** `СобытиеПоТестДрайву.Состояние = Перечисления.СостоянияСобытий.Завершено;`
- **Добавлено:**

```bsl
		//+Бардашов
		СобытиеПоТестДрайву.Состояние 	      			 = Перечисления.СостоянияСобытий.Запланировано;
		//СобытиеПоТестДрайву.Состояние 	      			 = Перечисления.СостоянияСобытий.Завершено;
        //-Бардашов
```

---

### №16 (16.03.2026)

**Изменение в Процедура СоздатьОбновитьРабочийЛистОП:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлена подстановка/обработка `НикТГ` в данных и дальнейших действиях.

**Код До:**

```bsl
		Если НЕ РабочийЛист = Документы.РабочийЛист.ПустаяСсылка() Тогда // Каргина- (02.06.2024)
			НовыйРабочийЛистОП = РабочийЛист.ПолучитьОбъект();
		Иначе
			НовыйРабочийЛистОП = Документы.РабочийЛист.СоздатьДокумент();
			НовыйРабочийЛистОП.Заполнить(НовыйРабочийЛистОП.ДокументОснование);
			НовыйРабочийЛистОП.Дата =  Дата(ДатаПрихода + ВремяПрихода);
		КонецЕсли;
			
		НовыйРабочийЛистОП.Организация 				= СтрокаРегистра.Организация;
		НовыйРабочийЛистОП.ПодразделениеКомпании 	= СтрокаРегистра.Подразделение;
		НовыйРабочийЛистОП.Автор 					= СтрокаРегистра.Автор;
		НовыйРабочийЛистОП.Менеджер 				= СтрокаРегистра.Участник;
		НовыйРабочийЛистОП.Контрагент 				= Контрагент;
		НовыйРабочийЛистОП.КодРегиона 				= СтрокаРегистра.КодРегиона;
		НовыйРабочийЛистОП.КодГорода 				= СтрокаРегистра.КодГорода;
		НовыйРабочийЛистОП.НомерТелефона 			= СтрокаРегистра.НомерТелефона;
		НовыйРабочийЛистОП.ПредставлениеТелефона 	= СтрокаРегистра.ПредставлениеТелефона;
		НовыйРабочийЛистОП.Статус 					= ?(ЗначениеЗаполнено(НовыйРабочийЛистОП.Статус), НовыйРабочийЛистОП.Статус, Справочники.СтатусыРабочегоЛиста.Создан);
		// Каргина+ (08.07.2024) - поле "Реклама" теперь обязательное при регистрации контакта, используем его. Если обращение из LMS, указываем "сайт импортерский"
		НовыйРабочийЛистОП.Реклама				 	= ?(СсылкаНаЛидLMS = Неопределено, СтрокаРегистра.Реклама, Справочники.ИсточникиИнформации.НайтиПоКоду("ЦБ000007"));
		// Каргина-
		НовыйРабочийЛистОП.РуководительМенеджера 	= РуководительМенеджера;
		НовыйРабочийЛистОП.ВидКонтакта 				= СтрокаРегистра.ВидКонтакта;
		НовыйРабочийЛистОП.Пол 						= Перечисления.ПолФизическихЛиц.НеУказан;
		НовыйРабочийЛистОП.ПрограммныйЗапрос 		= Истина;
		НовыйРабочийЛистОП.КонтактноеЛицо 			= СтрокаРегистра.Клиент;
```

**Код После:**

```bsl
			НовыйРабочийЛистОП = РабочийЛист.ПолучитьОбъект();
		Иначе
			НовыйРабочийЛистОП = Документы.РабочийЛист.СоздатьДокумент();
			НовыйРабочийЛистОП.Заполнить(НовыйРабочийЛистОП.ДокументОснование);
			НовыйРабочийЛистОП.Дата =  Дата(ДатаПрихода + ВремяПрихода);
		КонецЕсли;
			
		НовыйРабочийЛистОП.Организация 				= СтрокаРегистра.Организация;
		НовыйРабочийЛистОП.ПодразделениеКомпании 	= СтрокаРегистра.Подразделение;
		НовыйРабочийЛистОП.Автор 					= СтрокаРегистра.Автор;
		НовыйРабочийЛистОП.Менеджер 				= СтрокаРегистра.Участник;
		НовыйРабочийЛистОП.Контрагент 				= Контрагент;
		НовыйРабочийЛистОП.КодРегиона 				= СтрокаРегистра.КодРегиона;
		НовыйРабочийЛистОП.КодГорода 				= СтрокаРегистра.КодГорода;
		НовыйРабочийЛистОП.НомерТелефона 			= СтрокаРегистра.НомерТелефона; 
		//+Бардашов Н.А. № 16.03.2026 №12823
		НовыйРабочийЛистОП.НикТГ 					= СтрокаРегистра.НикТГ;
		//-Бардашов Н.А. № 16.03.2026 №12823
		НовыйРабочийЛистОП.ПредставлениеТелефона 	= СтрокаРегистра.ПредставлениеТелефона;
		НовыйРабочийЛистОП.Статус 					= ?(ЗначениеЗаполнено(НовыйРабочийЛистОП.Статус), НовыйРабочийЛистОП.Статус, Справочники.СтатусыРабочегоЛиста.Создан);
		// Каргина+ (08.07.2024) - поле "Реклама" теперь обязательное при регистрации контакта, используем его. Если обращение из LMS, указываем "сайт импортерский"
		НовыйРабочийЛистОП.Реклама				 	= ?(СсылкаНаЛидLMS = Неопределено, СтрокаРегистра.Реклама, Справочники.ИсточникиИнформации.НайтиПоКоду("ЦБ000007"));
		// Каргина-
		НовыйРабочийЛистОП.РуководительМенеджера 	= РуководительМенеджера;
		НовыйРабочийЛистОП.ВидКонтакта 				= СтрокаРегистра.ВидКонтакта;
		НовыйРабочийЛистОП.Пол 						= Перечисления.ПолФизическихЛиц.НеУказан;
```

**Данный участок кода:** добавлено присвоение поля НикТГ; убрана проверка «Если НЕ РабочийЛист = …» в начале (упрощение условия).

- **Удалено:** строка с условием `Если НЕ РабочийЛист = Документы.РабочийЛист.ПустаяСсылка() Тогда` (в «Код После» — сразу `НовыйРабочийЛистОП = РабочийЛист.ПолучитьОбъект()`).
- **Добавлено:**

```bsl
		//+Бардашов Н.А. № 16.03.2026 №12823
		НовыйРабочийЛистОП.НикТГ 					= СтрокаРегистра.НикТГ;
		//-Бардашов Н.А. № 16.03.2026 №12823
```

---

### №12823 (10.03.2026)

**Изменение в Процедура СоздатьОбновитьРабочийЛистОП:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлена подстановка/обработка `НикТГ` в данных и дальнейших действиях.

**Код До:**

```bsl
		СтруктураДанных.Вставить("ВидКонтакта", 	СтрокаРегистра.ВидКонтакта);
		СтруктураДанных.Вставить("Контрагент",  	СтрокаРегистра.Клиент);
		СтруктураДанных.Вставить("Идентификация",   СтрокаРегистра.Идентификация);
		
		НачатьТранзакцию();
		// Каргина+ (04.06.2024) - все процедуры по созданию РЛ и событий в одной попытке и транзакции
		Попытка
			НовыйРабочийЛистОП.ПометкаУдаления = Ложь;
			НовыйРабочийЛистОП.Записать(РежимЗаписиДокумента.Проведение);
			//+Бардашов Н.А. 23.09.2025 №6104 Добавил проверку на Тест-драйв
			Если НЕ СтрокаРегистра.фТестДрайв Тогда 
				//Вафина+ (28.02.24) добавлено условие
				Если Обращение Тогда  
					// Вафина+ (07.06.24) скорректировала условие, т.к. сейчас параметр Обращение - Булево
					Пользователь = ПолучитьПользователяПоСотруднику(НовыйРабочийЛистОП.Менеджер);
					
					Если НЕ обПраво("ОтменитьСозданиеСобытия",,,,Пользователь)  Тогда
						// Вафина+ (21.05.24) #8525291 если нет права у менеджера раб листа ОП, тогда создаем на него событие
						СоздатьОбновитьДокументСобытие(НовыйРабочийЛистОП.Ссылка, "Проведение", СтруктураДанных, Обращение); // Вафина+ (28.02.24) добавила параметр
					КонецЕсли;
				Иначе
					СоздатьОбновитьДокументСобытие(НовыйРабочийЛистОП.Ссылка, "Проведение", СтруктураДанных);
				КонецЕсли;
				//Вафина-
			КонецЕсли;
			
```

**Код После:**

```bsl
		СтруктураДанных.Вставить("ВидКонтакта", 	СтрокаРегистра.ВидКонтакта);
		СтруктураДанных.Вставить("Контрагент",  	СтрокаРегистра.Клиент);
		СтруктураДанных.Вставить("Идентификация",   СтрокаРегистра.Идентификация);
		
		НачатьТранзакцию();
		// Каргина+ (04.06.2024) - все процедуры по созданию РЛ и событий в одной попытке и транзакции
		Попытка
			НовыйРабочийЛистОП.ПометкаУдаления = Ложь;
			НовыйРабочийЛистОП.Записать(РежимЗаписиДокумента.Проведение);
			//+Бардашов Н.А. 23.09.2025 №6104 Добавил проверку на Тест-драйв
			Если НЕ СтрокаРегистра.фТестДрайв Тогда 
				//Вафина+ (28.02.24) добавлено условие
				Если Обращение Тогда  
					// Вафина+ (07.06.24) скорректировала условие, т.к. сейчас параметр Обращение - Булево
					Пользователь = ПолучитьПользователяПоСотруднику(НовыйРабочийЛистОП.Менеджер);
					//+Бардашов Н.А. №12823 10.03.2026
					Если СсылкаНаЛидLMS = Неопределено Тогда
						СтруктураДанных.Вставить("НикТГ",СтрокаРегистра.НикТГ);
					КонецЕсли;
					//-Бардашов Н.А. №12823 10.03.2026
					Если НЕ обПраво("ОтменитьСозданиеСобытия",,,,Пользователь)  Тогда
						// Вафина+ (21.05.24) #8525291 если нет права у менеджера раб листа ОП, тогда создаем на него событие
						СоздатьОбновитьДокументСобытие(НовыйРабочийЛистОП.Ссылка, "Проведение", СтруктураДанных, Обращение); // Вафина+ (28.02.24) добавила параметр
					КонецЕсли;
				Иначе
					СоздатьОбновитьДокументСобытие(НовыйРабочийЛистОП.Ссылка, "Проведение", СтруктураДанных);
```

**Данный участок кода:** в ветку «Обращение» добавлена вставка НикТГ в СтруктураДанных (если не из LMS).

```bsl
					//+Бардашов Н.А. №12823 10.03.2026
					Если СсылкаНаЛидLMS = Неопределено Тогда
						СтруктураДанных.Вставить("НикТГ",СтрокаРегистра.НикТГ);
					КонецЕсли;
					//-Бардашов Н.А. №12823 10.03.2026
```

---

## CommonModules/Шина1С/Ext/Module.bsl

### Изменение кода

**Изменение в процедуре/функции:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
﻿//Муртазин И. Ф. 04-03-2026
//блок отвечает за интеграцию с шиной
//==============================Философия======================================
//Пытаюсь реализовать принцип "отправил и забыл" - все данные отправляются по одному и тому же путь. Если сообщение отправилось
//и указаны правильные данные то дальше это уже проблема проблема BI-цев или ответственного за 1С-шину.  
//И програмист 1С может «включить дурачка» и сказать ЯСДЕЛАЛПОИНСТРУКЦИИНОНЕРАБОТАЕТПАМАГИТИ.
//ИнтеграцияСОсновнымСерверомДанных - отправка данных на сервер srvsql01.
//PostgresBI - отправка данных на личный Postgres сервер BI-цев.
//АвтодилерАктивный - создан на будущее для обмена с автодилером, пока не используется.


#Область ИнтеграцияСОсновнымСерверомДанных


```

**Код После:**

```bsl
﻿//Муртазин И. Ф. 04-03-2026
//блок отвечает за интеграцию с шиной
//==============================Философия======================================
//Пытаюсь реализовать принцип "отправил и забыл" - все данные отправляются по одному и тому же пути. Если сообщение отправилось
//и указаны правильные данные то дальше это уже проблема проблема BI-цев или ответственного за 1С-шину.  
//И програмист 1С может «включить дурачка» и сказать ЯСДЕЛАЛПОИНСТРУКЦИИНОНЕРАБОТАЕТПАМАГИТИ.
//ИнтеграцияСОсновнымСерверомДанных - отправка данных на сервер srvsql01.
//PostgresBI - отправка данных на личный Postgres сервер BI-цев.
//АвтодилерАктивный - создан на будущее для обмена с автодилером, пока не используется.


#Область ИнтеграцияСОсновнымСерверомДанных

//Муртазин И. Ф. 11-03-2026 задачи Консолидация данных (Задачи 13051, 13237) 
```

**Данный участок кода:** добавлен комментарий к области.

```bsl
//Муртазин И. Ф. 11-03-2026 задачи Консолидация данных (Задачи 13051, 13237) 
```

---

### Изменение кода

**Изменение в процедуре/функции:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
﻿//Муртазин И. Ф. 04-03-2026
//блок отвечает за интеграцию с шиной
//==============================Философия======================================
//Пытаюсь реализовать принцип "отправил и забыл" - все данные отправляются по одному и тому же путь. Если сообщение отправилось
//и указаны правильные данные то дальше это уже проблема проблема BI-цев или ответственного за 1С-шину.  
//И програмист 1С может «включить дурачка» и сказать ЯСДЕЛАЛПОИНСТРУКЦИИНОНЕРАБОТАЕТПАМАГИТИ.
//ИнтеграцияСОсновнымСерверомДанных - отправка данных на сервер srvsql01.
//PostgresBI - отправка данных на личный Postgres сервер BI-цев.
//АвтодилерАктивный - создан на будущее для обмена с автодилером, пока не используется.


#Область ИнтеграцияСОсновнымСерверомДанных



Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину() Экспорт

	//1 - это srvsql01
	перИндексПриёмщика = 1;	
	
	 ТабЗначПодразделения = ЗапроситьПодразделенияДляОсновнойБазы();
	 
	 ОтправитьТаблицуНаСервер(ТабЗначПодразделения, перИндексПриёмщика, 1);
```

**Код После:**

```bsl
﻿//Муртазин И. Ф. 04-03-2026
//блок отвечает за интеграцию с шиной
//==============================Философия======================================
//Пытаюсь реализовать принцип "отправил и забыл" - все данные отправляются по одному и тому же пути. Если сообщение отправилось
//и указаны правильные данные то дальше это уже проблема проблема BI-цев или ответственного за 1С-шину.  
//И програмист 1С может «включить дурачка» и сказать ЯСДЕЛАЛПОИНСТРУКЦИИНОНЕРАБОТАЕТПАМАГИТИ.
//ИнтеграцияСОсновнымСерверомДанных - отправка данных на сервер srvsql01.
//PostgresBI - отправка данных на личный Postgres сервер BI-цев.
//АвтодилерАктивный - создан на будущее для обмена с автодилером, пока не используется.


#Область ИнтеграцияСОсновнымСерверомДанных

//Муртазин И. Ф. 11-03-2026 задачи Консолидация данных (Задачи 13051, 13237) 
//Назначение области - отправка данных на сервер srvsql01
//Не имеет самостоятельного смысла без сервиса интеграции ШинаЗакамьеОсновнойПередатчикНаСервер
//Пока у финансового депортамента и BI-цев нет чёткого видения. На совещании были рассуждения в роде "Может надо, а может не надо? А что, пусть будет." 
//Поэтому приходится изменения делать мелкими шажками.
//Также стараюсь переносить изменения в рабочую базу, чтобы в sql не соседствовали актуальные данные с неактуальными.


//Муртазин И. Ф. 11-03-2026 - Добавил выгрузку реализаций в собственную таблицу на SQL dbo.Realizationz
//Муртазин И. Ф. 12-03-2026 - Добавил выгрузку доходов и расходов в dbo.fact_income_expense
//Муртазин И. Ф. 13-03-2026 - Добавил выгрузку доходов и расходов в dbo.dim_documents
```

---

### Изменение кода

**Изменение в процедуре/функции:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
﻿//Муртазин И. Ф. 04-03-2026
//блок отвечает за интеграцию с шиной
//==============================Философия======================================
//Пытаюсь реализовать принцип "отправил и забыл" - все данные отправляются по одному и тому же путь. Если сообщение отправилось
//и указаны правильные данные то дальше это уже проблема проблема BI-цев или ответственного за 1С-шину.  
//И програмист 1С может «включить дурачка» и сказать ЯСДЕЛАЛПОИНСТРУКЦИИНОНЕРАБОТАЕТПАМАГИТИ.
//ИнтеграцияСОсновнымСерверомДанных - отправка данных на сервер srvsql01.
//PostgresBI - отправка данных на личный Postgres сервер BI-цев.
//АвтодилерАктивный - создан на будущее для обмена с автодилером, пока не используется.


#Область ИнтеграцияСОсновнымСерверомДанных



Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину() Экспорт

	//1 - это srvsql01
	перИндексПриёмщика = 1;	
	
	 ТабЗначПодразделения = ЗапроситьПодразделенияДляОсновнойБазы();
	 
	 ОтправитьТаблицуНаСервер(ТабЗначПодразделения, перИндексПриёмщика, 1);
	 
	 
```

**Код После:**

```bsl
//ИнтеграцияСОсновнымСерверомДанных - отправка данных на сервер srvsql01.
//PostgresBI - отправка данных на личный Postgres сервер BI-цев.
//АвтодилерАктивный - создан на будущее для обмена с автодилером, пока не используется.


#Область ИнтеграцияСОсновнымСерверомДанных

//Муртазин И. Ф. 11-03-2026 задачи Консолидация данных (Задачи 13051, 13237) 
//Назначение области - отправка данных на сервер srvsql01
//Не имеет самостоятельного смысла без сервиса интеграции ШинаЗакамьеОсновнойПередатчикНаСервер
//Пока у финансового депортамента и BI-цев нет чёткого видения. На совещании были рассуждения в роде "Может надо, а может не надо? А что, пусть будет." 
//Поэтому приходится изменения делать мелкими шажками.
//Также стараюсь переносить изменения в рабочую базу, чтобы в sql не соседствовали актуальные данные с неактуальными.


//Муртазин И. Ф. 11-03-2026 - Добавил выгрузку реализаций в собственную таблицу на SQL dbo.Realizationz
//Муртазин И. Ф. 12-03-2026 - Добавил выгрузку доходов и расходов в dbo.fact_income_expense
//Муртазин И. Ф. 13-03-2026 - Добавил выгрузку доходов и расходов в dbo.dim_documents
Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину() Экспорт

	//1 - это srvsql01
	перИндексПриёмщика = 1;	
	
	 ТабЗначПодразделения = ЗапроситьПодразделенияДляОсновнойБазы();
	 
	 ОтправитьТаблицуНаСервер(ТабЗначПодразделения, перИндексПриёмщика, 1);
```

---

### Изменение кода

**Изменение в Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	 
	 ОтправитьТаблицуНаСервер(ТабЗначОрганизации, перИндексПриёмщика, 2);
	 
	 
	 ТабЗначЦентрыФинансовойОтветственности = ЗапроситьЦентрыФинансовойОтветственности();	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначЦентрыФинансовойОтветственности, перИндексПриёмщика, 3);
	 
	 датаНачало = НачалоДня(ТекущаяДата() - 86400);
	 датаКонец = КонецДня(ТекущаяДата() - 86400);
	 
	 ТабЗначРеализацииАвтомобилей = ЗапроситьРеализацииЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРеализацииАвтомобилей, перИндексПриёмщика, 4);

	 
КонецФункции // СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину()
```

**Код После:**

```bsl
	 ОтправитьТаблицуНаСервер(ТабЗначОрганизации, перИндексПриёмщика, 2);
	 
	 
	 ТабЗначЦентрыФинансовойОтветственности = ЗапроситьЦентрыФинансовойОтветственности();	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначЦентрыФинансовойОтветственности, перИндексПриёмщика, 3);
	 
	 датаНачало = НачалоДня(ТекущаяДата() - 86400);
	 датаКонец = КонецДня(ТекущаяДата() - 86400);
	 
	 ТабЗначРеализацииАвтомобилей = ЗапроситьРеализацииЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРеализацииАвтомобилей, перИндексПриёмщика, 4);
	 
	 
	 ТабЗначДоходыИРасходы = ЗапроситьДоходыИРасходыЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначДоходыИРасходы, перИндексПриёмщика, 5);
	 
	 
	 ТабЗначРегистраторыДоходовИРасходов = ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРегистраторыДоходовИРасходов, перИндексПриёмщика, 6);

	 
	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьСтрокиТаблицРегистраторовЗаПериод(датаНачало, датаКонец); 
```

**Данный участок кода:** добавлены вызовы выгрузки доходов/расходов и регистраторов: ЗапроситьДоходыИРасходыЗаПериод, ОтправитьТаблицуНаСервер(..., 5), ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод, ОтправитьТаблицуНаСервер(..., 6), ЗапроситьСтрокиТаблицРегистраторовЗаПериод и ОтправитьТаблицуНаСервер(..., 7). В начале блока убрана лишняя пустая строка.

```bsl
	 ТабЗначДоходыИРасходы = ЗапроситьДоходыИРасходыЗаПериод(датаНачало, датаКонец); 
	 ОтправитьТаблицуНаСервер(ТабЗначДоходыИРасходы, перИндексПриёмщика, 5);
	 ТабЗначРегистраторыДоходовИРасходов = ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод(датаНачало, датаКонец); 
	 ОтправитьТаблицуНаСервер(ТабЗначРегистраторыДоходовИРасходов, перИндексПриёмщика, 6);
	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьСтрокиТаблицРегистраторовЗаПериод(датаНачало, датаКонец); 
	 ОтправитьТаблицуНаСервер(ТабЗначСтрокиТаблицРегистраторовЗаПериод, перИндексПриёмщика, 7);
```

---

### Изменение кода

**Изменение в Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	 
	 ТабЗначЦентрыФинансовойОтветственности = ЗапроситьЦентрыФинансовойОтветственности();	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначЦентрыФинансовойОтветственности, перИндексПриёмщика, 3);
	 
	 датаНачало = НачалоДня(ТекущаяДата() - 86400);
	 датаКонец = КонецДня(ТекущаяДата() - 86400);
	 
	 ТабЗначРеализацииАвтомобилей = ЗапроситьРеализацииЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРеализацииАвтомобилей, перИндексПриёмщика, 4);

	 
КонецФункции // СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину()
```

**Код После:**

```bsl
	 
	 
	 ТабЗначРегистраторыДоходовИРасходов = ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРегистраторыДоходовИРасходов, перИндексПриёмщика, 6);

	 
	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьСтрокиТаблицРегистраторовЗаПериод(датаНачало, датаКонец); 
	 
	 ОтправитьТаблицуНаСервер(ТабЗначСтрокиТаблицРегистраторовЗаПериод, перИндексПриёмщика, 7);
	 
	 
КонецФункции // СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину()
```

**Данный участок кода:** удалён блок с реализациями и ЦФО (между Отправить 2 и следующим блоком); добавлены вызовы по доходам/расходам и регистраторам (типы 5, 6, 7). Итог — та же логика, что в предыдущем «Данный участок кода» для СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину.

---

### Изменение кода

**Изменение в Функция ОтправитьТаблицуНаСервер:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция ОтправитьТаблицуНаСервер(аргТабЗначДанные, аргПерИндексПриёмщика, аргПерИндексТипаТаблицы) Экспорт
	
	 стрСобранныеJson = СериализоватьВJson(аргТабЗначДанные);
	 
	 СоотвПараметрыСообщения = Новый Соответствие;
	 
	 СоотвПараметрыСообщения.Вставить("CommandIndex", аргПерИндексПриёмщика);
	 СоотвПараметрыСообщения.Вставить("DataTypeIndex", аргПерИндексТипаТаблицы);
	 
	 ОтправитьСообщениеНаСервер(стрСобранныеJson, СоотвПараметрыСообщения);
	 
КонецФункции // ОтправитьТаблицуНаСервер()
```

**Код После:**

```bsl
Функция ОтправитьТаблицуНаСервер(аргТабЗначДанные, аргПерИндексПриёмщика, аргПерИндексТипаТаблицы) Экспорт
	
	 стрСобранныеJson = СериализоватьВJson(аргТабЗначДанные);
	 
	 СоотвПараметрыСообщения = Новый Соответствие;
	 
	 СоотвПараметрыСообщения.Вставить("CommandIndex", аргПерИндексПриёмщика);
	 СоотвПараметрыСообщения.Вставить("DataTypeIndex", аргПерИндексТипаТаблицы);
	 
	 ОтправитьСообщениеНаСервер(стрСобранныеJson, СоотвПараметрыСообщения);
	 
КонецФункции // ОтправитьТаблицуНаСервер()
```

**Данный участок кода:** изменений в теле функции нет (только контекст в отчёте). Фактическая правка — в других местах модуля.

---

### Изменение кода

**Изменение в Функция ОтправитьТабличныйДокументНаСервер:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция ОтправитьТабличныйДокументНаСервер(аргТабДок, аргПерИндексПриёмщика, аргПерИндексТипаТабличногоДокумента) Экспорт

	 СоотвПараметрыСообщения = Новый Соответствие;
	 
	 СоотвПараметрыСообщения.Вставить("CommandIndex", аргПерИндексПриёмщика);
	 СоотвПараметрыСообщения.Вставить("DataTypeIndex", аргПерИндексТипаТабличногоДокумента);
	 
	 ОтправитьСообщениеНаСервер(аргТабДок, СоотвПараметрыСообщения);
	
КонецФункции // ОтправитьТабличныйДокументНаСервер()
```

**Код После:**

```bsl
Функция ОтправитьТабличныйДокументНаСервер(аргТабДок, аргПерИндексПриёмщика, аргПерИндексТипаТабличногоДокумента) Экспорт

	 СоотвПараметрыСообщения = Новый Соответствие;
	 
	 СоотвПараметрыСообщения.Вставить("CommandIndex", аргПерИндексПриёмщика);
	 СоотвПараметрыСообщения.Вставить("DataTypeIndex", аргПерИндексТипаТабличногоДокумента);
	 
	 ОтправитьСообщениеНаСервер(аргТабДок, СоотвПараметрыСообщения);
	
КонецФункции // ОтправитьТабличныйДокументНаСервер()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают. Изменения — в других участках модуля Шина1С.

---

### Изменение кода

**Изменение в Функция ОтправитьСообщениеНаСервер:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
	
	СервисыИнтеграции.ШинаЗакамьеОсновнойПередатчикНаСервер.
	ORANGE_DataExchangerFtom1SToSQLServer_Основной_DataExchangerFrom1SToSQLServer_АвтоАльфа1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеНаСервер()
```

**Код После:**

```bsl
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
	
	СервисыИнтеграции.ШинаЗакамьеОсновнойПередатчикНаСервер.
	ORANGE_DataExchangerFtom1SToSQLServer_Основной_DataExchangerFrom1SToSQLServer_АвтоАльфа1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеНаСервер()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают. Правки — в других местах модуля.

---

### Изменение кода

**Изменение в Функция ЗапроситьПодразделенияДляОсновнойБазы:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|	ПодразделенияКомпании.Наименование КАК name,
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(ПодразделенияКомпании.Организация.Ссылка) КАК organizationsGuid
		//|	ПодразделенияКомпании.Организация.Код КАК organization_one_c_key,
		//|	ПодразделенияКомпании.Организация.Наименование КАК organization_name
		|ИЗ
		|	Справочник.ПодразделенияКомпании КАК ПодразделенияКомпании
		|ГДЕ
		|	НЕ ПодразделенияКомпании.ПометкаУдаления";
	
	Возврат ЗапросПодразделений.Выполнить().Выгрузить(); 
	
	
КонецФункции // ЗапроситьПодразделения()
```

**Код После:**

```bsl
		|	ПодразделенияКомпании.Наименование КАК name,
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(ПодразделенияКомпании.Организация.Ссылка) КАК organizationsGuid
		//|	ПодразделенияКомпании.Организация.Код КАК organization_one_c_key,
		//|	ПодразделенияКомпании.Организация.Наименование КАК organization_name
		|ИЗ
		|	Справочник.ПодразделенияКомпании КАК ПодразделенияКомпании
		|ГДЕ
		|	НЕ ПодразделенияКомпании.ПометкаУдаления";
	
	Возврат ЗапросПодразделений.Выполнить().Выгрузить(); 
	
	
КонецФункции // ЗапроситьПодразделения()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают. Изменение — контекст/другое место в файле.

---

### Изменение кода

**Изменение в Функция ЗапроситьОрганизацииДляОсновнойБазы:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|	Организации.ОсвобожденОтНДС КАК isValueAddedTaxezFree,
		|	Организации.СистемаНалогообложения КАК taxezSystem,
		|	Организации.ОбъектНалогообложения КАК objectOfTaxez,
		|	Организации.ПометкаУдаления КАК isDeleted
		|ИЗ
		|	Справочник.Организации КАК Организации
		|ГДЕ
		|	НЕ Организации.ПометкаУдаления";
	
	Возврат ЗапросОрганизаций.Выполнить().Выгрузить();
		

КонецФункции // ЗапроситьОрганизацииДляОсновнойБазы()
```

**Код После:**

```bsl
		|	Организации.ОсвобожденОтНДС КАК isValueAddedTaxezFree,
		|	Организации.СистемаНалогообложения КАК taxezSystem,
		|	Организации.ОбъектНалогообложения КАК objectOfTaxez,
		|	Организации.ПометкаУдаления КАК isDeleted
		|ИЗ
		|	Справочник.Организации КАК Организации
		|ГДЕ
		|	НЕ Организации.ПометкаУдаления";
	
	Возврат ЗапросОрганизаций.Выполнить().Выгрузить();
		

КонецФункции // ЗапроситьОрганизацииДляОсновнойБазы()
```

**Данный участок кода:** в приведённых фрагментах изменений нет. Правки — в других участках модуля.

---

### Изменение кода

**Изменение в Функция ЗапроситьЦентрыФинансовойОтветственности:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(ЦФО.Ссылка) КАК guid,
		|	ЦФО.Код КАК code1C,
		|	ЦФО.Наименование КАК name,
		|	ЦФО.ПометкаУдаления КАК isDeleted
		|ИЗ
		|	Справочник.ЦФО КАК ЦФО
		|ГДЕ
		|	НЕ ЦФО.ПометкаУдаления";
	
	Возврат ЗапросЦентровФинансовойОтветственности.Выполнить().Выгрузить();
	

КонецФункции // Запросить ЦентрыФинансовойОтветственности()
```

**Код После:**

```bsl
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(ЦФО.Ссылка) КАК guid,
		|	ЦФО.Код КАК code1C,
		|	ЦФО.Наименование КАК name,
		|	ЦФО.ПометкаУдаления КАК isDeleted
		|ИЗ
		|	Справочник.ЦФО КАК ЦФО
		|ГДЕ
		|	НЕ ЦФО.ПометкаУдаления";
	
	Возврат ЗапросЦентровФинансовойОтветственности.Выполнить().Выгрузить();
	

КонецФункции // Запросить ЦентрыФинансовойОтветственности()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают.

---

### Изменение кода

**Изменение в Функция ЗапроситьРеализацииЗаПериод:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|ГДЕ
		|	РеализацияАвтомобилейАвтомобили.Ссылка.Дата МЕЖДУ &датаНачало И &датаКонец
		|	И НЕ РеализацияАвтомобилейАвтомобили.Ссылка.ПометкаУдаления
		|	И РеализацияАвтомобилейАвтомобили.Ссылка.Проведен";
	
	ЗапросРеализацииЗаПериод.УстановитьПараметр("датаНачало", аргДатаНачало);
	ЗапросРеализацииЗаПериод.УстановитьПараметр("датаКонец", аргДатаКонец);

	
	
	Возврат ЗапросРеализацииЗаПериод.Выполнить().Выгрузить();
	

КонецФункции // ЗапроситьРеализацииЗаПериод()
```

**Код После:**

```bsl
		|	РеализацияАвтомобилейАвтомобили.Ссылка.Дата МЕЖДУ &датаНачало И &датаКонец
		|	И НЕ РеализацияАвтомобилейАвтомобили.Ссылка.ПометкаУдаления
		|	И РеализацияАвтомобилейАвтомобили.Ссылка.Проведен";
	
	ЗапросРеализацииЗаПериод.УстановитьПараметр("датаНачало", аргДатаНачало);
	ЗапросРеализацииЗаПериод.УстановитьПараметр("датаКонец", аргДатаКонец);

	
	
	Возврат ЗапросРеализацииЗаПериод.Выполнить().Выгрузить();
	

КонецФункции // ЗапроситьРеализацииЗаПериод()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают (различие только в форматировании строки ГДЕ).

---

### Изменение кода

**Изменение в Функция ЗапроситьСтрокиТаблицРегистраторовЗаПериод:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|	РеализацияАвтомобилейАвтомобили.Ссылка.Дата МЕЖДУ &датаНачало И &датаКонец
		|	И НЕ РеализацияАвтомобилейАвтомобили.Ссылка.ПометкаУдаления
		|	И РеализацияАвтомобилейАвтомобили.Ссылка.Проведен";
	
	ЗапросРеализацииЗаПериод.УстановитьПараметр("датаНачало", аргДатаНачало);
	ЗапросРеализацииЗаПериод.УстановитьПараметр("датаКонец", аргДатаКонец);

	
	
	Возврат ЗапросРеализацииЗаПериод.Выполнить().Выгрузить();
	

КонецФункции // ЗапроситьРеализацииЗаПериод()
```

**Код После:**

```bsl
	
	
	ЗапросСтрокТаблицРегистраторовЗаПериод.УстановитьПараметр("датаНачало", аргДатаНачало);
	ЗапросСтрокТаблицРегистраторовЗаПериод.УстановитьПараметр("датаКонец", аргДатаКонец);
	
	
	
	
	Возврат ЗапросСтрокТаблицРегистраторовЗаПериод.Выполнить().Выгрузить();
	

КонецФункции // ЗапроситьСтрокиТаблицРегистраторов()
```

**Данный участок кода:** в «Код До» — фрагмент запроса реализаций; в «Код После» — вызов УстановитьПараметр и Выполнить для ЗапросСтрокТаблицРегистраторовЗаПериод (другая функция). Изменённый фрагмент — подстановка параметров и возврат выгрузки строк регистраторов.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	ТабДокРезультат.Записать(ссПотокФайла);
	
	ссЧтениеПотока = Новый ЧтениеДанных(ссПотокФайла);	
	ссРезультатЧтения = ссЧтениеПотока.Прочитать();	
	ТабДокДвоичный = ссПотокФайла.ЗакрытьИПолучитьДвоичныеДанные();
	
	ТабДокСтроковый = Base64Строка(ТабДокДвоичный);
	
	Возврат ТабДокСтроковый;
	
КонецФункции // СобратьТабличныйДокумент()
```

**Код После:**

```bsl
	
	ссЧтениеПотока = Новый ЧтениеДанных(ссПотокФайла);	
	ссРезультатЧтения = ссЧтениеПотока.Прочитать();	
	ТабДокДвоичный = ссПотокФайла.ЗакрытьИПолучитьДвоичныеДанные();
	
	ТабДокСтроковый = Base64Строка(ТабДокДвоичный);
	
	Возврат ТабДокСтроковый;
	
КонецФункции // СобратьТабличныйДокумент()
```

**Данный участок кода:** удалена строка `ТабДокРезультат.Записать(ссПотокФайла);` — запись в поток перед чтением убрана.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	ссЧтениеПотока = Новый ЧтениеДанных(ссПотокФайла);	
	ссРезультатЧтения = ссЧтениеПотока.Прочитать();	
	ТабДокДвоичный = ссПотокФайла.ЗакрытьИПолучитьДвоичныеДанные();
	
	ТабДокСтроковый = Base64Строка(ТабДокДвоичный);
	
	Возврат ТабДокСтроковый;
	
КонецФункции // СобратьТабличныйДокумент()
```

**Код После:**

```bsl
	ТабДокДвоичный = ссПотокФайла.ЗакрытьИПолучитьДвоичныеДанные();
	
	ТабДокСтроковый = Base64Строка(ТабДокДвоичный);
	
	Возврат ТабДокСтроковый;
	
КонецФункции // СобратьТабличныйДокумент()
```

**Данный участок кода:** удалены строки с ЧтениеДанных и Прочитать — остаётся только ЗакрытьИПолучитьДвоичныеДанные, Base64 и Возврат.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция СериализоватьВJson(аргОбъект)

	ПисательJson = Новый ЗаписьJSON;
	
	ПисательJson.УстановитьСтроку();
	
	СериализаторXDTO.ЗаписатьJSON(ПисательJson, аргОбъект, НазначениеТипаXML.Явное);
	
	Возврат ПисательJson.Закрыть();
	

КонецФункции // СериализоватьВJson()
```

**Код После:**

```bsl
	ТабДокСтроковый = Base64Строка(ТабДокДвоичный);
	
	Возврат ТабДокСтроковый;
	
КонецФункции // СобратьТабличныйДокумент()
```

**Данный участок кода:** в «Код До» — функция СериализоватьВJson; в «Код После» — фрагмент СобратьТабличныйДокумент (смещение контекста в отчёте). Фактическое изменение в коде — правки в разных функциях модуля.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция СериализоватьВJson(аргОбъект)

	ПисательJson = Новый ЗаписьJSON;
	
	ПисательJson.УстановитьСтроку();
	
	СериализаторXDTO.ЗаписатьJSON(ПисательJson, аргОбъект, НазначениеТипаXML.Явное);
	
	Возврат ПисательJson.Закрыть();
	

КонецФункции // СериализоватьВJson()
```

**Код После:**

```bsl

```

**Данный участок кода:** в приведённых фрагментах «Код После» пуст — в diff это удалённый или перенесённый фрагмент (функция СериализоватьВJson без изменений в этом месте).

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция СериализоватьВJson(аргОбъект)

	ПисательJson = Новый ЗаписьJSON;
	
	ПисательJson.УстановитьСтроку();
	
	СериализаторXDTO.ЗаписатьJSON(ПисательJson, аргОбъект, НазначениеТипаXML.Явное);
	
	Возврат ПисательJson.Закрыть();
	

КонецФункции // СериализоватьВJson()
```

**Код После:**

```bsl

```

**Данный участок кода:** контекст отчёта (в «Код До» — СериализоватьВJson, в «Код После» — пусто). Изменение в другом участке файла.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция ОтправитьСообщениеВPostgres(аргСтрСообщение)
	
 	обБуферСтроки = ПолучитьБуферДвоичныхДанныхИзСтроки(аргСтрСообщение);	
	
	созданноеСообщение = СервисыИнтеграции.ШинаЗакамьеPostgresBI.СоздатьСообщение();
	
	созданноеСообщение.Параметры.Вставить("CommandIndex", 1);
	
	обПотокСообщения = созданноеСообщение.ПолучитьТелоКакПоток();
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — начало функции ОтправитьСообщениеВPostgres (создание сообщения, запись в поток); в «Код После» — пусто (контекст из другого фрагмента diff).

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция ОтправитьСообщениеВPostgres(аргСтрСообщение)
	
 	обБуферСтроки = ПолучитьБуферДвоичныхДанныхИзСтроки(аргСтрСообщение);	
	
	созданноеСообщение = СервисыИнтеграции.ШинаЗакамьеPostgresBI.СоздатьСообщение();
	
	созданноеСообщение.Параметры.Вставить("CommandIndex", 1);
	
	обПотокСообщения = созданноеСообщение.ПолучитьТелоКакПоток();
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
	
	СервисыИнтеграции.ШинаЗакамьеPostgresBI.ILN_ORANGE_SenderDataFrom1SToBI_Основной_DataSenderFrom1SToBI_Канал1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — тело ОтправитьСообщениеВPostgres до ВыполнитьОбработку; в «Код После» — пусто. Изменение в другом участке.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	
	созданноеСообщение = СервисыИнтеграции.ШинаЗакамьеPostgresBI.СоздатьСообщение();
	
	созданноеСообщение.Параметры.Вставить("CommandIndex", 1);
	
	обПотокСообщения = созданноеСообщение.ПолучитьТелоКакПоток();
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
	
	СервисыИнтеграции.ШинаЗакамьеPostgresBI.ILN_ORANGE_SenderDataFrom1SToBI_Основной_DataSenderFrom1SToBI_Канал1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеВPostgres()
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — конец функции ОтправитьСообщениеВPostgres (ВыполнитьОбработку, КонецФункции); в «Код После» — пусто. Контекст из другого фрагмента diff.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	
	созданноеСообщение.Параметры.Вставить("CommandIndex", 1);
	
	обПотокСообщения = созданноеСообщение.ПолучитьТелоКакПоток();
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
	
	СервисыИнтеграции.ШинаЗакамьеPostgresBI.ILN_ORANGE_SenderDataFrom1SToBI_Основной_DataSenderFrom1SToBI_Канал1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеВPostgres()
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — середина функции ОтправитьСообщениеВPostgres; в «Код После» — пусто. Изменение в другом участке.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	обПотокСообщения = созданноеСообщение.ПолучитьТелоКакПоток();
	обПотокСообщения.Записать(обБуферСтроки, 0, обБуферСтроки.Размер);
	обПотокСообщения.СброситьБуферы();
	
	обПотокСообщения.Закрыть();
	
	СервисыИнтеграции.ШинаЗакамьеPostgresBI.ILN_ORANGE_SenderDataFrom1SToBI_Основной_DataSenderFrom1SToBI_Канал1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеВPostgres()
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — запись в поток и отправка в Postgres; в «Код После» — пусто.

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	
	СервисыИнтеграции.ШинаЗакамьеPostgresBI.ILN_ORANGE_SenderDataFrom1SToBI_Основной_DataSenderFrom1SToBI_Канал1СИсточник.
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеВPostgres()
```

**Код После:**

```bsl

```

---

### Изменение кода

**Изменение в Функция СобратьТабличныйДокумент:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	ОтправитьСообщение(созданноеСообщение);
	
	СервисыИнтеграции.ВыполнитьОбработку();
	

КонецФункции // ОтправитьСообщениеВPostgres()
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — отправка сообщения и ВыполнитьОбработку; в «Код После» — пусто.

---

### Изменение кода

**Изменение в Функция ОбработатьПолученныеДанныеИзШины:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		КонецЦикла;
		
	КонецЕсли;
	
	СтрукРезультат = Новый Структура;
	СтрукРезультат.Вставить("перВсегоУспешно", перВсегоУспешно);	
    СтрукРезультат.Вставить("перВсегоПроитерировано", перВсегоПроитерировано);
	СтрукРезультат.Вставить("масОшибок", масОшибок);
	
	
	Возврат СтрукРезультат;
	
КонецФункции // ОбработатьПолученныеДанныеИзШины()
```

**Код После:**

```bsl
		КонецЦикла;
		
	КонецЕсли;
	
	СтрукРезультат = Новый Структура;
	СтрукРезультат.Вставить("перВсегоУспешно", перВсегоУспешно);	
    СтрукРезультат.Вставить("перВсегоПроитерировано", перВсегоПроитерировано);
	СтрукРезультат.Вставить("масОшибок", масОшибок);
	
	
	Возврат СтрукРезультат;
	
КонецФункции // ОбработатьПолученныеДанныеИзШины()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают.

---

### Изменение кода

**Изменение в Функция ОбработатьПолученныйРабочийЛист:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		аргОбДокРабочииЛист.УстановитьСсылкуНового(стрНоваяСсылка);
		
		аргОбДокРабочииЛист.РуководительМенеджера = аргСсСотрудник;
		аргОбДокРабочииЛист.Менеджер = аргСсСотрудник;
		аргОбДокРабочииЛист.Контрагент = аргСсКонтрагент;
		
		аргОбДокРабочииЛист.Комментарий = "Создан автоматически при обмене С 1С Шиной"; 
		
	КонецЕсли; 
	
	аргОбДокРабочииЛист.Записать();

КонецФункции // ОбработатьПолученныйРабочийЛист()
```

**Код После:**

```bsl
		аргОбДокРабочииЛист.УстановитьСсылкуНового(стрНоваяСсылка);
		
		аргОбДокРабочииЛист.РуководительМенеджера = аргСсСотрудник;
		аргОбДокРабочииЛист.Менеджер = аргСсСотрудник;
		аргОбДокРабочииЛист.Контрагент = аргСсКонтрагент;
		
		аргОбДокРабочииЛист.Комментарий = "Создан автоматически при обмене С 1С Шиной"; 
		
	КонецЕсли; 
	
	аргОбДокРабочииЛист.Записать();

КонецФункции // ОбработатьПолученныйРабочийЛист()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают.

---

### Изменение кода

**Изменение в Функция НаитиИлиСоздатьСотрудника:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		//обСотрудник.Комментарий = "Создан автоматически при обмене С 1С Шиной";
		
		//обСотрудник.Записать();
		
		//Возврат обСотрудник.Ссылка;	
		
		
	КонецЕсли;
	
	
	Возврат Справочники.Сотрудники.ПустаяСсылка();

КонецФункции // НаитиИлиСоздатьСотрудника()
```

**Код После:**

```bsl
		//обСотрудник.Комментарий = "Создан автоматически при обмене С 1С Шиной";
		
		//обСотрудник.Записать();
		
		//Возврат обСотрудник.Ссылка;	
		
		
	КонецЕсли;
	
	
	Возврат Справочники.Сотрудники.ПустаяСсылка();

КонецФункции // НаитиИлиСоздатьСотрудника()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают.

---

### Изменение кода

**Изменение в Функция НайтиИлиСоздатьКонтрагента:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		
		обКонтрагент.Комментарий = "Создан автоматически при обмене С 1С Шиной";
		
		обКонтрагент.Записать();
		
		Возврат обКонтрагент.Ссылка;
		
	КонецЕсли;
	

	Возврат Справочники.Контрагенты.ПустаяСсылка();

КонецФункции // НайтиИлиСоздатьКонтрагента()
```

**Код После:**

```bsl
		
		обКонтрагент.Комментарий = "Создан автоматически при обмене С 1С Шиной";
		
		обКонтрагент.Записать();
		
		Возврат обКонтрагент.Ссылка;
		
	КонецЕсли;
	

	Возврат Справочники.Контрагенты.ПустаяСсылка();

КонецФункции // НайтиИлиСоздатьКонтрагента()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают.

---

### Изменение кода

**Изменение в Функция ПроверитьНаличиеПолей:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		Если Не ЗначениеЗаполнено(аргСоотвОбъект[элМас]) Тогда
			
			Возврат Ложь;
			
		КонецЕсли;
			
	КонецЦикла;
	
	Возврат Истина;

КонецФункции // ПроверитьНаличиеПолей()
```

**Код После:**

```bsl
			
			Возврат Ложь;
			
		КонецЕсли;
			
	КонецЦикла;
	
	Возврат Истина;

КонецФункции // ПроверитьНаличиеПолей()
```

**Данный участок кода:** в «Код После» удалена строка `Если Не ЗначениеЗаполнено(аргСоотвОбъект[элМас]) Тогда` и отступ перед `Возврат Ложь` (изменение условия в цикле).

---

## CommonModules/опОповещения/Ext/Module.bsl

### Изменение кода

**Изменение в Функция ПроверитьНеделю:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция ПроверитьНеделю(ЭтаЗапись, Дата)
	УстановленаНеделя = Число(Сред(ЭтаЗапись.ТипПериода,9,1)+" ");			
	Если УстановленаНеделя = 3 Тогда				
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 0;
	ИначеЕсли УстановленаНеделя = 4 Тогда
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 1;
	ИначеЕсли УстановленаНеделя = 5 Тогда   //Номера  недель месяца (10,14)
		НомерНедели = Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800)+1;
		Возврат ((НомерНедели<5) и (Сред(ЭтаЗапись.ТипПериода,9+НомерНедели,1)="1"))
			или  (Сред(ЭтаЗапись.ТипПериода,14,1)="1" и (КонецМесяца(Дата)<=КонецНедели(Дата)));
	ИначеЕсли УстановленаНеделя = 7 Тогда  //кол-во недель		
		КолвоНедель = Число(Сред(ЭтаЗапись.ТипПериода,10,3)+" ");
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
```

**Код После:**

```bsl
Функция ПроверитьНеделю(ЭтаЗапись, Дата)
	УстановленаНеделя = Число(Сред(ЭтаЗапись.ТипПериода,9,1)+" ");			
	Если УстановленаНеделя = 3 Тогда				
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 0;
	ИначеЕсли УстановленаНеделя = 4 Тогда
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 1;
	ИначеЕсли УстановленаНеделя = 5 Тогда   //Номера  недель месяца (10,14)
		НомерНедели = Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800)+1;
		Возврат ((НомерНедели<5) и (Сред(ЭтаЗапись.ТипПериода,9+НомерНедели,1)="1"))
			или  (Сред(ЭтаЗапись.ТипПериода,14,1)="1" и (КонецМесяца(Дата)<=КонецНедели(Дата)));
	ИначеЕсли УстановленаНеделя = 7 Тогда  //кол-во недель		
		КолвоНедель = Число(Сред(ЭтаЗапись.ТипПериода,10,3)+" ");
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
```

**Данный участок кода:** в «Код До» отсутствовала первая строка функции; в «Код После» добавлены объявление УстановленаНеделя и начало блока Если. Изменённый фрагмент — оформление начала функции ПроверитьНеделю.

---

### Изменение кода

**Изменение в Функция ПроверитьНеделю:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	Если УстановленаНеделя = 3 Тогда				
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 0;
	ИначеЕсли УстановленаНеделя = 4 Тогда
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 1;
	ИначеЕсли УстановленаНеделя = 5 Тогда   //Номера  недель месяца (10,14)
		НомерНедели = Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800)+1;
		Возврат ((НомерНедели<5) и (Сред(ЭтаЗапись.ТипПериода,9+НомерНедели,1)="1"))
			или  (Сред(ЭтаЗапись.ТипПериода,14,1)="1" и (КонецМесяца(Дата)<=КонецНедели(Дата)));
	ИначеЕсли УстановленаНеделя = 7 Тогда  //кол-во недель		
		КолвоНедель = Число(Сред(ЭтаЗапись.ТипПериода,10,3)+" ");
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
		Иначе        
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
КонецФункции // ПроверитьНеделю()
```

**Код После:**

```bsl
Функция ПроверитьНеделю(ЭтаЗапись, Дата)
	УстановленаНеделя = Число(Сред(ЭтаЗапись.ТипПериода,9,1)+" ");			
	Если УстановленаНеделя = 3 Тогда				
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 0;
	ИначеЕсли УстановленаНеделя = 4 Тогда
		Возврат Цел((Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800) + 1)%2) = 1;
	ИначеЕсли УстановленаНеделя = 5 Тогда   //Номера  недель месяца (10,14)
		НомерНедели = Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800)+1;
		Возврат ((НомерНедели<5) и (Сред(ЭтаЗапись.ТипПериода,9+НомерНедели,1)="1"))
			или  (Сред(ЭтаЗапись.ТипПериода,14,1)="1" и (КонецМесяца(Дата)<=КонецНедели(Дата)));
	ИначеЕсли УстановленаНеделя = 7 Тогда  //кол-во недель		
		КолвоНедель = Число(Сред(ЭтаЗапись.ТипПериода,10,3)+" ");
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
		Иначе        
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
```

---

### Изменение кода

**Изменение в Функция ПроверитьНеделю:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
		Иначе        
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
КонецФункции // ПроверитьНеделю()
```

**Код После:**

```bsl
		НомерНедели = Цел((Дата-НачалоНедели(НачалоМесяца(Дата)))/604800)+1;
		Возврат ((НомерНедели<5) и (Сред(ЭтаЗапись.ТипПериода,9+НомерНедели,1)="1"))
			или  (Сред(ЭтаЗапись.ТипПериода,14,1)="1" и (КонецМесяца(Дата)<=КонецНедели(Дата)));
	ИначеЕсли УстановленаНеделя = 7 Тогда  //кол-во недель		
		КолвоНедель = Число(Сред(ЭтаЗапись.ТипПериода,10,3)+" ");
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
		Иначе        
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
КонецФункции // ПроверитьНеделю()
```

**Данный участок кода:** в «Код До» — конец блока 5 (Возврат) и начало блока 7; в «Код После» — та же логика с добавлением ИначеЕсли УстановленаНеделя = 7. Изменение — порядок/оформление веток.

---

### Изменение кода

**Изменение в Функция ПроверитьНеделю:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		Иначе        
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
КонецФункции // ПроверитьНеделю()
```

**Код После:**

```bsl
			или  (Сред(ЭтаЗапись.ТипПериода,14,1)="1" и (КонецМесяца(Дата)<=КонецНедели(Дата)));
	ИначеЕсли УстановленаНеделя = 7 Тогда  //кол-во недель		
		КолвоНедель = Число(Сред(ЭтаЗапись.ТипПериода,10,3)+" ");
		Если КолвоНедель<>0 Тогда			
			Возврат (НачалоНедели(Дата) - НачалоНедели(ЭтаЗапись.ДатаНачала))%(604800*КолвоНедель) = 0;
		Иначе        
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
КонецФункции // ПроверитьНеделю()
```

**Данный участок кода:** в «Код До» — Иначе для УстановленаНеделя = 7 и финал функции; в «Код После» — пусто. Контекст из другого фрагмента.

---

### Изменение кода

**Изменение в Функция ПроверитьНеделю:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	 		Возврат	Истина;
		КонецЕсли;		
	Иначе
		Возврат	Истина;
	КонецЕсли; 			
КонецФункции // ПроверитьНеделю()
```

**Код После:**

```bsl

```

**Данный участок кода:** в «Код До» — конец функции ПроверитьНеделю; в «Код После» — пусто.

---

## DataProcessors/ВыгрузкаФайловВBI/Ext/ObjectModule.bsl

### №13834

**Изменение в Процедура ВыгрузитьПодразделенияВФайл:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	ОблШапка = Макет.ПолучитьОбласть("ОбластьШапка");
	ТабДокумент.Вывести(ОблШапка);
	
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПодразделенияКомпании.Код КАК Код,
		|	ПодразделенияКомпании.Родитель.Наименование КАК Город,
		|	ПодразделенияКомпании.Наименование КАК Наименование,
		|	ПодразделенияКомпании.НаправлениеДеятельности КАК Направление
		|ИЗ
		|	Справочник.ПодразделенияКомпании КАК ПодразделенияКомпании
		|ГДЕ
		|	НЕ ПодразделенияКомпании.Предопределенный
		|	И ПодразделенияКомпании.Родитель.Родитель <> ЗНАЧЕНИЕ(Справочник.ПодразделенияКомпании.Архив)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Город";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	ОблСтрока = Макет.ПолучитьОбласть("ОбластьСтрока");

	Пока Выборка.Следующий() Цикл
		ОблСтрока.Параметры.Заполнить(Выборка);
```

**Код После:**

```bsl
	ОблШапка = Макет.ПолучитьОбласть("ОбластьШапка");
	ТабДокумент.Вывести(ОблШапка);
	
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПодразделенияКомпании.Код КАК Код,
		|	ПодразделенияКомпании.Родитель.Наименование КАК Город,
		|	ПодразделенияКомпании.Наименование КАК Наименование,
		|	ПодразделенияКомпании.НаправлениеДеятельности КАК Направление
		|ИЗ
		|	Справочник.ПодразделенияКомпании КАК ПодразделенияКомпании
		|ГДЕ
		|	НЕ ПодразделенияКомпании.Предопределенный    
		// Хамитова + 16.03.26 №13834 Добавляем в выгрузку архивные данные
		//|	И ПодразделенияКомпании.Родитель.Родитель <> ЗНАЧЕНИЕ(Справочник.ПодразделенияКомпании.Архив)
		// Хамитова -
		|
		|УПОРЯДОЧИТЬ ПО
		|	Город";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	ОблСтрока = Макет.ПолучитьОбласть("ОбластьСтрока");

```

**Данный участок кода (№13834):** в запрос ГДЕ условие исключения архивных подразделений закомментировано — в выгрузку попадают и архивные. Комментарии Хамитовой 16.03.26.

---

### №13834

**Изменение в Процедура ВыгрузитьПодразделенияВФайл:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|	Справочник.ПодразделенияКомпании КАК ПодразделенияКомпании
		|ГДЕ
		|	НЕ ПодразделенияКомпании.Предопределенный
		|	И ПодразделенияКомпании.Родитель.Родитель <> ЗНАЧЕНИЕ(Справочник.ПодразделенияКомпании.Архив)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Город";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	ОблСтрока = Макет.ПолучитьОбласть("ОбластьСтрока");

	Пока Выборка.Следующий() Цикл
		ОблСтрока.Параметры.Заполнить(Выборка);
		ОблСтрока.Параметры.Регион 				 = ?(Выборка.Город = "Казань", "Казань", ?(Выборка.Город = "Уфа", "Уфа", "Закамье"));
		ОблСтрока.Параметры.ОбщепринятоеНазвание = Лев(СокрЛП(Выборка.Наименование), СтрНайти(Выборка.Наименование, "(") - 1);
		ОблСтрока.Параметры.ТипПродаж 			 = ?(СтрНайти(Выборка.Наименование, "АСП") = 0 И СтрНайти(Выборка.Наименование, "ОП") > 0, "Новые", 
												 ?(СтрНайти(Выборка.Наименование, "ОП АСП") > 0, "АСП", "")); 
		ТабДокумент.Вывести(ОблСтрока);
	КонецЦикла;
		
	пПутьКФайлу    = РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение("ВыгрузкаФайловВBI", "Подразделения") + "\Подразделения.xlsx";
	
	Если Не ЗначениеЗаполнено(пПутьКФайлу) Тогда 
		Возврат;
	КонецЕсли;
```

**Код После:**

```bsl
		|	НЕ ПодразделенияКомпании.Предопределенный    
		// Хамитова + 16.03.26 №13834 Добавляем в выгрузку архивные данные
		//|	И ПодразделенияКомпании.Родитель.Родитель <> ЗНАЧЕНИЕ(Справочник.ПодразделенияКомпании.Архив)
		// Хамитова -
		|
		|УПОРЯДОЧИТЬ ПО
		|	Город";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	ОблСтрока = Макет.ПолучитьОбласть("ОбластьСтрока");

	Пока Выборка.Следующий() Цикл
		ОблСтрока.Параметры.Заполнить(Выборка);
		ОблСтрока.Параметры.Регион 				 = ?(Выборка.Город = "Казань", "Казань", ?(Выборка.Город = "Уфа", "Уфа", "Закамье")); 
		// Хамитова + 16.03.26 №13834 Полное наименование
        ОблСтрока.Параметры.ОбщепринятоеНазвание = Выборка.Наименование;
		// Хамитова -
		ОблСтрока.Параметры.ТипПродаж 			 = ?(СтрНайти(Выборка.Наименование, "АСП") = 0 И СтрНайти(Выборка.Наименование, "ОП") > 0, "Новые", 
												 ?(СтрНайти(Выборка.Наименование, "ОП АСП") > 0, "АСП", "")); 
		ТабДокумент.Вывести(ОблСтрока);
	КонецЦикла;
		
	пПутьКФайлу    = РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение("ВыгрузкаФайловВBI", "Подразделения") + "\Подразделения.xlsx";
	
	Если Не ЗначениеЗаполнено(пПутьКФайлу) Тогда 
```

**Данный участок кода (№13834):** ОбщепринятоеНазвание берётся как полное наименование (`Выборка.Наименование`) вместо среза до первой скобки; в запрос ГДЕ добавлены комментарии по архивным данным (как в предыдущем блоке).

```bsl
        // Хамитова + 16.03.26 №13834 Полное наименование
        ОблСтрока.Параметры.ОбщепринятоеНазвание = Выборка.Наименование;
		// Хамитова -
```

---

## DataProcessors/СтартСистемы/Ext/ObjectModule.bsl

### Изменение кода

**Изменение в Функция ВыполнитьПроверкуЗаполненияКонстант:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		ИначеЕсли Константа = Константы.смсИмяПользователяПоУмолчанию Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсКонецПериодаЗапрета Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсНачалоПериодаЗапрета Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсНеОтправлятьSMS Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсПарольПользователя Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсПолучатьТолькоПолныеСообщения Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсПроксиПароль Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсПроксиПользователь Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсПроксиПорт Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсПроксиСервер Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсСрокЖизниSMS Тогда Продолжить; 
		ИначеЕсли Константа = Константы.смсРоботРегламентныхЗаданий Тогда Продолжить; 
		ИначеЕсли Константа = Константы.ДнейДоОкончанияДействияКлюча Тогда Продолжить; 
		//(хрк	
		ИначеЕсли Константа = Константы.КаталогВыгрузкиОстатковАвтомобилей Тогда Продолжить;
		//хрк)
		КонецЕсли; 
		
		ЗначениеКонстанты = Константа.Получить();
		Если обЗначениеНеЗаполнено(ЗначениеКонстанты) Тогда
			//++ AZ исключим из проверки константы стандартных подсистем
			ЭтоОбъектСтандартныхПодсистем = Ложь;
		    Для Каждого Подсистема Из Метаданные.Подсистемы.СтандартныеПодсистемы.Подсистемы Цикл
				Если Подсистема.Состав.Содержит(Метаданные.Константы[СтрЗаменить(Строка(Константа),"КонстантаМенеджер.","")]) Тогда
					ЭтоОбъектСтандартныхПодсистем = Истина;
					Продолжить;
```

**Код После:** (без смсИмяПользователяПоУмолчанию, добавлены три константы для рассылки — см. ниже)

**Данный участок кода:** удалена строка `ИначеЕсли Константа = Константы.смсИмяПользователяПоУмолчанию Тогда Продолжить;`. Добавлены три исключения проверки: ТаймаутФормыСообщениеПользователюЛичное, ТаймаутФормыСообщениеПользователюОбщее, ИнтервалПроверкиНовыхСообщенийПользователю.

```bsl
		// +Зиянгиров Артур (16.03.2026) Добавлю свои константы для рассылки.	
		ИначеЕсли Константа = Константы.ТаймаутФормыСообщениеПользователюЛичное Тогда Продолжить;
		ИначеЕсли Константа = Константы.ТаймаутФормыСообщениеПользователюОбщее Тогда Продолжить;
		ИначеЕсли Константа = Константы.ИнтервалПроверкиНовыхСообщенийПользователю Тогда Продолжить;	
		// -Зиянгиров Артур (16.03.2026) Добавлю свои константы для рассылки.	
```

---

## Documents/ЗаказНаряд/Ext/ManagerModule.bsl

### Изменение кода

**Изменение в Функция ЗапроситьДополнительныйЗаказНаряд:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	
	РезультатЗапроса = ЗапросДополнительныхЗаказНарядов.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		
		Возврат ВыборкаДетальныеЗаписи.Ссылка;
		
	КонецЕсли;
	
	
	Возврат Неопределено;

КонецФункции // ЗапроситьДополнительныйЗаказНаряд()
```

**Код После:**

```bsl
	РезультатЗапроса = ЗапросДополнительныхЗаказНарядов.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		
		Возврат ВыборкаДетальныеЗаписи.Ссылка;
		
	КонецЕсли;
	
	
	Возврат Неопределено;

КонецФункции // ЗапроситьДополнительныйЗаказНаряд()
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают (убрана пустая строка в начале).

---

### Изменение кода

**Изменение в Функция ЗапроситьГарантийныйЗаказНаряд:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	РезультатЗапроса = ЗапросДополнительныхЗаказНарядов.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		
		Возврат ВыборкаДетальныеЗаписи.Ссылка;
		
	КонецЕсли;
	
	
	Возврат Неопределено;

КонецФункции // ЗапроситьДополнительныйЗаказНаряд()
```

**Код После:**

```bsl
Функция ЗапроситьГарантийныйЗаказНаряд(ссЗаказНаряд) Экспорт
	
	ЗапросГарантийныхЗаказНарядов = Новый Запрос;
	ЗапросГарантийныхЗаказНарядов.Текст = 
	"ВЫБРАТЬ
	|	ЗаказНаряд.Ссылка КАК Ссылка
	|ИЗ
	|	Документ.ЗаказНаряд КАК ЗаказНаряд
	|ГДЕ
	|	НЕ ЗаказНаряд.ПометкаУдаления
	|	И ЗаказНаряд.Организация = &Организация
	|	И ЗаказНаряд.ПодразделениеКомпании = &ПодразделениеКомпании
	|	И ЗаказНаряд.ВидРемонта В(&масВидыРемонта)
```

**Данный участок кода:** добавлена новая функция ЗапроситьГарантийныйЗаказНаряд с запросом по гарантийным заказ-нарядам (текст запроса с отбором по Организация, Подразделение, ВидРемонта и т.д.).

---

### Изменение кода

**Изменение в Функция ЗапроситьГарантийныйЗаказНаряд:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		
		Возврат ВыборкаДетальныеЗаписи.Ссылка;
		
	КонецЕсли;
	
	
	Возврат Неопределено;

КонецФункции // ЗапроситьДополнительныйЗаказНаряд()
```

**Код После:**

```bsl
	|ИЗ
	|	Документ.ЗаказНаряд КАК ЗаказНаряд
	|ГДЕ
	|	НЕ ЗаказНаряд.ПометкаУдаления
	|	И ЗаказНаряд.Организация = &Организация
	|	И ЗаказНаряд.ПодразделениеКомпании = &ПодразделениеКомпании
	|	И ЗаказНаряд.ВидРемонта В(&масВидыРемонта)
	|	И ЗаказНаряд.Цех = &Цех
	|	И ЗаказНаряд.Мастер = &Мастер
	|	И ЗаказНаряд.Диспетчер = &Диспетчер
	|	И ЗаказНаряд.Заказчик = &Заказчик
	|	И ЗаказНаряд.Автомобиль = &Автомобиль
	|	И НЕ ЗаказНаряд.Ссылка = &ОсновнойЗаказНаряд";
	
	
	//ЦБ000048 - Сервисные компании 
	масВидыРемонта = Новый Массив;
	масВидыРемонта.Добавить(Справочники.ВидыРемонта.НайтиПоКоду("ЦБ000039"));
	
	
	ЗапросГарантийныхЗаказНарядов.УстановитьПараметр("масВидыРемонта", масВидыРемонта);
	ЗапросГарантийныхЗаказНарядов.УстановитьПараметр("Диспетчер", ссЗаказНаряд.Диспетчер);
	ЗапросГарантийныхЗаказНарядов.УстановитьПараметр("Мастер", ссЗаказНаряд.Мастер);
	ЗапросГарантийныхЗаказНарядов.УстановитьПараметр("Организация", ссЗаказНаряд.Организация);
	ЗапросГарантийныхЗаказНарядов.УстановитьПараметр("ПодразделениеКомпании", ссЗаказНаряд.ПодразделениеКомпании);
	ЗапросГарантийныхЗаказНарядов.УстановитьПараметр("Цех", ссЗаказНаряд.Цех);
```

**Данный участок кода:** в запрос добавлены условия по Цех, Мастер, Диспетчер, Заказчик, Автомобиль, ОсновнойЗаказНаряд; инициализация масВидыРемонта (ЦБ000039) и установка параметров запроса.

---

## Documents/ЗаказНаряд/Ext/ObjectModule.bsl

### Изменение кода

**Изменение в Процедура ПриЗаписи:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		КонецЦикла; 
		
		Если НЕ СервиснаяКампанияАктуальна Тогда
			дкДобавитьОшибку(Ошибки,СервиснаяКампанияАктуальнаСообщение);
			Отказ=Истина;
		Иначе
			Запрос=Новый Запрос;
			Запрос.Текст="ВЫБРАТЬ
			|	ВыполнениеСервисныхКампаний.VIN,
			|	ВыполнениеСервисныхКампаний.СервиснаяКампания
			|ИЗ
			|	РегистрСведений.ВыполнениеСервисныхКампаний КАК ВыполнениеСервисныхКампаний
			|ГДЕ
			|	ВыполнениеСервисныхКампаний.ДокументВыполнения = &ДокументВыполнения";
			Запрос.УстановитьПараметр("ДокументВыполнения",Ссылка);
			Выборка=Запрос.Выполнить().Выбрать();
			Пока Выборка.Следующий() Цикл
				ВыполнениеСервисныхКампанийНаборЗаписей=РегистрыСведений.ВыполнениеСервисныхКампаний.СоздатьНаборЗаписей();
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.VIN.Значение=Выборка.VIN;
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.VIN.Использование=Истина;
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.СервиснаяКампания.Значение=Выборка.СервиснаяКампания;
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.СервиснаяКампания.Использование=Истина;
				ВыполнениеСервисныхКампанийНаборЗаписей.Прочитать();
				ВыполнениеСервисныхКампанийНаборЗаписей.НеИзменятьАвтора=Истина;
				Для Каждого СтрокаНабора Из ВыполнениеСервисныхКампанийНаборЗаписей Цикл
					СтрокаНабора.ДатаВыполнения='00010101';
```

**Код После:**

```bsl
		
		Если НЕ СервиснаяКампанияАктуальна Тогда
			дкДобавитьОшибку(Ошибки,СервиснаяКампанияАктуальнаСообщение);
			Отказ=Истина;
		Иначе
			Запрос=Новый Запрос;
			Запрос.Текст="ВЫБРАТЬ
			|	ВыполнениеСервисныхКампаний.VIN,
			|	ВыполнениеСервисныхКампаний.СервиснаяКампания
			|ИЗ
			|	РегистрСведений.ВыполнениеСервисныхКампаний КАК ВыполнениеСервисныхКампаний
			|ГДЕ
			|	ВыполнениеСервисныхКампаний.ДокументВыполнения = &ДокументВыполнения";
			Запрос.УстановитьПараметр("ДокументВыполнения",Ссылка);
			Выборка=Запрос.Выполнить().Выбрать();
			Пока Выборка.Следующий() Цикл
				ВыполнениеСервисныхКампанийНаборЗаписей=РегистрыСведений.ВыполнениеСервисныхКампаний.СоздатьНаборЗаписей();
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.VIN.Значение=Выборка.VIN;
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.VIN.Использование=Истина;
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.СервиснаяКампания.Значение=Выборка.СервиснаяКампания;
				ВыполнениеСервисныхКампанийНаборЗаписей.Отбор.СервиснаяКампания.Использование=Истина;
				ВыполнениеСервисныхКампанийНаборЗаписей.Прочитать();
				ВыполнениеСервисныхКампанийНаборЗаписей.НеИзменятьАвтора=Истина;
				Для Каждого СтрокаНабора Из ВыполнениеСервисныхКампанийНаборЗаписей Цикл
					СтрокаНабора.ДатаВыполнения='00010101';
					СтрокаНабора.ДокументВыполнения=Документы.ЗаказНаряд.ПустаяСсылка();
```

**Данный участок кода:** в цикле по строкам набора записей ВыполнениеСервисныхКампаний добавлено обнуление реквизита ДокументВыполнения (ПустаяСсылка) при сбросе даты выполнения.

```bsl
					СтрокаНабора.ДокументВыполнения=Документы.ЗаказНаряд.ПустаяСсылка();
```

---

### Изменение кода

**Изменение в Функция ЭтоДОДляРеализации:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
		|	ЗаказНаряд.Ссылка = &Ссылка
		|	И ЗаказНаряд.Дата < РеализацияАвтомобилейАвтомобили.Ссылка.Дата";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	РезультатЗапроса = Запрос.Выполнить();
		
	Если РезультатЗапроса.Пустой() Тогда 
		Возврат Ложь;
	Иначе 
		Возврат Истина;
	КонецЕсли;	
		
КонецФункции 
```

**Код После:**

```bsl
		|	ЗаказНаряд.Ссылка = &Ссылка
		|	И ЗаказНаряд.Дата < РеализацияАвтомобилейАвтомобили.Ссылка.Дата";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	РезультатЗапроса = Запрос.Выполнить();
		
	Если РезультатЗапроса.Пустой() Тогда 
		Возврат Ложь;
	Иначе 
		Возврат Истина;
	КонецЕсли;	
		
КонецФункции 
```

**Данный участок кода:** в приведённых фрагментах «Код До» и «Код После» совпадают.

---

## Documents/РабочийЛист/Ext/ObjectModule.bsl

### №16 (16.03.2026)

**Изменение в Функция ПолучитьОбязательныеРеквизиты:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлена подстановка/обработка `НикТГ` в данных и дальнейших действиях.

**Код До:**

```bsl
Функция ПолучитьОбязательныеРеквизиты() Экспорт
	                     
	// Обязательные реквизиты документа
	ОбязательныеРеквизиты=Новый Структура();
	ОбязательныеРеквизиты.Вставить("Автор",1);
	ОбязательныеРеквизиты.Вставить("ПодразделениеКомпании",1);
	ОбязательныеРеквизиты.Вставить("Организация",1);
	ОбязательныеРеквизиты.Вставить("Менеджер",1);
	ОбязательныеРеквизиты.Вставить("РуководительМенеджера",1);
	ОбязательныеРеквизиты.Вставить("Контрагент",1);
	ОбязательныеРеквизиты.Вставить("Пол",1);
	ОбязательныеРеквизиты.Вставить("ПредставлениеТелефона",1);
	ОбязательныеРеквизиты.Вставить("ВидКонтакта",1);
	ОбязательныеРеквизиты.Вставить("Реклама",1);
	ОбязательныеРеквизиты.Вставить("ПредполагаемыйСрок",1);
	ОбязательныеРеквизиты.Вставить("ВидОплаты",1);
	ОбязательныеРеквизиты.Вставить("Статус",1);
	
	ОбязательныеОпции=Новый Структура();
	ОбязательныеОпции.Вставить("Опция",      3);
	ОбязательныеОпции.Вставить("Количество", 1);
```

**Код После:**

```bsl
Функция ПолучитьОбязательныеРеквизиты() Экспорт
	                     
	// Обязательные реквизиты документа
	ОбязательныеРеквизиты=Новый Структура();
	ОбязательныеРеквизиты.Вставить("Автор",1);
	ОбязательныеРеквизиты.Вставить("ПодразделениеКомпании",1);
	ОбязательныеРеквизиты.Вставить("Организация",1);
	ОбязательныеРеквизиты.Вставить("Менеджер",1);
	ОбязательныеРеквизиты.Вставить("РуководительМенеджера",1);
	ОбязательныеРеквизиты.Вставить("Контрагент",1);
	ОбязательныеРеквизиты.Вставить("Пол",1);
	//+Бардашов Н.А. № 16.03.2026 №12823
	Если Не ЗначениеЗаполнено(ЭтотОбъект.ПредставлениеТелефона) 
		И Не ЗначениеЗаполнено(ЭтотОбъект.НикТГ) Тогда
	
		ОбязательныеРеквизиты.Вставить("ПредставлениеТелефона",1);
		ОбязательныеРеквизиты.Вставить("НикТГ",1);		
	
	КонецЕсли;
    //ОбязательныеРеквизиты.Вставить("ПредставлениеТелефона",1);
	//-Бардашов Н.А. № 16.03.2026 №12823
```

**Данный участок кода:** обязательность «ПредставлениеТелефона» и «НикТГ» задаётся только если оба поля пусты (если заполнено хотя бы одно — оба не обязательны). Закомментирована безусловная вставка «ПредставлениеТелефона».

```bsl
	//+Бардашов Н.А. № 16.03.2026 №12823
	Если Не ЗначениеЗаполнено(ЭтотОбъект.ПредставлениеТелефона) 
		И Не ЗначениеЗаполнено(ЭтотОбъект.НикТГ) Тогда
		ОбязательныеРеквизиты.Вставить("ПредставлениеТелефона",1);
		ОбязательныеРеквизиты.Вставить("НикТГ",1);		
	КонецЕсли;
```

---

## Ext/OrdinaryApplicationModule.bsl

### Изменение кода

**Изменение в процедуре/функции:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
////////////////////////////////////////////////////////////////////////////////
// ООО "1С-Рарус Альфа-Авто". Департамент разработки. 2011-2016 год.
//------------------------------------------------------------------------------
// Типовое решение "Альфа-Авто: Автосалон+Автосервис+Автозапчасти ПРОФ, редакция 5.1"
//------------------------------------------------------------------------------
// Модуль приложения

//------------------------------------------------------------------------------
// Размерность числовых типов конфигурации:
// Реквизиты типа Сумма       - 15.2
// Реквизиты типа Количество  - 15.3
```

**Код После:**

```bsl
﻿////////////////////////////////////////////////////////////////////////////////
// ООО "1С-Рарус Альфа-Авто". Департамент разработки. 2011-2016 год.
//------------------------------------------------------------------------------
// Типовое решение "Альфа-Авто: Автосалон+Автосервис+Автозапчасти ПРОФ, редакция 5.1"
//------------------------------------------------------------------------------
// Модуль приложения

//------------------------------------------------------------------------------
// Размерность числовых типов конфигурации:
// Реквизиты типа Сумма       - 15.2
// Реквизиты типа Количество  - 15.3
```

**Данный участок кода:** в начало модуля добавлен BOM (символ ﻿). Остальное совпадает.

---

### Изменение кода

**Изменение в Процедура глПроверитьПодключениеНапоминания:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Процедура глПроверитьПодключениеНапоминания() Экспорт
	Если Константы.ИнтервалПроверкиНапоминаний.Получить() > 0 Тогда
		ПодключитьОбработчикОжидания("опОбработкаОповещений", 60 * Константы.ИнтервалПроверкиНапоминаний.Получить());
	Иначе 
		ОтключитьОбработчикОжидания("опОбработкаОповещений");
	КонецЕсли;
	// + Зиянгиров Артур (16.03.2026) Интервал проверки новых сообщений — из константы; при 0 обработчик отключается.
	Если Константы.ИнтервалПроверкиНовыхСообщенийПользователю.Получить() > 0 Тогда
		ПодключитьОбработчикОжидания("ПроверкаНовыхСообщенийПользователю", 60 * Константы.ИнтервалПроверкиНовыхСообщенийПользователю.Получить());
	Иначе
		ОтключитьОбработчикОжидания("ПроверкаНовыхСообщенийПользователю");
	КонецЕсли;
КонецПроцедуры // ПроверитьПодключениеНапоминания()
```

**Код После:**

```bsl
Процедура глПроверитьПодключениеНапоминания() Экспорт
	Если Константы.ИнтервалПроверкиНапоминаний.Получить() > 0 Тогда
		ПодключитьОбработчикОжидания("опОбработкаОповещений", 60 * Константы.ИнтервалПроверкиНапоминаний.Получить());
	Иначе 
		ОтключитьОбработчикОжидания("опОбработкаОповещений");
	КонецЕсли;
		// + Зиянгиров Артур (16.03.2026) Интервал проверки новых сообщений — из константы; при 0 обработчик отключается.
	Если Константы.ИнтервалПроверкиНовыхСообщенийПользователю.Получить() > 0 Тогда
		ПодключитьОбработчикОжидания("ПроверкаНовыхСообщенийПользователю", 60 * Константы.ИнтервалПроверкиНовыхСообщенийПользователю.Получить());
	Иначе
		ОтключитьОбработчикОжидания("ПроверкаНовыхСообщенийПользователю");
	КонецЕсли;
```

**Данный участок кода:** изменён отступ перед комментарием Зиянгирова (сдвиг вправо). Логика та же: подключение/отключение обработчика «ПроверкаНовыхСообщенийПользователю» по константе ИнтервалПроверкиНовыхСообщенийПользователю.

---

## HTTPServices/HTTPCHBirtix/Ext/Module.bsl

### Изменение кода

**Изменение в Функция Avto_ArestПроверкаАреста:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция Avto_ArestПроверкаАреста(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);    
	
	СтрокаVIN = Запрос.ПараметрыURL.Получить("vin");      
	
	СамАвто=Справочники.Автомобили.НайтиПоРеквизиту("VIN",СтрокаVIN);     
	
	Если СамАвто=Справочники.Автомобили.ПустаяСсылка() Тогда
		Ответ = Новый HTTPСервисОтвет(406,"Автомобиля не найден оп VIN");    
		Возврат Ответ; 
	КонецЕсли; 
	
	Запрос=Новый Запрос;
	
```

**Код После:**

```bsl
Функция Avto_ArestПроверкаАреста(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);    
	
	СтрокаVIN = Запрос.ПараметрыURL.Получить("VIN");      
	
	СамАвто=Справочники.Автомобили.НайтиПоРеквизиту("VIN",СокрЛП(СтрокаVIN));     
	
	Если СамАвто=Справочники.Автомобили.ПустаяСсылка() Тогда
		Ответ = Новый HTTPСервисОтвет(406,"Автомобиля не найден оп VIN");    
		Возврат Ответ; 
	КонецЕсли; 
	
	Запрос=Новый Запрос;
	
```

**Данный участок кода:** параметр URL исправлен на "VIN" (вместо "vin"); при поиске автомобиля к значению применяется СокрЛП(СтрокаVIN).

```bsl
	СтрокаVIN = Запрос.ПараметрыURL.Получить("VIN");      
	СамАвто=Справочники.Автомобили.НайтиПоРеквизиту("VIN",СокрЛП(СтрокаVIN));
```

---

### Изменение кода

**Изменение в Функция Avto_ArestПроверкаАреста:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Функция Avto_ArestПроверкаАреста(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);    
	
	СтрокаVIN = Запрос.ПараметрыURL.Получить("vin");      
	
	СамАвто=Справочники.Автомобили.НайтиПоРеквизиту("VIN",СтрокаVIN);     
	
	Если СамАвто=Справочники.Автомобили.ПустаяСсылка() Тогда
		Ответ = Новый HTTPСервисОтвет(406,"Автомобиля не найден оп VIN");    
		Возврат Ответ; 
	КонецЕсли; 
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	             |	ОстаткиАвтомобилейОстатки.СкладКомпании.Код КАК СкладКомпанииКод,
```

**Код После:**

```bsl
Функция Avto_ArestПроверкаАреста(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);    
	
	СтрокаVIN = Запрос.ПараметрыURL.Получить("VIN");      
	
	СамАвто=Справочники.Автомобили.НайтиПоРеквизиту("VIN",СокрЛП(СтрокаVIN));     
	
	Если СамАвто=Справочники.Автомобили.ПустаяСсылка() Тогда
		Ответ = Новый HTTPСервисОтвет(406,"Автомобиля не найден оп VIN");    
		Возврат Ответ; 
	КонецЕсли; 
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	             |	ОстаткиАвтомобилейОстатки.СкладКомпании.Код КАК СкладКомпанииКод,
```

**Данный участок кода:** то же изменение — параметр "VIN" и СокрЛП(СтрокаVIN) при поиске (см. предыдущий блок).

---

## Reports/ВозможностиИРезультатыПодразделения/Ext/ObjectModule.bsl

### Изменение кода

**Изменение в Процедура ВыгрузитьОтчетВДашборд:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
Процедура ВыгрузитьОтчетВДашборд() Экспорт
	НастройкаИмя		= "ВыгрузкаФайловВBI";
	ПараметрИмя			= "ПутьОтчетВозможностиИРезультатыПодразделения";
	НастройкаЗначение	= РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение(НастройкаИмя, ПараметрИмя);
	
	Если НЕ ЗначениеЗаполнено(НастройкаЗначение)
		И ТипЗнч(НастройкаЗначение) <> Тип("Строка") Тогда
		Возврат;
	КонецЕсли;
	
	ПутьКФайлу	= НастройкаЗначение + "\ВозможностиИРезультатыПодразделения.xls";
	ТабДокумент	= Новый ТабличныйДокумент;
	Отчет		= Отчеты.ВозможностиИРезультатыПодразделения.Создать();
	Отчет.СкомпоноватьРезультат(ТабДокумент);
	
	ТабДокумент.УдалитьОбласть(ТабДокумент.Область("C3"), ТипСмещенияТабличногоДокумента.ПоГоризонтали);
	ТабДокумент.Записать(ПутьКФайлу, ТипФайлаТабличногоДокумента.XLS);

	Odin_Общий.ЗагрузитьФайлыДляBIНаFTP(ПутьКФайлу);
КонецПроцедуры
```

**Код После:**

```bsl
Процедура ВыгрузитьОтчетВДашборд() Экспорт
	НастройкаИмя		= "ВыгрузкаФайловВBI";
	ПараметрИмя			= "ПутьОтчетВозможностиИРезультатыПодразделения";
	НастройкаЗначение	= РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение(НастройкаИмя, ПараметрИмя);
	
	Если НЕ ЗначениеЗаполнено(НастройкаЗначение)
		И ТипЗнч(НастройкаЗначение) <> Тип("Строка") Тогда
		Возврат;
	КонецЕсли;
	
	ПутьКФайлу	= НастройкаЗначение + "\ВозможностиИРезультатыПодразделения.xlsx";
	ТабДокумент	= Новый ТабличныйДокумент;
	Отчет		= Отчеты.ВозможностиИРезультатыПодразделения.Создать();
	Отчет.СкомпоноватьРезультат(ТабДокумент);
	
	ТабДокумент.УдалитьОбласть(ТабДокумент.Область("C3"), ТипСмещенияТабличногоДокумента.ПоГоризонтали);
	ТабДокумент.Записать(ПутьКФайлу, ТипФайлаТабличногоДокумента.XLSX);

	Odin_Общий.ЗагрузитьФайлыДляBIНаFTP(ПутьКФайлу);
КонецПроцедуры
```

**Данный участок кода:** формат выгрузки изменён с .xls на .xlsx: ПутьКФайлу и ТабДокумент.Записать используют ТипФайлаТабличногоДокумента.XLSX вместо XLS.

```bsl
	ПутьКФайлу	= НастройкаЗначение + "\ВозможностиИРезультатыПодразделения.xlsx";
	ТабДокумент.Записать(ПутьКФайлу, ТипФайлаТабличногоДокумента.XLSX);
```

---

### Изменение кода

**Изменение в Процедура ВыгрузитьОтчетВДашборд:**

**Суть правки:** добавление/изменение логики по результатам diff.

**Что меняется/эффект:** Добавлены/изменены условия и поля логики обработки (см. фрагмент кода).

**Код До:**

```bsl
	НастройкаИмя		= "ВыгрузкаФайловВBI";
	ПараметрИмя			= "ПутьОтчетВозможностиИРезультатыПодразделения";
	НастройкаЗначение	= РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение(НастройкаИмя, ПараметрИмя);
	
	Если НЕ ЗначениеЗаполнено(НастройкаЗначение)
		И ТипЗнч(НастройкаЗначение) <> Тип("Строка") Тогда
		Возврат;
	КонецЕсли;
	
	ПутьКФайлу	= НастройкаЗначение + "\ВозможностиИРезультатыПодразделения.xls";
	ТабДокумент	= Новый ТабличныйДокумент;
	Отчет		= Отчеты.ВозможностиИРезультатыПодразделения.Создать();
	Отчет.СкомпоноватьРезультат(ТабДокумент);
	
	ТабДокумент.УдалитьОбласть(ТабДокумент.Область("C3"), ТипСмещенияТабличногоДокумента.ПоГоризонтали);
	ТабДокумент.Записать(ПутьКФайлу, ТипФайлаТабличногоДокумента.XLS);

	Odin_Общий.ЗагрузитьФайлыДляBIНаFTP(ПутьКФайлу);
КонецПроцедуры
```

**Код После:**

```bsl
	НастройкаИмя		= "ВыгрузкаФайловВBI";
	ПараметрИмя			= "ПутьОтчетВозможностиИРезультатыПодразделения";
	НастройкаЗначение	= РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение(НастройкаИмя, ПараметрИмя);
	
	Если НЕ ЗначениеЗаполнено(НастройкаЗначение)
		И ТипЗнч(НастройкаЗначение) <> Тип("Строка") Тогда
		Возврат;
	КонецЕсли;
	
	ПутьКФайлу	= НастройкаЗначение + "\ВозможностиИРезультатыПодразделения.xlsx";
	ТабДокумент	= Новый ТабличныйДокумент;
	Отчет		= Отчеты.ВозможностиИРезультатыПодразделения.Создать();
	Отчет.СкомпоноватьРезультат(ТабДокумент);
	
	ТабДокумент.УдалитьОбласть(ТабДокумент.Область("C3"), ТипСмещенияТабличногоДокумента.ПоГоризонтали);
	ТабДокумент.Записать(ПутьКФайлу, ТипФайлаТабличногоДокумента.XLSX);

	Odin_Общий.ЗагрузитьФайлыДляBIНаFTP(ПутьКФайлу);
КонецПроцедуры
```

**Данный участок кода:** то же изменение — выгрузка в .xlsx вместо .xls (см. предыдущий блок).

---
