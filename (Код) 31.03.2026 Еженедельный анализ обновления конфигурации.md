# (Код) 31.03.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `0c71db6a` `(Обновление 25.03.2026)`  
**Дата анализа:** 31.03.2026

---

## Формат разбора

По каждому изменению показано:
- участок кода;
- **Код до**;
- **Код после**;
- **Что добавлено по коду**.

---

## 1) `CommonModules/Odin_Общий/Ext/Module.bsl`

### Участок: график работы мастера (`time_to`)

**Код до**
```bsl
СтруктураДанных_ГрафикРаботы.Вставить("time_to", "20:00");
```

**Код после**
```bsl
СтруктураДанных_ГрафикРаботы.Вставить("time_to", "22:00");// ... btrx#14002
```

**Что добавлено по коду**
- Изменено время окончания рабочего дня: `20:00` -> `22:00`.

### Участок: URL проверки графика

**Код до**
```bsl
АдресПроверки = СтрШаблон("%1?year=%2&month=%3&dealerId=%4&day=%5&userId=%6", ...);
```

**Код после**
```bsl
// было %1?year=%2&month=%3&dealerId=%4&day=%5&userId=%6
АдресПроверки = СтрШаблон("%1?year=%2&month=%3&day=%4&userId=%5", ...);
//СтруктураДанныхДляПодключения.БрендID, //dealerId
```

**Что добавлено по коду**
- Удален параметр `dealerId` из URL.
- Добавлен комментарий с прежним шаблоном URL.

---

## 2) `DataProcessors/ALLMaintenanceSchedule_ВыгрузкаДанных/Ext/ObjectModule.bsl`

### Участок: постановка в очередь для Lada

**Код до**
```bsl
Запись.ВОчереди = ?(Запись.ТекстОшибки = "" И Запись.IDКлиентаDNM <> "" И Запись.IDДокументаDNM <> "" , Ложь, Истина);
НаборПротокола.Записать();
```

**Код после**
```bsl
Запись.ВОчереди = ?(Запись.ТекстОшибки = "" И Запись.IDКлиентаDNM <> "" И Запись.IDДокументаDNM <> "" , Ложь, Истина);
Если ТипБренда = Справочники.ТипБренда.Lada Тогда
	Запись.ВОчереди = Ложь;
КонецЕсли;
НаборПротокола.Записать();
```

**Что добавлено по коду**
- Добавлен блок исключения повторной постановки в очередь для `Lada`.

---

## 3) `DataProcessors/Б24_ВыгрузкаКонтрагентов/Ext/ObjectModule.bsl`

### Участок: выбор конкретного договора

**Код до**
```bsl
Функция ПолучитьСписокДоговоровКонтрагентов(Контрагент, Таблица = Неопределено) Экспорт
...
|ГДЕ
|	Контрагенты.Ссылка = &ВыбранныйКонтрагент";
```

**Код после**
```bsl
Функция ПолучитьСписокДоговоровКонтрагентов(Контрагент, Таблица = Неопределено, ВыбранныйДоговор=0) Экспорт
...
|ГДЕ
|	Контрагенты.Ссылка = &ВыбранныйКонтрагент
|	И (ДоговорыВзаиморасчетов.Ссылка = &ВыбранныйДоговор
|			ИЛИ &ВыбранныйДоговор = 0)";
...
Запрос.УстановитьПараметр("ВыбранныйДоговор", ВыбранныйДоговор);
```

**Что добавлено по коду**
- Новый параметр функции и запроса: `ВыбранныйДоговор`.
- Новый фильтр в `ГДЕ` для точечного отбора договора.

---

## 4) `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`

### Участок: поиск заявки и формирование запроса

**Код до**
```bsl
ВсеКромеПС=Лев(РабочийКод,СтрДлина(РабочийКод-1));
СтрокаТабТекст= СтрокаТабТекст+"&total=1000";
ЗаявкаId=Заявка.Получить("id");
+ "&fields[UF_CRM_49_CATEGORY][0]=1483"
+ "&fields[UF_CRM_49_CATEGORY][1]=2567"
```

**Код после**
```bsl
ВсеКромеПС=Лев(РабочийКод,СтрДлина(РабочийКод)-1);
//СтрокаТабТекст= СтрокаТабТекст+"&total=1000";
ЗаявкаId=Формат(Заявка.Получить("id"),"ЧГ=0");
...
Для каждого СамаКватегория из СписокКатегорий цикл
	ТекстКатегорий=ТекстКатегорий+"&fields[UF_CRM_49_CATEGORY]["+Формат(СчетКатегорий,"ЧГ-0")+"]="+СокрЛП(СамаКватегория.Значение);
	СчетКатегорий=СчетКатегорий+1;
КонецЦикла;
```

**Что добавлено по коду**
- Исправлена формула длины строки.
- Отключен жесткий лимит `total=1000`.
- Добавлено форматирование `id` через `Формат(...,"ЧГ=0")`.
- Статические категории заменены на динамический список категорий.

---

## 5) `DataProcessors/Обмен_с_ОЗОН/Forms/ФормаОбмена/Ext/Form/Module.bsl`

### Участок: массовая выгрузка по всем площадкам

**Код до**
```bsl
&НаКлиенте
Процедура ЗапросЗаказов(Команда)
	ЗапросЗаказовНаСервере();
КонецПроцедуры
```

**Код после**
```bsl
&НаКлиенте
Процедура ПоВсемПлощадкам(Команда)
	ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда)
	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ ... ИЗ Справочник.ТорговыеПлощадки ...";
	ТР=Запрос.Выполнить().Выгрузить();
	Для каждого СТР из ТР цикл
		Сценарий=СТР.Ссылка;
		ЗаполнитьОбразцыВыгрузки();
		ВыгрузитьВыбранноеНаСервере();
	КонецЦикла;
КонецПроцедуры
```

**Что добавлено по коду**
- Добавлены процедуры `ПоВсемПлощадкам` и `ПоВсемПлощадкамНаСервере`.
- Добавлен цикл массовой выгрузки по активным площадкам.

---

## 6) `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/RecordSetModule.bsl`

### Участок: структура модуля

**Код до**
```bsl
////////////////////////////////////////////////////////////////////////////////
// odin+ (11.03.26) ARM-120: ...
////////////////////////////////////////////////////////////////////////////////
#Область ОписаниеПеременных
...
#Область ОбработчикиСобытий
...
```

**Код после**
```bsl
////////////////////////////////////////////////////////////////////////////////
// odin+ (11.03.26) ARM-120: ...
////////////////////////////////////////////////////////////////////////////////
```

**Что добавлено по коду**
- Нового кода не добавлено; удалены служебные секции шаблона модуля.

---

## 7) Измененные XML/метаданные (9 файлов)

Изменены:
- `Catalogs/ТорговыеПлощадки.xml`
- `ChartsOfCharacteristicTypes/ПраваИНастройки/Ext/Predefined.xml`
- `CommonTemplates/ПечатьДиаграммы/Ext/Template.xml`
- `ConfigDumpInfo.xml`
- `Configuration.xml`
- `DataProcessors/Обмен_с_ОЗОН/Forms/ФормаОбмена/Ext/Form.xml`
- `Documents/ВводВЭксплуатацию.xml`
- `Documents/ДокументЦУУ.xml`
- `InformationRegisters/СообщенияПользователей.xml`

**Что добавлено по коду**
- Обновлен состав метаданных и реквизитов объектов после зеркалирования выгрузки.
- Для полного построчного `до/после` по этим XML использовать реестр diff-файлов:  
  `diff_modified_31.03.2026.txt` + `diff_name_status_31.03.2026.txt`.
