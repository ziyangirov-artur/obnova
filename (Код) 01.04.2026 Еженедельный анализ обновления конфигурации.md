# (Код) 01.04.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `772d70d6` `(Обновление 01.04.2026)`  
**Дата анализа:** 01.04.2026

---

## 1) `CommonModules/Odin_Склады/Ext/Module.bsl`

### Что изменено
- Доработана логика параметров наценки и флагов:
  - `МенятьЦенуТолькоВМоментСменыШага`
  - `ВестиИсториюРасчетаЦенАвтомобилей`
- Добавлены дополнительные защитные проверки перед перерасчетом (ранний выход, если запуск не из целевого процесса).
- Для комиссионных автомобилей вместо подстановки/замены текста общего запроса введен отдельный текст запроса.

### Код до (смысловой фрагмент)
```bsl
Запрос.Текст = СтрЗаменить(Запрос.Текст, "РегистрСведений.НаценкиНаUsedCars", "РегистрСведений.НаценкиНаUsedCarsНаКомиссии");
...
Если НЕ СтруктураДанных.МенятьЦенуТолькоВМоментСменыШага Тогда
    ...
КонецЕсли;
```

### Код после (смысловой фрагмент)
```bsl
// отдельный запрос для комиссионного автомобиля
Запрос.Текст =
    "ВЫБРАТЬ
    |   НаценкиНаUsedCarsНаКомиссии....
    |ИЗ
    |   РегистрСведений.НаценкиНаUsedCarsНаКомиссии ...
    |ГДЕ ...";

Если НЕ СтруктураДанных.Свойство("ПерваяПартия") Тогда
    Возврат;
КонецЕсли;

Если СтруктураДанных.МенятьЦенуТолькоВМоментСменыШага <> Истина Тогда
    Возврат Неопределено;
КонецЕсли;
```

### Что добавлено по коду
- Явное разделение логики для комиссии.
- Жестче контроль условий, когда разрешено изменять цену.

---

## 2) `DataProcessors/СобственныйОбработчикККТ/Ext/ObjectModule.bsl`

### Что изменено
- Удален крупный блок автозаполнения агентских/поставщицких данных из договора взаиморасчетов.
- Удалены вспомогательные функции обхода цепочки оснований документа и преобразования признака агента.
- Взамен оставлена ссылка на официальный раздел для программистов в `1c_kkt_54fz`.

### Код до (смысловой фрагмент)
```bsl
ДокументОснование = ...
ДоговорВзаиморасчетов = ПолучитьДоговорВзаиморасчетовДляАгента(...);
...
ИмяДоговора = "AUTO_AGENT_CONTRACT_" + КлючДоговора;
ИмяПоставщика = "AUTO_AGENT_SUPPLIER_" + КлючДоговора;
...
```

### Код после (смысловой фрагмент)
```bsl
// ОПИСАНИЕ:
// https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#послеформированиитаблицычека
```

### Что добавлено по коду
- Упрощен модуль: оставлена только рекомендуемая точка расширения по официальной документации.
- Снижена поддержка кастомной логики, риск расхождений с базовой обработкой.

---

## 3) `_tmp_mirror_new_to_myconfig1.ps1`

### Что изменено
- Поиск корня выгрузки изменен с проверки только непосредственных подпапок на рекурсивный поиск `ConfigDumpInfo.xml`.
- При нескольких совпадениях выбирается наиболее близкий к корню путь (самый короткий).

### Код до
```powershell
$dirs = Get-ChildItem -LiteralPath $Root -Directory
foreach ($d in $dirs) {
  $candidate = Join-Path $d.FullName 'ConfigDumpInfo.xml'
  if (Test-Path -LiteralPath $candidate) { ... }
}
```

### Код после
```powershell
$candidates = Get-ChildItem -LiteralPath $Root -Recurse -File -Filter 'ConfigDumpInfo.xml'
$selected = $candidates | Sort-Object { $_.FullName.Length } | Select-Object -First 1
```

### Что добавлено по коду
- Поддержка случаев вложенности вида `New\0104\0104`.

---

## 4) XML-изменения

- `ConfigDumpInfo.xml` (`+2/-2`) — обновлены служебные данные выгрузки.
- `CommonTemplates/ПечатьДиаграммы/Ext/Template.xml` (`+1/-1`) — точечная правка шаблона.
