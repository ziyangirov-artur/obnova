# (Код) 01.04.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `f3a3f8dc` `(Обновление 01.04.2026)`  
**Дата анализа:** 01.04.2026

---

## 1) `CommonModules/Odin_Общий/Ext/Module.bsl`

- Изменения компактные (`+12/-9`), точечные правки условий и комментариев в блоках интеграции.
- Актуализированы метки комментариев по `btrx#13628` и связанным участкам.

## 2) `CommonModules/Odin_Склады/Ext/Module.bsl`

- Крупная доработка (`+265/-13`), основной фокус на логике ARM-123 и расчете/фиксации цены автомобиля по этапам.
- Добавлены новые ветки обработки и служебные комментарии к переходам между шагами процесса.
- Введены дополнительные блоки, влияющие на момент пересчета и применения цены.

## 3) `DataProcessors/ALLCRM_ВыгрузкаДанных/Ext/ObjectModule.bsl`

- Небольшая доработка (`+8/-0`) в логике выгрузки.
- Добавлены защитные условия и комментарии по сценариям выгрузки.

## 4) `DataProcessors/ALLServiceDashboard_ВыгрузкаДанных/Ext/ObjectModule.bsl`

- Аналогично ALLCRM (`+8/-0`): добавлены точечные условия/ветки в экспортной логике.

## 5) `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`

- Существенная доработка (`+56/-19`) в формировании данных и интеграционном обмене.
- Обновлены блоки обработки реквизитов и формирования параметров запроса.
- Добавлены/исправлены проверки и ветвления в нескольких участках модуля.

## 6) `Documents/ДокументЦУУ/Ext/ObjectModule.bsl`

- Точечные правки (`+5/-1`) в объектной логике документа.

## 7) `Documents/ЗаказНаряд/Ext/ObjectModule.bsl`

- Доработка среднего объема (`+60/-1`), добавлены новые блоки обработки данных документа.

## 8) `Documents/ЗаявкаНаПлатеж/Forms/ФормаДокумента/Ext/Form/Module.bsl`

- Доработана форма (`+33/-2`): обновлена клиентская/серверная логика обработки формы.

## 9) `Documents/ЗаявкаНаРемонт/Ext/ObjectModule.bsl`

- Значимый прирост (`+79/-2`), добавлены новые ветви обработки и проверки по процессу документа.

## 10) `Documents/ПоступлениеТоваров/Ext/ObjectModule.bsl`

- Доработка (`+41/-1`) с поддержкой новой формы подбора заявок.

## 11) `Documents/РабочийЛист/Ext/ObjectModule.bsl`

- Доработка (`+53/-1`) по логике рабочего листа и связанным условиям обработки.

---

## 12) Новый объект: `InformationRegisters/ИсторияРасчетаЦенАвтомобилей`

Добавлено:
- `InformationRegisters/ИсторияРасчетаЦенАвтомобилей.xml` (`+944`)
- `.../Ext/ManagerModule.bsl` (`+5`)
- `.../Ext/RecordSetModule.bsl` (`+5`)

Регистр введен как отдельный слой хранения истории расчета цен автомобилей.

---

## 13) Метаданные и структура

- Обновлены: `ConfigDumpInfo.xml`, `Configuration.xml`, шаблон `ПечатьДиаграммы`, метаданные документов и регистра очереди.
- Удален `AGENTS.md`.

---

## 14) Рекомендации по проверке

1. Прогнать сценарии ARM-123 в модулях `Odin_Склады` и `Odin_Общий`.
2. Проверить корректность записи и чтения регистра `ИсторияРасчетаЦенАвтомобилей`.
3. Проверить интеграционные выгрузки (`ALLCRM`, `ALLServiceDashboard`, `ЗаявкиРДС_Битрикс`).
4. Проверить пользовательские сценарии в документах `ЗаказНаряд`, `ЗаявкаНаРемонт`, `ПоступлениеТоваров`, `РабочийЛист`.
