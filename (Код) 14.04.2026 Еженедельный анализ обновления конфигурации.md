# (Код) 14.04.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `9d806210` `(Обновление 07.04.2026)`  
**Дата анализа:** 14.04.2026

---

## 1) `CommonModules/Odin_Общий/Ext/Module.bsl`

### Что изменено
- Добавлен бренд `Solaris` в ветки бренд-специфичной логики.
- Расширено условие обработки (KGM и Solaris в одном блоке).
- Добавлен блок исключений по подразделениям для этапов задач (в т.ч. "Звонок заботы", "Рекомендация 1/2", "Дружеские отношения", "Входящий трафик").
- Установлен `status = 0` в формировании структуры работ для соответствующего кейса.

### Что добавлено по коду
- Точечные расширения условий без изменения общей архитектуры модуля.

---

## 2) `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/ManagerModule.bsl`

### Что изменено
- Добавлена валидация обязательных полей (место выкупа и выкупщик) перед продолжением процесса.
- Введена проверка ограничения по сроку с последней продажи автомобиля (3/6 месяцев в зависимости от типа).
- Добавлена новая функция проверки `КлиентНедавноКупилУНасЭтотАвто(...)` с отдельным запросом.

### Что добавлено по коду
- Явные ранние выходы в невалидных сценариях.
- Новый запросный блок для бизнес-ограничения по давности продаж.

---

## 3) `Documents/АктСверкиВзаиморасчетов/Ext/ObjectModule.bsl`

### Что изменено
- Скорректирован вывод сумм в печатной форме акта сверки: нулевые суммы скрываются.
- Изменены формулы заполнения параметров областей документа на условный вывод через `?(... <> 0, Формат(...), "")`.

### Что добавлено по коду
- Единый подход "не показывать нули" применен для строковых и итоговых блоков формы.

---

## 4) `Documents/ЗаказНаряд/Ext/ObjectModule.bsl`

### Что изменено
- Добавлена точечная бизнес-логика по документу (небольшой дифф `+5/-0`).

## 5) `Documents/ПоступлениеТоваров/Ext/ObjectModule.bsl`

### Что изменено
- Небольшая корректировка обработки документа (`+4/-2`) с правкой ветвления.

## 6) `CommonModules/Odin_ЗаказНаряд/Ext/Module.bsl`

### Что изменено
- Минимальные точечные правки (`+2/-0`) в общей логике модуля.

## 7) `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`

### Что изменено
- Небольшой технический фикс (`+1/-1`) в интеграционном блоке.

---

## 8) Метаданные и структура

- Добавлены новые объекты: `СтатусВРеестре`, `ЧерныйСписокКлиентов`, интерфейс `_РуководительПСО_Мойка_ДопОборуд`.
- Удален файл внешней обработки: `ВнешняяОбработка_ВизитыПоТелефонам_Этап1.bsl`.
- Обновлены: `ConfigDumpInfo.xml`, `Configuration.xml`, форма и метаданные `ЗаявкаНаПлатеж`, шаблон `ПечатьДиаграммы`.
