# (Код) 14.04.2026 Еженедельный анализ обновления конфигурации

**Источник:** коммит `9d806210` `(Обновление 07.04.2026)`  
**Дата анализа:** 14.04.2026
**Примечание:** Оформление разделов отчета приведено к актуальному шаблону.
**Technical note:** Follow-up commit added to keep readable title in Git history.

---

## Информация по объектам конфигурации

### Какие объекты добавлены

- Справочник `СтатусВРеестре` (включая форму списка).
- Регистр сведений `ЧерныйСписокКлиентов`.
- Интерфейс `_РуководительПСО_Мойка_ДопОборуд`.

### Какие объекты изменены

- Общий модуль `Odin_Общий` — изменили: `Фомин Сергей`, `Бардашов Н.А.`.
- Общий модуль `Odin_ЗаказНаряд` — изменил: `Латыпов`.
- Обработка `ЗаявкиРДС_Битрикс` (модуль объекта).
- Документ `АктСверкиВзаиморасчетов` (модуль объекта) — изменила: `Хамитова`.
- Документ `ЗаказНаряд` (модуль объекта) — изменила: `Хамитова`.
- Документ `ПоступлениеТоваров` (модуль объекта) — изменил: `Луковников`.
- Документ `ЗаявкаНаПлатеж` (объект и форма документа).
- Регистр сведений `ОчередьНаПолучениеСертификатаВыкупаИзWeb` (модуль менеджера).
- Общий шаблон `ПечатьДиаграммы`.
- Конфигурация (технические метаданные `Configuration` и `ConfigDumpInfo`).

---

## 1) Общий модуль `Odin_Общий` — модуль

**Процедура:** `ОтправкаЗаказНаряда_в_DNM` (блок формирования `purchase_work`) — изменил: `Фомин Сергей`

**Код До:**
```bsl
БрендKGM = Справочники.ТипБренда.KGM;//+Фомин Сергей 05.03.2026 btrx#13628

...

//Для бренда KGM нужно брать не код, а артикул(номер по каталогу)
Если Бренд = БрендKGM Тогда
	СтруктураДанных_Работы.Вставить("code", ?(ЗначениеЗаполнено(СтрРаботы.Работа.Артикул), СтрРаботы.Работа.Артикул, СтрРаботы.Работа.Код));
Иначе
	СтруктураДанных_Работы.Вставить("code", ?(Бренд = БрендДжетур И ЗначениеЗаполнено(Код), Код, СтрРаботы.Работа.Код));
КонецЕсли;

...

СтруктураДанных_Работы.Вставить("sum", СтрРаботы.Сумма);
```

**Код После:**
```bsl
БрендKGM = Справочники.ТипБренда.KGM;//+Фомин Сергей 05.03.2026 btrx#13628
БрендSolaris = Справочники.ТипБренда.Solaris;//+Фомин Сергей 01.04.2026 btrx#13643

...

//Для бренда KGM нужно брать не код, а артикул(номер по каталогу)
//+Фомин Сергей 01.04.2026 btrx#13643
Если Бренд = БрендKGM ИЛИ Бренд = БрендSolaris Тогда
	СтруктураДанных_Работы.Вставить("code", ?(ЗначениеЗаполнено(СтрРаботы.Работа.Артикул), СтрРаботы.Работа.Артикул, СтрРаботы.Работа.Код));
Иначе
	СтруктураДанных_Работы.Вставить("code", ?(Бренд = БрендДжетур И ЗначениеЗаполнено(Код), Код, СтрРаботы.Работа.Код));
КонецЕсли;

...

СтруктураДанных_Работы.Вставить("sum", СтрРаботы.Сумма);
СтруктураДанных_Работы.Вставить("status", 0); //+Фомин Сергей 31.03.2026
```

**Данный участок кода:**
```bsl
Добавлено:
+ БрендSolaris = Справочники.ТипБренда.Solaris;
+ Если Бренд = БрендKGM ИЛИ Бренд = БрендSolaris Тогда
+ СтруктураДанных_Работы.Вставить("status", 0);
```

**Процедура:** `СоздатьЗакрытьЗадачуПроцессаCRM` — изменил: `Бардашов Н.А.`

**Код До:**
```bsl
Процедура СоздатьЗакрытьЗадачуПроцессаCRM(..., Этап = Неопределено, Обращение = Ложь) Экспорт
	Пользователь = ?(Пользователь = Неопределено, ПолучитьПользователяПоСотруднику(ДокументСсылка.Менеджер), Пользователь);
```

**Код После:**
```bsl
Процедура СоздатьЗакрытьЗадачуПроцессаCRM(..., Этап = Неопределено, Обращение = Ложь) Экспорт
	ПодразделенияИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьНастройки("ПодразделениеНеСоздаватьЗадачи");
	Если ПодразделенияИсключения.Количество() <> 0 Тогда
		ПодразделениеДокумента = ДокументСсылка.ПодразделениеКомпании;
		ЗначениеНастройки = ПодразделенияИсключения.Найти(ПодразделениеДокумента, "Параметр");
		Если ЗначениеНастройки <> Неопределено И (Этап = "Звонок заботы"
			Или Этап = "Рекомендация 1"
			Или Этап = "Рекомендация 2"
			Или Этап = "Дружеские отношения"
			Или Этап = "Входящий трафик") Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;
	Пользователь = ?(Пользователь = Неопределено, ПолучитьПользователяПоСотруднику(ДокументСсылка.Менеджер), Пользователь);
```

**Данный участок кода:**
```bsl
Добавлено:
+ ПодразделенияИсключения = ...ПолучитьНастройки("ПодразделениеНеСоздаватьЗадачи");
+ Если ЗначениеНастройки <> Неопределено И (Этап = "Звонок заботы" ... ) Тогда
+ 	Возврат;
```

---

## 2) Регистр сведений `ОчередьНаПолучениеСертификатаВыкупаИзWeb` — модуль менеджера

**Фрагмент 1 (валидация перед записью):**

**Код До:**
```bsl
НовЗапись.МестоВыкупа = Форма.МестоВыкупа;
НовЗапись.Выкупщик    = Форма.Выкупщик;

НовЗапись.Записать();
```

**Код После:**
```bsl
НовЗапись.МестоВыкупа = Форма.МестоВыкупа;
НовЗапись.Выкупщик    = Форма.Выкупщик;

Если НЕ ЗначениеЗаполнено(НовЗапись.МестоВыкупа)
	ИЛИ НЕ ЗначениеЗаполнено(НовЗапись.Выкупщик) Тогда
	Сообщить("Не было указано место выкупа или не был выбран выкупщик!");
	Возврат;
КонецЕсли;

НовЗапись.Записать();
```

**Данный участок кода:**
```bsl
Добавлено:
+ Если НЕ ЗначениеЗаполнено(НовЗапись.МестоВыкупа) ИЛИ НЕ ЗначениеЗаполнено(НовЗапись.Выкупщик) Тогда
+ 	Сообщить(...);
+ 	Возврат;
+ КонецЕсли;
```

**Фрагмент 2 (ограничение по давности покупки):**

**Код До:**
```bsl
Для Каждого Стр Из НастройкиСписок Цикл
	...
КонецЦикла;

Возврат Истина; // Подходит
```

**Код После:**
```bsl
Для Каждого Стр Из НастройкиСписок Цикл
	...
КонецЦикла;

Если КлиентНедавноКупилУНасЭтоАвто(ДокументСсылка) Тогда
	Возврат Ложь; // Не подходит
КонецЕсли;

Возврат Истина; // Подходит
```

**Данный участок кода:**
```bsl
Добавлено:
+ Если КлиентНедавноКупилУНасЭтоАвто(ДокументСсылка) Тогда
+ 	Возврат Ложь;
+ КонецЕсли;
+
+ Функция КлиентНедавноКупилУНасЭтоАвто(ДокументСсылка)
+ 	// запрос в РегистрНакопления.ПродажиАвтомобилей
+ КонецФункции
```

---

## 3) Документ `АктСверкиВзаиморасчетов` — модуль объекта

**Код До:**
```bsl
ОбластьСтрока.Параметры.СуммаОборотДт = Формат(Выборка.СуммаОборотДТ,ФорматВыводаСуммы);
ОбластьСтрока.Параметры.СуммаОборотДтКонтр = Формат(Выборка.СуммаОборотДТКонтр,ФорматВыводаСуммы);
ОбластьСтрока.Параметры.СуммаОборотКТ = Формат(Выборка.СуммаОборотКТ,ФорматВыводаСуммы);
ОбластьСтрока.Параметры.СуммаОборотКтКонтр = Формат(Выборка.СуммаОборотКТКонтр,ФорматВыводаСуммы);
```

**Код После:**
```bsl
//Хамитова + 31.03.26 убираем нули из акта сверки btrx№ 13343
ОбластьСтрока.Параметры.СуммаОборотДт = ?(Выборка.СуммаОборотДТ <> 0, Формат(Выборка.СуммаОборотДТ, "ЧДЦ=2"), "");
ОбластьСтрока.Параметры.СуммаОборотДтКонтр = ?(Выборка.СуммаОборотДТКонтр <> 0, Формат(Выборка.СуммаОборотДТКонтр, "ЧДЦ=2"), "");
ОбластьСтрока.Параметры.СуммаОборотКт = ?(Выборка.СуммаОборотКТ <> 0, Формат(Выборка.СуммаОборотКТ, "ЧДЦ=2"), "");
ОбластьСтрока.Параметры.СуммаОборотКтКонтр = ?(Выборка.СуммаОборотКТКонтр <> 0, Формат(Выборка.СуммаОборотКТКонтр, "ЧДЦ=2"), "");
```

**Данный участок кода:**
```bsl
Удалено:
- Формат(Выборка.СуммаОборот..., ФорматВыводаСуммы)

Добавлено:
+ ?(Выборка.СуммаОборот... <> 0, Формат(..., "ЧДЦ=2"), "")
```

---

## 4) Документ `ЗаказНаряд` — модуль объекта

**Процедура:** `ПроверкаСОПД_DNM` — изменила: `Хамитова`

**Код До:**
```bsl
Если СтрНайти(БрендИмя, "LADA") <> 0 Тогда
	БрендИмя = "LADA";
КонецЕсли;

ТипБренда=Справочники.ТипБренда.НайтиПоНаименованию(БрендИмя);
```

**Код После:**
```bsl
Если СтрНайти(БрендИмя, "LADA") <> 0 Тогда
	БрендИмя = "LADA";
КонецЕсли;
// Хамитова + 01.04.26 btrx №14454
Если БрендИмя = "HAVAL" И Организация = Справочники.Организации.НайтиПоНаименованию("АПЕЛЬСИН-АВТО ООО") Тогда
	БрендИмя = "HAVAL PRO";
КонецЕсли;
// Хамитова -

ТипБренда=Справочники.ТипБренда.НайтиПоНаименованию(БрендИмя);
```

**Данный участок кода:**
```bsl
Добавлено:
+ Если БрендИмя = "HAVAL" И Организация = ...("АПЕЛЬСИН-АВТО ООО") Тогда
+ 	БрендИмя = "HAVAL PRO";
+ КонецЕсли;
```

---

## 5) Документ `ПоступлениеТоваров` — модуль объекта

**Код До:**
```bsl
Если перРазница > 0.001 Тогда
	Предупреждение("Сумма документа и сумма заявок не сходятся. " + СуммаДокумента + " <> " + перСуммаЗаявок + " .", , "Внимание");
	Отказ = Истина;
	Возврат;
КонецЕсли;

Если Не перСуммаПроцентов = 100 Тогда
	Предупреждение("Количество процентов в Распределении не равно 100!", , "Внимание");
	Отказ = Истина;
	Возврат;
КонецЕсли;
```

**Код После:**
```bsl
Если перРазница > 0.001 Тогда
	//Предупреждение(...)
	Сообщить("Сумма документа и сумма заявок (вкладка ""Распределение ЦФО"") не сходятся. " + СуммаДокумента + " <> " + перСуммаЗаявок + " .");//Луковников 01.04.2026
	Отказ = Истина;
	Возврат;
КонецЕсли;

Если Не перСуммаПроцентов = 100 Тогда
	//Предупреждение(...)
	Сообщить("Количество процентов в Распределении не равно 100!");//Луковников 01.04.2026
	Отказ = Истина;
	Возврат;
КонецЕсли;
```

**Данный участок кода:**
```bsl
Удалено:
- Предупреждение(...)

Добавлено:
+ Сообщить("Сумма документа и сумма заявок (вкладка ""Распределение ЦФО"") ...");
+ Сообщить("Количество процентов в Распределении не равно 100!");
```

---

## 6) Общий модуль `Odin_ЗаказНаряд` — модуль

**Код До:**
```bsl
СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000016")); // Инженер по гарантии
```

**Код После:**
```bsl
СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000016")); // Инженер по гарантии
//Латыпов+ (06.04.26) Б24#15078 - Добавил в фильтр еще одну должность
СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000262")); // Старший мастер ПСО
```

**Данный участок кода:**
```bsl
Добавлено:
+ СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000262")); // Старший мастер ПСО
```

---

## 7) Обработка `ЗаявкиРДС_Битрикс` — модуль объекта

**Код До:**
```bsl
Возврат Строка(Формат(СтруктураОтвета.result, "ЧГ=0"));
```

**Код После:**
```bsl
Возврат СтруктураОтвета.result;
```

**Данный участок кода:**
```bsl
Удалено:
- Возврат Строка(Формат(СтруктураОтвета.result, "ЧГ=0"));

Добавлено:
+ Возврат СтруктураОтвета.result;
```

---

