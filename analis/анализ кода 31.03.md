# Полный анализ кода обновления 31.03.2026

**Основание:** детальный повторный разбор коммита `0c71db6a` с фокусом на максимальное покрытие.

---

## Что включено в полный набор

1. Полный реестр **всех** изменений (`A/M/D/R`) по файлам.
2. Детальный код-разбор всех измененных `.bsl` модулей (до/после/добавлено).
3. Разбор измененных XML/метаданных.
4. Перечень всех переименований.
5. Отдельный полный список удалений.

---

## Где смотреть полный реестр (100%)

- `diff_name_status_31.03.2026.txt` — общий список (`A/M/D/R`)
- `diff_added_31.03.2026.txt`
- `diff_modified_31.03.2026.txt`
- `diff_deleted_31.03.2026.txt`
- `diff_renamed_31.03.2026.txt`

---

## Детальный кодовый разбор

Основной файл с блоками **Код до / Код после / Что добавлено по коду**:

- `(Код) 31.03.2026 Еженедельный анализ обновления конфигурации.md`

Покрытые модули:
- `CommonModules/Odin_Общий/Ext/Module.bsl`
- `DataProcessors/ALLMaintenanceSchedule_ВыгрузкаДанных/Ext/ObjectModule.bsl`
- `DataProcessors/Б24_ВыгрузкаКонтрагентов/Ext/ObjectModule.bsl`
- `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`
- `DataProcessors/Обмен_с_ОЗОН/Forms/ФормаОбмена/Ext/Form/Module.bsl`
- `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/RecordSetModule.bsl`

---

## Ключевые технические выводы

- В `Odin_Общий` изменены параметры графика и формат URL (`dealerId` исключен).
- В `ALLMaintenanceSchedule_ВыгрузкаДанных` добавлено исключение повторной очереди для `Lada`.
- В `Б24_ВыгрузкаКонтрагентов` добавлен параметр `ВыбранныйДоговор` и фильтрация в запросе.
- В `ЗаявкиРДС_Битрикс` исправлены строковые операции и переработано формирование категорий.
- В `Обмен_с_ОЗОН` добавлен сценарий массовой выгрузки по торговым площадкам.
- В `ОчередьНаПолучениеСертификата...` очищена шаблонная часть модуля.

---

## Что проверить на стенде

1. Сценарий выгрузки графика мастера в `Odin_Общий` (параметры URL и интервал времени).
2. Поведение очереди при ошибке для `Lada`.
3. Отбор по договору в Б24 (`ВыбранныйДоговор=0` и конкретный договор).
4. Поиск `ЗаявкаId` и список категорий в `ЗаявкиРДС_Битрикс`.
5. Массовую выгрузку `ПоВсемПлощадкам` в ОЗОН.
