# Полный анализ кода обновления

**Источник:** diff `MyConfig1` ↔ `Desktop\New\2403` для `24.03.2026`.  
**Дата:** 24.03.2026.

Для каждого изменения приведены три блока: **Код До**, **Код После**, **Данный участок кода**. Полный перечень блоков по всем модулям — в отчёте «(Код) 24.03.2026 Еженедельный анализ обновления конфигурации.md».

---

## Сводная таблица

| Модуль | Номер/автор | Процедура/Функция | Суть |
|--------|-------------|-------------------|------|
| Odin_Общий | btrx#14002, Фомин | DSF_ЗагрузитьДанныеПоГрафикуРаботыМастера | Убран dealerId из URL |
| Odin_Общий | №12516, Бардашов | СоздатьОбновитьДокументСобытие | Ветка «Тест драйв», комментарии |
| Odin_Общий | №12516, Бардашов | СоздатьОбновитьДокументСобытие | Раскомментированы СоздатьЗакрытьЗадачуПроцессаCRM, ДобавитьСобытиеКЗаписиВРегистра |
| ALLMaintenanceSchedule_ВыгрузкаДанных | btrx#14002, Фомин | (цикл выгрузки) | Lada: ВОчереди=Ложь при ошибке |
| ЗаявкиРДС_Битрикс | — | ВыгрузитьБанковскийСчетКонтрагентаВБитрикс | Условие: КонтрагентРС=Неопределено |
| ЗаявкиРДС_Битрикс | — | (несколько) | СтрДлина, total=1000, ЗаявкаId, UF_CRM_49, ОбработатьПлатёжныеПараметры |
| Обмен_с_ОЗОН | — | ФормаОбмена | Изменения в процедурах формы |
| ОчередьНаПолучениеСертификатаВыкупаИзWeb | — | RecordSetModule | Модуль сокращён до комментария |

---

## CommonModules/Odin_Общий/Ext/Module.bsl

### btrx#14002 (Фомин С., 18.03.2026)

**Изменение в Процедура DSF_ЗагрузитьДанныеПоГрафикуРаботыМастера:**

**Суть правки:** Убран параметр dealerId из URL запроса графика работ мастера. API DNM больше не требует dealerId.

**Что меняется/эффект:** URL упрощён; day и userId сдвинуты в шаблоне.

**Код До:**

```bsl
	массивПодСтрок = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(СтруктураДанных.date,"-",Истина,Истина);
	
	АдресПроверки = СтрШаблон("%1?year=%2&month=%3&dealerId=%4&day=%5&userId=%6", 
								Адрес, 
								массивПодСтрок[0], //year
								СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[1]), //month
								СтруктураДанныхДляПодключения.БрендID, //dealerId
								СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[2]), //day
								Формат(СтруктураДанных.user_id, "ЧГ=0")); //userId
	
	ЗапросHTTP = Новый HTTPЗапрос(АдресПроверки, Заголовки);
```

**Код После:**

```bsl
	массивПодСтрок = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(СтруктураДанных.date,"-",Истина,Истина);
	
	//+ Фомин Сергей 18.03.2026 btrx#14002
	// было %1?year=%2&month=%3&dealerId=%4&day=%5&userId=%6
	АдресПроверки = СтрШаблон("%1?year=%2&month=%3&day=%4&userId=%5", 
								Адрес, 
								массивПодСтрок[0], //year
								СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[1]), //month
								//СтруктураДанныхДляПодключения.БрендID, //dealerId
								СтроковыеФункцииКлиентСервер.СтрокаВЧисло(массивПодСтрок[2]), //day
								Формат(СтруктураДанных.user_id, "ЧГ=0")); //userId
	
	ЗапросHTTP = Новый HTTPЗапрос(АдресПроверки, Заголовки);
```

**Данный участок кода:**

- **Удалено:** dealerId в шаблоне; аргумент БрендID.
- **Добавлено:** комментарий; шаблон без dealerId; закомментирована строка с БрендID.

---

### №12516 (Бардашов Н.А., 16.03.2026)

**Изменение в Процедура СоздатьОбновитьДокументСобытие:**

**Суть правки:** Добавлена ветка «Тест драйв» для заполнения Описание, ЗначениеСвойства, ДатаСобытия, Содержание.

**Что меняется/эффект:** При Свойство = "Тест драйв" создаётся/обновляется событие.

**Код До:**

```bsl
		ИначеЕсли Свойство = "Входящий трафик" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Входящий трафик";
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
			
		КонецЕсли;
```

**Код После:**

```bsl
		ИначеЕсли Свойство = "Входящий трафик" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Входящий трафик";
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
		//+Бардашов Н.А. 16.03.2026 №12516	
		ИначеЕсли Свойство = "Тест драйв" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Тест драйв";
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
	    //-Бардашов Н.А. 16.03.2026 №12516
		КонецЕсли;
```

**Данный участок кода:** Добавлен блок ИначеЕсли Свойство = "Тест драйв" с присвоением Описание, ЗначениеСвойства, ДатаСобытия, Содержание.

---

### №12516 (Бардашов Н.А., 24.02.2026)

**Изменение в Процедура СоздатьОбновитьДокументСобытие:**

**Суть правки:** Раскомментированы вызовы СоздатьЗакрытьЗадачуПроцессаCRM и ДобавитьСобытиеКЗаписиВРегистра.

**Что меняется/эффект:** При тест-драйве создаётся задача CRM и добавляется запись в регистр.

**Код До:**

```bsl
		ОбновитьСвойствоДокументаСобытие(СобытиеПоТестДрайву.Ссылка, "Тест драйв"); 
		
		//+Бардашов Н.А. 24.02.2026 №12516 
	//	СоздатьЗакрытьЗадачуПроцессаCRM(СобытиеПоТестДрайву.Ссылка, "Входящий трафик", "Связаться с клиентом по входящему трафику", ,"Связаться с клиентом по входящему трафику");
	//
	//	ДобавитьСобытиеКЗаписиВРегистра(СобытиеПоТестДрайву.Ссылка, СтруктураДанных.Идентификация);
        //-Бардашов Н.А. 24.02.2026 №12516
```

**Код После:**

```bsl
		ОбновитьСвойствоДокументаСобытие(СобытиеПоТестДрайву.Ссылка, "Тест драйв"); 
		
		//+Бардашов Н.А. 24.02.2026 №12516 
		СоздатьЗакрытьЗадачуПроцессаCRM(СобытиеПоТестДрайву.Ссылка, "Входящий трафик", "Связаться с клиентом по входящему трафику", ,"Связаться с клиентом по входящему трафику");
	
		ДобавитьСобытиеКЗаписиВРегистра(СобытиеПоТестДрайву.Ссылка, СтруктураДанных.Идентификация);
        //-Бардашов Н.А. 24.02.2026 №12516
```

**Данный участок кода:** Удалены символы комментария `//` перед двумя вызовами — процедуры выполняются.

---

## DataProcessors/ALLMaintenanceSchedule_ВыгрузкаДанных/Ext/ObjectModule.bsl

### btrx#14002 (Фомин С., 18.03.2026)

**Суть правки:** Для Lada при ошибке выгрузки запись не ставится повторно в очередь.

**Что меняется/эффект:** Снижение нагрузки при сбоях DNM для Lada.

**Код До:**

```bsl
		Odin_Общий.DSF_ПолучитьIDДокумента(Запись, СтруктураДанныхДляПодключения, "service/maintenance-schedule?code=" + Запись.ЗаявкаНаРемонт.Номер);
		Запись.ВОчереди = ?(Запись.ТекстОшибки = "" И Запись.IDКлиентаDNM <> "" И Запись.IDДокументаDNM <> "" , Ложь, Истина);
		
		НаборПротокола.Записать();
```

**Код После:**

```bsl
		Odin_Общий.DSF_ПолучитьIDДокумента(Запись, СтруктураДанныхДляПодключения, "service/maintenance-schedule?code=" + Запись.ЗаявкаНаРемонт.Номер);
		Запись.ВОчереди = ?(Запись.ТекстОшибки = "" И Запись.IDКлиентаDNM <> "" И Запись.IDДокументаDNM <> "" , Ложь, Истина);
		
		//+Фомин Сергей 18.03.2026 btrx#14002
		//Один раз пытаемся выгрузить. Повторно при ошибке не ставим на выгрузку. Только для Lada
		Если ТипБренда = Справочники.ТипБренда.Lada Тогда
			Запись.ВОчереди = Ложь;	
		КонецЕсли;
		//-Фомин Сергей 18.03.2026 btrx#14002
		
		НаборПротокола.Записать();
```

**Данный участок кода:** Добавлена проверка ТипБренда = Lada и при совпадении — Запись.ВОчереди = Ложь.

---

## DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl

### Изменение 1 — ВыгрузитьБанковскийСчетКонтрагентаВБитрикс

**Суть правки:** Условие раннего возврата: `КонтрагентРС=Неопределено` вместо проверки расчётного счёта.

**Код До:** `Если СамКонтрагент.РасчСчет=Справочники.БанковскиеСчета.ПустаяСсылка() Тогда Возврат 0 КонецЕсли;`

**Код После:** `Если КонтрагентРС=Неопределено Тогда Возврат 0 КонецЕсли;`

**Данный участок кода:** Заменено условие.

---

### Изменение 2 — СтрДлина

**Суть правки:** Исправление скобок: `СтрДлина(рабочийКод)-1` вместо `СтрДлина(рабочийКод-1)`.

**Данный участок кода:** `СредКопия=Лев(рабочийКод,СтрДлина(рабочийКод)-1);`

---

### Изменение 3 — total=1000

**Суть правки:** Закомментирована строка `строкаТаблица= строкаТаблица+"&total=1000"`.

---

### Изменение 4 — ЗаявкаId

**Суть правки:** ЗаявкаId получается через Формат(..., "ЧГ=0").

**Код До:** `ЗаявкаId=Заявка.Получить("id");`  
**Код После:** `ЗаявкаId=Формат(Заявка.Получить("id"),"ЧГ=0");`

---

### Изменение 5 — UF_CRM_49, даты, категории

**Суть правки:** Добавлены списКатегорий, текстКатегории; логика UF_CRM_49_CATEGORY из справочника; при пустой ДатеВыкупа — пустая строка; параметры заявки. Детали — в отчёте (Код) и diff_zaявки_24.03.txt.

---

### Изменение 6 — ОбработатьПлатёжныеПараметры

**Суть правки:** Проверка Свойство("ОбменМенюБитрикс"); рефакторинг инициализации и заполнения структуры через Свойство/Вставить.

---

## DataProcessors/Обмен_с_ОЗОН, InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb

См. отчёт «(Код) 24.03.2026 Еженедельный анализ обновления конфигурации.md» и diff-файлы.
