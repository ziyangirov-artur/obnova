# Полный анализ кода обновления 01.04.2026

**Основание:** коммит `772d70d6` `(Обновление 01.04.2026)`.

---

## Сводка

- Всего файлов: `5` (`A=0`, `M=5`, `D=0`, `R=0`).
- Основной функциональный фокус:
  - пересчет/управление ценой в `Odin_Склады`;
  - упрощение кастомизации в `СобственныйОбработчикККТ`;
  - устойчивое определение корня выгрузки в `_tmp_mirror_new_to_myconfig1.ps1`.

---

## Таблица изменений

| Файл | Изменение | Эффект |
|---|---|---|
| `CommonModules/Odin_Склады/Ext/Module.bsl` | Уточнены условия изменения цены, выделен отдельный запрос для комиссионных авто | Более предсказуемый расчет наценки |
| `DataProcessors/СобственныйОбработчикККТ/Ext/ObjectModule.bsl` | Удалена большая часть самописной агентской логики | Сближение с базовой логикой `1c_kkt_54fz` |
| `_tmp_mirror_new_to_myconfig1.ps1` | Рекурсивный поиск `ConfigDumpInfo.xml` + выбор ближайшего корня | Корректное зеркалирование при глубокой вложенности |
| `ConfigDumpInfo.xml` | Служебное обновление выгрузки | Актуальные метаданные дампа |
| `CommonTemplates/ПечатьДиаграммы/Ext/Template.xml` | Точечная правка шаблона | Локальная корректировка шаблона |

---

## Контрольные проверки

1. Проверить сценарий изменения цены в шагах, где не должна происходить смена наценки.
2. Проверить печать/пробитие чека с текущей логикой ККТ после удаления кастомных блоков.
3. Проверить работу скрипта зеркалирования на структуре `New` с дополнительной вложенностью.

---

## Полный реестр

- `diff_name_status_01.04.2026.txt`
- `diff_added_01.04.2026.txt`
- `diff_modified_01.04.2026.txt`
- `diff_deleted_01.04.2026.txt`
- `diff_renamed_01.04.2026.txt`
