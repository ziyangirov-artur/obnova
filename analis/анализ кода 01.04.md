# Полный анализ кода обновления 01.04.2026

**Основание:** коммит `f3a3f8dc` `(Обновление 01.04.2026)`.

---

## 1. Общая картина изменений

- Всего: `25` файлов (`A=6`, `M=18`, `D=1`, `R=0`).
- Основной объем кода пришелся на:
  - `CommonModules/Odin_Склады/Ext/Module.bsl` (`+265/-13`);
  - `Documents/ЗаказНаряд/Ext/ObjectModule.bsl` (`+60/-1`);
  - `Documents/ЗаявкаНаРемонт/Ext/ObjectModule.bsl` (`+79/-2`);
  - `Documents/РабочийЛист/Ext/ObjectModule.bsl` (`+53/-1`);
  - `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl` (`+56/-19`).

---

## 2. Ключевые смысловые изменения

1. **ARM-123 / цена автомобиля по этапам**
   - Сконцентрировано в `Odin_Склады` и частично затрагивает связанные документы.
   - Усилен контроль момента, когда цена может изменяться в процессе перехода по шагам.

2. **Новый регистр истории цен**
   - Добавлен `InformationRegisters/ИсторияРасчетаЦенАвтомобилей` (+ XML и 2 модуля).
   - Создана база для аудита/истории пересчета цен.

3. **Интеграционные доработки**
   - Обновлены обработчики выгрузки в `ALLCRM`, `ALLServiceDashboard`, `ЗаявкиРДС_Битрикс`.
   - Добавлены проверки и корректировки формирования данных.

4. **Документы и формы**
   - Доработаны объектные модули и формы документов (`ДокументЦУУ`, `ЗаказНаряд`, `ЗаявкаНаПлатеж`, `ЗаявкаНаРемонт`, `ПоступлениеТоваров`, `РабочийЛист`).

---

## 3. Где смотреть полный реестр

- `diff_name_status_01.04.2026.txt` — полный список всех изменений.
- `diff_added_01.04.2026.txt`
- `diff_modified_01.04.2026.txt`
- `diff_deleted_01.04.2026.txt`
- `diff_renamed_01.04.2026.txt`

---

## 4. Что проверить в базе после обновления

1. Переходы шагов и изменение цены по сценарию ARM-123.
2. Запись истории в регистр `ИсторияРасчетаЦенАвтомобилей`.
3. Документные сценарии: проведение, пересчет, заполнение формы подбора заявок.
4. Обменные сценарии: выгрузка данных в CRM/дашборд/Битрикс.
