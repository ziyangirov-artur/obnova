# Полный анализ кода обновления 25.03.2026

**Источник анализа:** коммит `0c71db6a` `(Обновление 25.03.2026)`.

---

## Что изменилось в формате анализа

По вашей просьбе добавлена детальная раскладка по каждому изменению:
- участок кода;
- **Код до**;
- **Код после**;
- отдельный блок **Что добавлено по коду**.

Полная детализация вынесена в файл:  
`(Код) 25.03.2026 Еженедельный анализ обновления конфигурации.md`.

---

## Сводная таблица изменений по модулям

| № | Файл | Конкретное изменение в коде |
|---|---|---|
| 1 | `CommonModules/Odin_Общий/Ext/Module.bsl` | `time_to: 20:00 -> 22:00`; URL графика без `dealerId` |
| 2 | `DataProcessors/ALLMaintenanceSchedule_ВыгрузкаДанных/Ext/ObjectModule.bsl` | Добавлен блок `Если ТипБренда = ...Lada Тогда Запись.ВОчереди = Ложь` |
| 3 | `DataProcessors/Б24_ВыгрузкаКонтрагентов/Ext/ObjectModule.bsl` | Добавлен параметр `ВыбранныйДоговор` в сигнатуру, `ГДЕ` и параметры запроса |
| 4 | `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl` | Исправлено `СтрДлина(РабочийКод)-1`, отключен `total=1000`, `id` через `Формат`, категории динамически |
| 5 | `DataProcessors/Обмен_с_ОЗОН/Forms/ФормаОбмена/Ext/Form/Module.bsl` | Добавлены `ПоВсемПлощадкам` и `ПоВсемПлощадкамНаСервере` с циклом по площадкам |
| 6 | `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/RecordSetModule.bsl` | Убраны шаблонные области модуля, оставлен заголовок |

---

## Контрольные проверки

1. Проверить формирование URL графика мастера и интервал времени в `Odin_Общий`.
2. Проверить, что Lada-записи не возвращаются повторно в очередь выгрузки.
3. Проверить отбор договоров в Б24 при заполненном/пустом `ВыбранныйДоговор`.
4. Проверить поиск `ЗаявкаId` и формирование массива категорий в `ЗаявкиРДС_Битрикс`.
5. Проверить выполнение кнопки «По всем площадкам» в ОЗОН и корректный проход цикла.
