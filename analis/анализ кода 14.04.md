# Полный анализ кода обновления 14.04.2026

**Основание:** коммит `9d806210` `(Обновление 07.04.2026)`.

---

## Информация по объектам конфигурации

### Какие объекты добавлены

- Справочник `СтатусВРеестре` (включая форму списка).
- Регистр сведений `ЧерныйСписокКлиентов`.
- Интерфейс `_РуководительПСО_Мойка_ДопОборуд`.

### Какие объекты изменены

- Общий модуль `Odin_Общий`.
- Общий модуль `Odin_ЗаказНаряд`.
- Обработка `ЗаявкиРДС_Битрикс` (модуль объекта).
- Документ `АктСверкиВзаиморасчетов` (модуль объекта).
- Документ `ЗаказНаряд` (модуль объекта).
- Документ `ПоступлениеТоваров` (модуль объекта).
- Документ `ЗаявкаНаПлатеж` (объект и форма документа).
- Регистр сведений `ОчередьНаПолучениеСертификатаВыкупаИзWeb` (модуль менеджера).
- Общий шаблон `ПечатьДиаграммы`.
- Конфигурация (технические метаданные `Configuration` и `ConfigDumpInfo`).

---

## Сводка

- Всего изменений: `17` файлов (`A=4`, `M=12`, `D=1`, `R=0`).
- Измененные `.bsl`: `7` файлов.
- Формат разбора: по каждому изменению приведены блоки `Код До`, `Код После`, `Данный участок кода`.

---

## Детальный разбор `.bsl`

### 1. `CommonModules/Odin_Общий/Ext/Module.bsl`

**Код До:**
```bsl
БрендKGM = Справочники.ТипБренда.KGM;
Если Бренд = БрендKGM Тогда
	СтруктураДанных_Работы.Вставить("code", ?(ЗначениеЗаполнено(СтрРаботы.Работа.Артикул), СтрРаботы.Работа.Артикул, СтрРаботы.Работа.Код));
КонецЕсли;
```

**Код После:**
```bsl
БрендKGM = Справочники.ТипБренда.KGM;
БрендSolaris = Справочники.ТипБренда.Solaris;
Если Бренд = БрендKGM ИЛИ Бренд = БрендSolaris Тогда
	СтруктураДанных_Работы.Вставить("code", ?(ЗначениеЗаполнено(СтрРаботы.Работа.Артикул), СтрРаботы.Работа.Артикул, СтрРаботы.Работа.Код));
КонецЕсли;
СтруктураДанных_Работы.Вставить("status", 0);
```

**Данный участок кода:**
```bsl
Добавлено:
+ БрендSolaris = Справочники.ТипБренда.Solaris;
+ Если Бренд = БрендKGM ИЛИ Бренд = БрендSolaris Тогда
+ СтруктураДанных_Работы.Вставить("status", 0);
```

### 2. `InformationRegisters/ОчередьНаПолучениеСертификатаВыкупаИзWeb/Ext/ManagerModule.bsl`

**Код До:**
```bsl
НовЗапись.МестоВыкупа = Форма.МестоВыкупа;
НовЗапись.Выкупщик = Форма.Выкупщик;
НовЗапись.Записать();
...
Возврат Истина; // Подходит
```

**Код После:**
```bsl
НовЗапись.МестоВыкупа = Форма.МестоВыкупа;
НовЗапись.Выкупщик = Форма.Выкупщик;
Если НЕ ЗначениеЗаполнено(НовЗапись.МестоВыкупа)
	ИЛИ НЕ ЗначениеЗаполнено(НовЗапись.Выкупщик) Тогда
	Сообщить("Не было указано место выкупа или не был выбран выкупщик!");
	Возврат;
КонецЕсли;
НовЗапись.Записать();
...
Если КлиентНедавноКупилУНасЭтоАвто(ДокументСсылка) Тогда
	Возврат Ложь;
КонецЕсли;
```

**Данный участок кода:**
```bsl
Добавлено:
+ Валидация заполнения МестоВыкупа/Выкупщик перед записью.
+ Вызов КлиентНедавноКупилУНасЭтоАвто(ДокументСсылка).
+ Новая функция проверки давности покупки по регистру продаж.
```

### 3. `Documents/АктСверкиВзаиморасчетов/Ext/ObjectModule.bsl`

**Код До:**
```bsl
ОбластьСтрока.Параметры.СуммаОборотДт = Формат(Выборка.СуммаОборотДТ,ФорматВыводаСуммы);
ОбластьСтрока.Параметры.СуммаОборотДтКонтр = Формат(Выборка.СуммаОборотДТКонтр,ФорматВыводаСуммы);
```

**Код После:**
```bsl
ОбластьСтрока.Параметры.СуммаОборотДт = ?(Выборка.СуммаОборотДТ <> 0, Формат(Выборка.СуммаОборотДТ, "ЧДЦ=2"), "");
ОбластьСтрока.Параметры.СуммаОборотДтКонтр = ?(Выборка.СуммаОборотДТКонтр <> 0, Формат(Выборка.СуммаОборотДТКонтр, "ЧДЦ=2"), "");
```

**Данный участок кода:**
```bsl
Удалено:
- Формат(..., ФорматВыводаСуммы)

Добавлено:
+ ?(... <> 0, Формат(..., "ЧДЦ=2"), "")
```

### 4. `Documents/ЗаказНаряд/Ext/ObjectModule.bsl`

**Код До:**
```bsl
Если СтрНайти(БрендИмя, "LADA") <> 0 Тогда
	БрендИмя = "LADA";
КонецЕсли;
```

**Код После:**
```bsl
Если СтрНайти(БрендИмя, "LADA") <> 0 Тогда
	БрендИмя = "LADA";
КонецЕсли;
Если БрендИмя = "HAVAL" И Организация = Справочники.Организации.НайтиПоНаименованию("АПЕЛЬСИН-АВТО ООО") Тогда
	БрендИмя = "HAVAL PRO";
КонецЕсли;
```

**Данный участок кода:**
```bsl
Добавлено:
+ Спец-правило HAVAL -> HAVAL PRO для организации "АПЕЛЬСИН-АВТО ООО".
```

### 5. `Documents/ПоступлениеТоваров/Ext/ObjectModule.bsl`

**Код До:**
```bsl
Предупреждение("Сумма документа и сумма заявок не сходятся. ...", , "Внимание");
Предупреждение("Количество процентов в Распределении не равно 100!", , "Внимание");
```

**Код После:**
```bsl
Сообщить("Сумма документа и сумма заявок (вкладка ""Распределение ЦФО"") не сходятся. ...");
Сообщить("Количество процентов в Распределении не равно 100!");
```

**Данный участок кода:**
```bsl
Удалено:
- Предупреждение(...)

Добавлено:
+ Сообщить(...)
```

### 6. `CommonModules/Odin_ЗаказНаряд/Ext/Module.bsl`

**Код До:**
```bsl
СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000016")); // Инженер по гарантии
```

**Код После:**
```bsl
СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000016")); // Инженер по гарантии
СписокДолжностей.Добавить(Справочники.Должности.НайтиПоКоду("ЦБ000262")); // Старший мастер ПСО
```

**Данный участок кода:**
```bsl
Добавлено:
+ СписокДолжностей.Добавить(... "ЦБ000262");
```

### 7. `DataProcessors/ЗаявкиРДС_Битрикс/Ext/ObjectModule.bsl`

**Код До:**
```bsl
Возврат Строка(Формат(СтруктураОтвета.result, "ЧГ=0"));
```

**Код После:**
```bsl
Возврат СтруктураОтвета.result;
```

**Данный участок кода:**
```bsl
Удалено:
- Возврат Строка(Формат(СтруктураОтвета.result, "ЧГ=0"));

Добавлено:
+ Возврат СтруктураОтвета.result;
```

---

## Полный реестр изменений

- `diff_name_status_14.04.2026.txt`
- `diff_added_14.04.2026.txt`
- `diff_modified_14.04.2026.txt`
- `diff_deleted_14.04.2026.txt`
- `diff_renamed_14.04.2026.txt`
