# (Код) 21.04.2026 Еженедельный анализ обновления конфигурации

**Источник:** сравнение `MyConfig1` и `New/2104`
**Дата анализа:** 21.04.2026

---

## Информация по объектам конфигурации

### Какие объекты добавлены

- Обработка `Обмен_с_ОЗОН`.
- Отчет `АктивностьПользователейЗаНеделю`.
- Отчет `ОтчетПоЗаказНарядам`.

### Какие объекты изменены

- Документ `ЗаказНаряд`.
- Документ `ИзвлечениеТоваровИзПроизводства`.
- Документ `ПоступлениеТоваров`.
- Документ `РеализацияТоваров`.
- Интерфейс `_ПолныйТехПоддержка`.
- Интерфейс `_РуководительПСО_Мойка`.
- Интерфейс `_РуководительСЦ_ОтчетПСО`.
- Конфигурация (структура метаданных).
- Конфигурация (технические метаданные ConfigDumpInfo).
- Обработка `Web_ЗагрузкаДанных`.
- Обработка `АРМАдминистратор`.
- Обработка `МониторАвтосервиса`.
- Обработка `НеобходимоСогласовать`.
- Обработка `Обмен_с_ОЗОН`.
- Обработка `СобственныйОбработчикККТ`.
- Общая форма `ОтправкаВЧЗ`.
- Общая форма `ФормаСпискаМаркировок`.
- Общий модуль `Odin_Общий`.
- Общий модуль `Шина1С`.
- Общий модуль `обПроцедурыОбщегоНазначенияУФ`.
- Общий шаблон `ПечатьДиаграммы`.
- Отчет `АнализРаботыТелемаркетолога`.
- Отчет `ОшибкиВыгрузкиDNM`.
- Отчет `СводныйАнализПродажАвтомобилей`.
- Регистр сведений `РегистрацияЗвонковИКонтактов`.
- Регистр сведений `СообщенияПользователей`.

---

## 1) Обработка Обмен_с_ОЗОН — модуль объекта

**Процедура:** `Глобальный контекст`

**Код До:**

```bsl
// фрагмент отсутствует
```

**Код После:**

```bsl
﻿Перем ТабВыгрузки;   
	
Функция ВыполнитьОбменПоВсем() Экспорт
	ПоВсемПлощадкамНаСервере();	
КонецФункции

Процедура ПоВсемПлощадкамНаСервере() Экспорт 
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	|	ТорговыеПлощадки.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ТорговыеПлощадки КАК ТорговыеПлощадки
	|ГДЕ
	|	ТорговыеПлощадки.УчаствуетВАвтообмене
	|	И НЕ ТорговыеПлощадки.ПометкаУдаления";
	
	ТР=Запрос.Выполнить().Выгрузить();
	
	Для каждого СТР из ТР цикл
		
		ЭтотОбъект.Сценарий=СТР.Ссылка;    
		ЭтотОбъект.КатегорияТовара=ЭтотОбъект.Сценарий.Категория;
		
		ТабВыгрузки=ЗапросОбщийНаСервере();
		
		ВыгрузитьВыбранноеНаСервере();
		
	КонецЦикла;   
	
КонецПроцедуры
	
&НаСервере
Функция ЗапросОбщийНаСервере() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = ЭтотОбъект.Сценарий.ТекстЗапроса;
	
	УстановитьБезопасныйРежим(Истина);
	Для Каждого СтрокаПараметры Из ЭтотОбъект.Сценарий.ПараметрыЗапроса Цикл
		Выполнить("Запрос.УстановитьПараметр(СтрокаПараметры.Параметр," + СтрокаПараметры.Значение + ");");
	КонецЦикла;
	УстановитьБезопасныйРежим(Ложь);
	
	Запрос.УстановитьПараметр("ЭтаПлощадка", ЭтотОбъект.Сценарий);
	
	Попытка
		Результат = Запрос.Выполнить();
	Исключение
		ТекстОшибки = "Не удалось выполнить запрос. Проверьте правильность написания запроса и параметров.";
	    Сообщить(ТекстОшибки);
	КонецПопытки;
		
	РезультирующаяТаблица = Результат.Выгрузить();  
	
	Возврат РезультирующаяТаблица;
	
КонецФункции

&НаСервере
Процедура ВыгрузитьВыбранноеНаСервере()
	// Вставить содержимое обработчика.   
	
	ТабДанных=0;
	
	МассивАвто=Новый массив;       
	
	МассивОстатки=Новый Массив;
		
	Для каждого СтрокаОбмена из ТабВыгрузки цикл
		
		Если НЕ СтрокаОбмена.Грузим Тогда Продолжить; КонецЕсли;   
		
		Если ТабДанных=0 Тогда ТабДанных=ЗапросОбщийНаСервере(); КонецЕсли; 
		
		СамоАвто=Новый Соответствие;
		
		СамоАвто.Вставить("description_category_id",ЭтотОбъект.КатегорияТовара.КатегорияТовара);  
				
		НужнаяСтрока=ТабДанных.Найти(СтрокаОбмена.Артикул,"Артикул");
		
		АтрибутыАвто=ПолучитьАтрибутыСтроки(НужнаяСтрока);
		
		СамоАвто.Вставить("attributes",АтрибутыАвто);  
		СамоАвто.Вставить("barcode",СокрЛП(НужнаяСтрока.Автомобиль.VIN));  
		//СамоАвто.Вставить("color_image","Маркетинговый цвет");  
		//СамоАвто.Вставить("complex_attributes","Характеристики с вложенными атрибутам");  
		СамоАвто.Вставить("currency_code","RUB");  
		СамоАвто.Вставить("depth",200);  
		СамоАвто.Вставить("dimension_unit","cm");  
		//СамоАвто.Вставить("geo_names","ТУТ ШТРИХКОД");  
		СамоАвто.Вставить("height",200);  
		//СамоАвто.Вставить("image_group_id","ТУТ ШТРИХКОД");  
		
		СсылкиНаФото = сме_ИнтеграцияСервер.СсылкиНаФотоАвтомобиля(НужнаяСтрока.Автомобиль);
		Если СсылкиНаФото<>Неопределено Тогда              
			Если СсылкиНаФото.Количество()=0 Тогда 
				Сообщить("Пропущен "+СокрЛп(НужнаяСтрока.Автомобиль.VIN));
				Продолжить; 
			КонецЕсли;
			СамоАвто.Вставить("images",СсылкиНаФото); 
		Иначе
			Продолжить;
		КонецЕсли;	       
		
		
		
		//СамоАвто.Вставить("images360","ТУТ ШТРИХКОД");  
		СамоАвто.Вставить("name",СокрЛП(НужнаяСтрока.Наименование));  
		СамоАвто.Вставить("offer_id",СокрЛП(НужнаяСтрока.Артикул));  
		//СамоАвто.Вставить("pdf_list","ТУТ ШТРИХКОД");  
		//СамоАвто.Вставить("premium_price","ТУТ ШТРИХКОД");  
		СамоАвто.Вставить("price",Формат((сме_ИнтеграцияСервер.ЦенаПродажи(НужнаяСтрока.Автомобиль, ТекущаяДата())+70000),"ЧГ=0"));  
		СамоАвто.Вставить("weight",2000);  
		СамоАвто.Вставить("weight_unit","kg");  
		СамоАвто.Вставить("width",200);  
		СамоАвто.Вставить("primary_image",СсылкиНаФото[0]);    
		СамоАвто.Вставить("type_id",ЭтотОбъект.КатегорияТовара.ТипТовара); 
		//СамоАвто.Вставить("service_type","ТУТ ШТРИХКОД");  
// ... сокращено, всего строк: 424
```

**Данный участок кода:**

```bsl
+ ﻿Перем ТабВыгрузки;   
+ 	
+ Функция ВыполнитьОбменПоВсем() Экспорт
+ 	ПоВсемПлощадкамНаСервере();	
+ КонецФункции
+ 
+ Процедура ПоВсемПлощадкамНаСервере() Экспорт 
+ 	
+ 	Запрос=Новый Запрос;
+ 	
+ 	Запрос.Текст="ВЫБРАТЬ
+ 	|	ТорговыеПлощадки.Ссылка КАК Ссылка
+ 	|ИЗ
+ 	|	Справочник.ТорговыеПлощадки КАК ТорговыеПлощадки
+ 	|ГДЕ
+ 	|	ТорговыеПлощадки.УчаствуетВАвтообмене
+ 	|	И НЕ ТорговыеПлощадки.ПометкаУдаления";
+ 	
+ 	ТР=Запрос.Выполнить().Выгрузить();
+ 	
+ 	Для каждого СТР из ТР цикл
+ 		
+ 		ЭтотОбъект.Сценарий=СТР.Ссылка;    
+ 		ЭтотОбъект.КатегорияТовара=ЭтотОбъект.Сценарий.Категория;
+ 		
+ 		ТабВыгрузки=ЗапросОбщийНаСервере();
+ 		
+ 		ВыгрузитьВыбранноеНаСервере();
+ 		
+ 	КонецЦикла;   
+ 	
+ КонецПроцедуры
+ 	
+ &НаСервере
+ Функция ЗапросОбщийНаСервере() Экспорт
+ 	
+ 	Запрос = Новый Запрос;
+ 	Запрос.Текст = ЭтотОбъект.Сценарий.ТекстЗапроса;
+ 	
+ 	УстановитьБезопасныйРежим(Истина);
+ 	Для Каждого СтрокаПараметры Из ЭтотОбъект.Сценарий.ПараметрыЗапроса Цикл
+ 		Выполнить("Запрос.УстановитьПараметр(СтрокаПараметры.Параметр," + СтрокаПараметры.Значение + ");");
+ 	КонецЦикла;
+ 	УстановитьБезопасныйРежим(Ложь);
+ 	
+ 	Запрос.УстановитьПараметр("ЭтаПлощадка", ЭтотОбъект.Сценарий);
+ 	
+ 	Попытка
+ 		Результат = Запрос.Выполнить();
+ 	Исключение
+ 		ТекстОшибки = "Не удалось выполнить запрос. Проверьте правильность написания запроса и параметров.";
+ 	    Сообщить(ТекстОшибки);
+ 	КонецПопытки;
+ 		
+ 	РезультирующаяТаблица = Результат.Выгрузить();  
+ 	
+ 	Возврат РезультирующаяТаблица;
+ 	
+ КонецФункции
+ 
+ &НаСервере
+ Процедура ВыгрузитьВыбранноеНаСервере()
+ 	// Вставить содержимое обработчика.   
+ 	
+ 	ТабДанных=0;
+ 	
+ 	МассивАвто=Новый массив;       
+ 	
+ 	МассивОстатки=Новый Массив;
+ 		
+ 	Для каждого СтрокаОбмена из ТабВыгрузки цикл
+ 		
+ 		Если НЕ СтрокаОбмена.Грузим Тогда Продолжить; КонецЕсли;   
+ 		
+ 		Если ТабДанных=0 Тогда ТабДанных=ЗапросОбщийНаСервере(); КонецЕсли; 
+ 		
+ 		СамоАвто=Новый Соответствие;
+ 		
+ 		СамоАвто.Вставить("description_category_id",ЭтотОбъект.КатегорияТовара.КатегорияТовара);  
+ 				
// ... сокращено, всего строк: 424
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 2) Отчет ОтчетПоЗаказНарядам — модуль объекта

**Процедура:** `Глобальный контекст`

**Код До:**

```bsl
// фрагмент отсутствует
```

**Код После:**

```bsl
﻿
Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	ДокументРезультат.Очистить();
КонецПроцедуры
```

**Данный участок кода:**

```bsl
+ ﻿
+ Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
+ 	ДокументРезультат.Очистить();
+ КонецПроцедуры
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 3) Обработка СобственныйОбработчикККТ — модуль объекта

**Процедура:** `Глобальный контекст` — изменил: `Зиянгиров Артур`

**Код До:**

```bsl
Перем ОсновнаяОбработка Экспорт; // Объект обработки, из которой происходит печать

//ОПИСАНИЕ ФУНКЦИЙ И ДОРАБОТКИ ПРЕДСТАВЛЕНО ПО ССЫЛКЕ
//https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#для-программистов


//МАРКИРОВКА

//ДанныеМаркировки - Структура - Содержит 3 параметра ("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX")
//ТипИдентификатораТовара - это код обозначающий, какой именно штрихкод будет разобран
//Доступные значения: "КодТовараВФорматеEAN8", "КодТовараВФорматеEAN13", "КодТовараВФорматеITF14",
//"КодТовараВФорматеDataMatrixGS1","ИзделияИзНатуральногоМеха","КодТовараВФорматеЕГАИС2", "КодТовараВФорматеЕГАИС3"
//
//При этом заполнен должен быть только ТипИдентификатораТовара, остальные параметры будут заполнены уже вызванной функцией
//Значение base64 маркировки будет храниться в поле РеквизитКодаТовара
//
//Штрихкод - Строка - сама маркировка
//Важно, данный функционал необходимо использовать только со штрихкодами состоящими из чисел.
//
//ОсновнаяОбработка.СформироватьДвоичныеДанныеДляЧисла(ДанныеМаркировки, Штрихкод, ДопИнформация = Неопределено);
//
//Пример:
//
//ДанныеМаркировки = Новый Структура("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX");
//ДанныеМаркировки.ТипИдентификатораТовара = "КодТовараВФорматеEAN13";
//ОсновнаяОбработка.СформироватьДвоичныеДанныеДляЧисла(ДанныеМаркировки, "1234567890123");
//Сообщить(ДанныеМаркировки.РеквизитКодаТовара);

//Для более сложной маркировки
//Вот такая маркировка "(01)02900002404104(21)Yv=dAABS6WBefSlJWqZ4", записываюется в базе вот так
//ДанныеМаркировки = Новый Структура("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX");
//ДанныеМаркировки.ТипИдентификатораТовара = "КодТовараВФорматеDataMatrixGS1";
//ОсновнаяОбработка.СформироватьДвоичныеДанныеДляЧисла(ДанныеМаркировки, "02900002404104", "Yv=dAABS6WBefSlJWqZ4");
//Сообщить(ДанныеМаркировки.РеквизитКодаТовара);

//Выполняется перед получением товара для печати
//Позволяет переопределить, либо добавить собственные нетиповые документы, 
//которые будет поддерживать обработка
// 
//Возвращаемое значение:
//  Результат - Тип Массив, возвращает список структур см. ДанныеНетиповогоДокумента()
Функция МассивНетиповыхДокументов() Экспорт
	МассивНетиповыхДокументов = Новый Массив;
	Возврат МассивНетиповыхДокументов;
КонецФункции

//Выполняется до пробития чека по экварингу, 
//также в коде можно отказать от выполнения операции
Процедура ДоОперацииПоЭквайрингу(ИмяЭквайринга, ТипТранзакции, ВходныеПараметры, 
				ПараметрыОборудования, ПараметрыПодключения, ОтказатьсяОтОбработки) Экспорт 
		
КонецПроцедуры

//Выполняется после пробития чека по эквайрингу и печати слип чека
Процедура ПослеОперацииПоЭквайрингу(ИмяЭквайринга, ТипТранзакции, СлипЧек, СсылочныйНомер) Экспорт 
		
КонецПроцедуры
 
//Выполняется перед самой печатью чека
//Можно подменить выходной xml файл для печати
//Либо вообще отказаться от печати чека, либо запустить собственную процедуру печати
//Для этого необходимо установить параметр ОтказатьОтПечати = Истина;
// 
//Параметры:
//  пПараметры - Тип Структура, настройки Обработки, см. мПараметрыУстройства
//  пОбщиеПараметры - Тип Структура, Структура текущего чека, см. мОбщиеПараметры.
//  пПараметрыПодключения - Тип Структура, параметры подключения конкретного оборудования.
//  пДанныеЧека - Тип Строка, XML представление будущего чека.
// 	ОтказатьсяОтПечати - Тип Булево, указывает нужно ли выводить чек на печать, по умолчанию - Ложь
//  НомерЧека - Тип Число, номер напечатанного чека, до фискализации не заполнен, по умолчанию - 0
// 	НомерСмены - Тип Число, номер текущей смены, до фискализации не заполнен, по умолчанию - 0
// 	ФискальныйПризнак - Тип строка, фискальный признак напечатанного чека, до фискализации не заполнен, по умолчанию - ""
//
Процедура ДоФискализацииЧека(пПараметры, пОбщиеПараметры, пПараметрыПодключения, пДанныеЧека, ОтказатьсяОтПечати, НомерЧека, НомерСмены, ФискальныйПризнак) Экспорт
	//ПРИМЕР:
	//Вставка email клиента в будущий чек
	//Если Найти(пДанныеЧека, "CustomerEmail") = 0 Тогда
	//	пДанныеЧека = СтрЗаменить(пДанныеЧека, "<Parameters","<Parameters CustomerEmail=""test@gmail.com"""); 
	//КонецЕсли; 
	
	//Изменения xml данные чека
	//пОбщиеПараметры.Получатель = "Василий Иванович";
	//пДанныеЧека = ОсновнаяОбработка.ККТ_XMLПредставлениеЧека(пОбщиеПараметры, пПараметрыПодключения);

	// В clean-режиме не вмешиваемся в формирование payload чека.
	Попытка
		АгентШапка = "";
		Если пОбщиеПараметры.Свойство("ПризнакАгента") Тогда
			АгентШапка = Строка(пОбщиеПараметры.ПризнакАгента);
		КонецЕсли;
		
		АгентСтрока = "";
		Если пОбщиеПараметры.Свойство("ПозицииЧека")
			И пОбщиеПараметры.ПозицииЧека <> Неопределено
			И пОбщиеПараметры.ПозицииЧека.Количество() > 0 Тогда
			ПерваяПозиция = пОбщиеПараметры.ПозицииЧека[0];
			Если ПерваяПозиция.Свойство("ПризнакАгентаПоПредметуРасчета") Тогда
				АгентСтрока = Строка(ПерваяПозиция.ПризнакАгентаПоПредметуРасчета);
			КонецЕсли;
		КонецЕсли;
		
		ПоставщикИНН = "";
		ПоставщикТелефон = "";
		Если пОбщиеПараметры.Свойство("ДанныеПоставщика")
			И пОбщиеПараметры.ДанныеПоставщика <> Неопределено Тогда
			Если пОбщиеПараметры.ДанныеПоставщика.Свойство("ИНН") Тогда
				ПоставщикИНН = пОбщиеПараметры.ДанныеПоставщика.ИНН;
			КонецЕсли;
			Если пОбщиеПараметры.ДанныеПоставщика.Свойство("Телефон") Тогда
				ПоставщикТелефон = пОбщиеПараметры.ДанныеПоставщика.Телефон;
			КонецЕсли;
		КонецЕсли;
		
		// Минимальная синхронизация под XML-сериализатор:
		// если в шапке есть агент, но в строках признак/данные поставщика не заполнены,
		// дублируем их в ПозицииЧека и пересобираем XML.
		НужноПересобратьXML = Ложь;
		Если ЗначениеЗаполнено(АгентШапка)
			И пОбщиеПараметры.Свойство("ПозицииЧека")
			И пОбщиеПараметры.ПозицииЧека <> Неопределено Тогда
// ... сокращено, всего строк: 696
```

**Код После:**

```bsl
// фрагмент отсутствует
```

**Данный участок кода:**

```bsl
- Перем ОсновнаяОбработка Экспорт; // Объект обработки, из которой происходит печать
- 
- //ОПИСАНИЕ ФУНКЦИЙ И ДОРАБОТКИ ПРЕДСТАВЛЕНО ПО ССЫЛКЕ
- //https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#для-программистов
- 
- 
- //МАРКИРОВКА
- 
- //ДанныеМаркировки - Структура - Содержит 3 параметра ("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX")
- //ТипИдентификатораТовара - это код обозначающий, какой именно штрихкод будет разобран
- //Доступные значения: "КодТовараВФорматеEAN8", "КодТовараВФорматеEAN13", "КодТовараВФорматеITF14",
- //"КодТовараВФорматеDataMatrixGS1","ИзделияИзНатуральногоМеха","КодТовараВФорматеЕГАИС2", "КодТовараВФорматеЕГАИС3"
- //
- //При этом заполнен должен быть только ТипИдентификатораТовара, остальные параметры будут заполнены уже вызванной функцией
- //Значение base64 маркировки будет храниться в поле РеквизитКодаТовара
- //
- //Штрихкод - Строка - сама маркировка
- //Важно, данный функционал необходимо использовать только со штрихкодами состоящими из чисел.
- //
- //ОсновнаяОбработка.СформироватьДвоичныеДанныеДляЧисла(ДанныеМаркировки, Штрихкод, ДопИнформация = Неопределено);
- //
- //Пример:
- //
- //ДанныеМаркировки = Новый Структура("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX");
- //ДанныеМаркировки.ТипИдентификатораТовара = "КодТовараВФорматеEAN13";
- //ОсновнаяОбработка.СформироватьДвоичныеДанныеДляЧисла(ДанныеМаркировки, "1234567890123");
- //Сообщить(ДанныеМаркировки.РеквизитКодаТовара);
- 
- //Для более сложной маркировки
- //Вот такая маркировка "(01)02900002404104(21)Yv=dAABS6WBefSlJWqZ4", записываюется в базе вот так
- //ДанныеМаркировки = Новый Структура("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX");
- //ДанныеМаркировки.ТипИдентификатораТовара = "КодТовараВФорматеDataMatrixGS1";
- //ОсновнаяОбработка.СформироватьДвоичныеДанныеДляЧисла(ДанныеМаркировки, "02900002404104", "Yv=dAABS6WBefSlJWqZ4");
- //Сообщить(ДанныеМаркировки.РеквизитКодаТовара);
- 
- //Выполняется перед получением товара для печати
- //Позволяет переопределить, либо добавить собственные нетиповые документы, 
- //которые будет поддерживать обработка
- // 
- //Возвращаемое значение:
- //  Результат - Тип Массив, возвращает список структур см. ДанныеНетиповогоДокумента()
- Функция МассивНетиповыхДокументов() Экспорт
- 	МассивНетиповыхДокументов = Новый Массив;
- 	Возврат МассивНетиповыхДокументов;
- КонецФункции
- 
- //Выполняется до пробития чека по экварингу, 
- //также в коде можно отказать от выполнения операции
- Процедура ДоОперацииПоЭквайрингу(ИмяЭквайринга, ТипТранзакции, ВходныеПараметры, 
- 				ПараметрыОборудования, ПараметрыПодключения, ОтказатьсяОтОбработки) Экспорт 
- 		
- КонецПроцедуры
- 
- //Выполняется после пробития чека по эквайрингу и печати слип чека
- Процедура ПослеОперацииПоЭквайрингу(ИмяЭквайринга, ТипТранзакции, СлипЧек, СсылочныйНомер) Экспорт 
- 		
- КонецПроцедуры
-  
- //Выполняется перед самой печатью чека
- //Можно подменить выходной xml файл для печати
- //Либо вообще отказаться от печати чека, либо запустить собственную процедуру печати
- //Для этого необходимо установить параметр ОтказатьОтПечати = Истина;
- // 
- //Параметры:
- //  пПараметры - Тип Структура, настройки Обработки, см. мПараметрыУстройства
- //  пОбщиеПараметры - Тип Структура, Структура текущего чека, см. мОбщиеПараметры.
- //  пПараметрыПодключения - Тип Структура, параметры подключения конкретного оборудования.
- //  пДанныеЧека - Тип Строка, XML представление будущего чека.
- // 	ОтказатьсяОтПечати - Тип Булево, указывает нужно ли выводить чек на печать, по умолчанию - Ложь
- //  НомерЧека - Тип Число, номер напечатанного чека, до фискализации не заполнен, по умолчанию - 0
- // 	НомерСмены - Тип Число, номер текущей смены, до фискализации не заполнен, по умолчанию - 0
- // 	ФискальныйПризнак - Тип строка, фискальный признак напечатанного чека, до фискализации не заполнен, по умолчанию - ""
- //
- Процедура ДоФискализацииЧека(пПараметры, пОбщиеПараметры, пПараметрыПодключения, пДанныеЧека, ОтказатьсяОтПечати, НомерЧека, НомерСмены, ФискальныйПризнак) Экспорт
- 	//ПРИМЕР:
- 	//Вставка email клиента в будущий чек
- 	//Если Найти(пДанныеЧека, "CustomerEmail") = 0 Тогда
- 	//	пДанныеЧека = СтрЗаменить(пДанныеЧека, "<Parameters","<Parameters CustomerEmail=""test@gmail.com"""); 
- 	//КонецЕсли; 
- 	
// ... сокращено, всего строк: 696
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 4) Общая форма ОтправкаВЧЗ — модуль формы

**Процедура:** `ОтправитьВЧЗ` — изменил: `Фомин Сергей`

**Код До:**

```bsl
			ЭтотОбъект,
			ОбъектДок,
			ДополнительныеПараметры); 
	
	ИначеЕсли Параметры.ИмяДействия = "ОбновитьСтатусДокумента" Тогда
		
		МаркировкаТоваровКлиент.ОбновитьСтатусДокумента(ЭтотОбъект, Параметры.Ссылка.ПолучитьОбъект());  	
		
	ИначеЕсли Параметры.ИмяДействия = "ОтправкаЗапросаНаПолучениеТекущихСтатусовМаркировки" Тогда	
		
		ОбъектДок = Параметры.Ссылка.ПолучитьОбъект();	 
		ТЗ = Параметры.Ссылка.КодыМаркировки.Выгрузить();			
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Действие", Параметры.ИмяДействия);
		ДополнительныеПараметры.Вставить("КодыМаркировки", ЗаполнитьКодыМаркировкиДляПроверки(ТЗ));
		ДополнительныеПараметры.Вставить("Организация", Параметры.Ссылка.Организация);
		ДополнительныеПараметры.Вставить("ПодразделениеКомпании", Параметры.Ссылка.ПодразделениеКомпании);
		ДополнительныеПараметры.Вставить("НеТребуетсяКлючСессии", Ложь);
		ДополнительныеПараметры.Вставить("РазрешенныеСостояния", РазрешенныеСостояния());
		ОбработчикОповещения = Новый ОписаниеОповещения("ПолучитьЗапросЗавершение", ЭтотОбъект);
		ДополнительныеПараметры.Вставить("ОбработкаРезультата", ОбработчикОповещения);
```

**Код После:**

```bsl
			ЭтотОбъект,
			ОбъектДок,
			ДополнительныеПараметры); 
	
	ИначеЕсли Параметры.ИмяДействия = "ОбновитьСтатусДокумента" Тогда
		
		МаркировкаТоваровКлиент.ОбновитьСтатусДокумента(ЭтотОбъект, Параметры.Ссылка.ПолучитьОбъект());  	
		
	ИначеЕсли Параметры.ИмяДействия = "ОтправкаЗапросаНаПолучениеТекущихСтатусовМаркировки" Тогда	
		
		//+Фомин Сергей 20.04.2026 маркировка
		ОбъектДок = Параметры.Ссылка.ПолучитьОбъект();	
		СтруктураОтбора = Новый Структура;
		СтруктураОтбора.Вставить("ИдентификаторТовара",Параметры.ИдентификаторСтроки);
		ТЗ = Параметры.Ссылка.КодыМаркировки.Выгрузить(СтруктураОтбора);	
		//-Фомин Сергей 20.04.2026 маркировка
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Действие", Параметры.ИмяДействия);
		ДополнительныеПараметры.Вставить("КодыМаркировки", ЗаполнитьКодыМаркировкиДляПроверки(ТЗ));
		ДополнительныеПараметры.Вставить("Организация", Параметры.Ссылка.Организация);
		ДополнительныеПараметры.Вставить("ПодразделениеКомпании", Параметры.Ссылка.ПодразделениеКомпании);
		ДополнительныеПараметры.Вставить("НеТребуетсяКлючСессии", Ложь);
		ДополнительныеПараметры.Вставить("РазрешенныеСостояния", РазрешенныеСостояния());
		ОбработчикОповещения = Новый ОписаниеОповещения("ПолучитьЗапросЗавершение", ЭтотОбъект);
		ДополнительныеПараметры.Вставить("ОбработкаРезультата", ОбработчикОповещения);
```

**Данный участок кода:**

```bsl
- 		ОбъектДок = Параметры.Ссылка.ПолучитьОбъект();	 
- 		ТЗ = Параметры.Ссылка.КодыМаркировки.Выгрузить();			
+ 		//+Фомин Сергей 20.04.2026 маркировка
+ 		ОбъектДок = Параметры.Ссылка.ПолучитьОбъект();	
+ 		СтруктураОтбора = Новый Структура;
+ 		СтруктураОтбора.Вставить("ИдентификаторТовара",Параметры.ИдентификаторСтроки);
+ 		ТЗ = Параметры.Ссылка.КодыМаркировки.Выгрузить(СтруктураОтбора);	
+ 		//-Фомин Сергей 20.04.2026 маркировка
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 5) Общий модуль Odin_Общий — модуль

**Процедура:** `DSF_ЗаполнитьСтруктуруПоРегистрацииКонтакта` — изменил: `Бардашов Н.А.`

**Код До:**

```bsl
		КлиентОтчество = ?(КлиентСсылка, ЗаписьСобытия.Клиент.Отчество,	СокрЛП(ЗаписьСобытия.Отчество));
		ЧастноеЛицо	   = НЕ ЗаписьСобытия.ЮридическоеЛицо;
		Телефон		   = ЗаписьСобытия.ПредставлениеТелефона;

		Если ЧастноеЛицо И КлиентИмя = "" Тогда // odin+ (14.04.22) Пользователи имя пишут в поле фамилия
			КлиентИмя = ?(КлиентФамилия = "", КлиентОтчество, КлиентФамилия); 
		КонецЕсли;
		
		СтруктураДанных_Клиент = Новый Структура;
		СтруктураДанных_Клиент.Вставить("code",							ТекЗапись.Код);
		СтруктураДанных_Клиент.Вставить("type",							?(НЕ ЧастноеЛицо, 1, 0));
		СтруктураДанных_Клиент.Вставить("name",							?(НЕ ЧастноеЛицо, "", КлиентИмя));
				
		// Каргина+ (17.02.2025) №213 - при POST запросе передается phone
		// при PUT - phones (массив)
		ЭтоНовыйКлиент = (ТекЗапись.IDКлиентаDNM = "");
		
		Если ЕстьТестДрайв Тогда
			СтруктураДанных_Клиент.Вставить("last_name",		?(НЕ ЧастноеЛицо, "", КлиентФамилия));  
			СтруктураДанных_Клиент.Вставить("middle_name",		?(НЕ ЧастноеЛицо, "", КлиентОтчество));
```

**Код После:**

```bsl
		КлиентОтчество = ?(КлиентСсылка, ЗаписьСобытия.Клиент.Отчество,	СокрЛП(ЗаписьСобытия.Отчество));
		ЧастноеЛицо	   = НЕ ЗаписьСобытия.ЮридическоеЛицо;
		Телефон		   = ЗаписьСобытия.ПредставлениеТелефона;

		Если ЧастноеЛицо И КлиентИмя = "" Тогда // odin+ (14.04.22) Пользователи имя пишут в поле фамилия
			КлиентИмя = ?(КлиентФамилия = "", КлиентОтчество, КлиентФамилия); 
		КонецЕсли;
		
		СтруктураДанных_Клиент = Новый Структура;
		СтруктураДанных_Клиент.Вставить("code",							ТекЗапись.Код);
		СтруктураДанных_Клиент.Вставить("type",							?(НЕ ЧастноеЛицо, 1, 0));  
		//+Бардашов Н.А. 20.04.2026 №15655
		Если НЕ ЧастноеЛицо Тогда
			СтруктураДанных_Клиент.Вставить("company_legal_form",		ЗаписьСобытия.ФормаСобственности);
		КонецЕсли;
		//-Бардашов Н.А. 20.04.2026 №15655
		СтруктураДанных_Клиент.Вставить("name",							?(НЕ ЧастноеЛицо, "", КлиентИмя));
				
		// Каргина+ (17.02.2025) №213 - при POST запросе передается phone
		// при PUT - phones (массив)
		ЭтоНовыйКлиент = (ТекЗапись.IDКлиентаDNM = "");
		
		Если ЕстьТестДрайв Тогда
			СтруктураДанных_Клиент.Вставить("last_name",		?(НЕ ЧастноеЛицо, "", КлиентФамилия));  
			СтруктураДанных_Клиент.Вставить("middle_name",		?(НЕ ЧастноеЛицо, "", КлиентОтчество));
```

**Данный участок кода:**

```bsl
- 		СтруктураДанных_Клиент.Вставить("type",							?(НЕ ЧастноеЛицо, 1, 0));
+ 		СтруктураДанных_Клиент.Вставить("type",							?(НЕ ЧастноеЛицо, 1, 0));  
+ 		//+Бардашов Н.А. 20.04.2026 №15655
+ 		Если НЕ ЧастноеЛицо Тогда
+ 			СтруктураДанных_Клиент.Вставить("company_legal_form",		ЗаписьСобытия.ФормаСобственности);
+ 		КонецЕсли;
+ 		//-Бардашов Н.А. 20.04.2026 №15655
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `СоздатьОбновитьДокументСобытие`

**Код До:**

```bsl
			Описание 		 = "Необходимо связаться с клиентом " + ДокументСсылка.Контрагент + " для получения второй рекомендации. Клиент приобрел(а) автомобиль " + ДокументСсылка.Автомобиль + ".";
			ДатаСобытия 	 = ДатаОснования + 950400; // Событие и задачу по второй рекомендации создаем через 11 дней
			ЗначениеСвойства = "Рекомендация 2";
			Содержание		 = "Процесс прошел этап ""Рекомендация 1""";
			
		ИначеЕсли Свойство = Неопределено И ЭтоРабочийЛистОП Тогда // Добавим ветку для Рабочего листа ОП
			Описание		 = "Связаться с клиентом по входящему трафику";	
			ДатаСобытия      = СтруктураДанных.ДатаПришел;
			ДатаОкончания    = СтруктураДанных.ДатаУшел;
			ЗначениеСвойства = "Входящий трафик";   
			Содержание		 = "Зарегистрирован входящий трафик";
		
		ИначеЕсли Свойство = "Входящий трафик" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Входящий трафик";
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
		//+Бардашов Н.А. 16.03.2026 №12516	
		ИначеЕсли Свойство = "Тест драйв" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Тест драйв";
			ДатаСобытия      = СтруктураДанных.Дата;
```

**Код После:**

```bsl
			Описание 		 = "Необходимо связаться с клиентом " + ДокументСсылка.Контрагент + " для получения второй рекомендации. Клиент приобрел(а) автомобиль " + ДокументСсылка.Автомобиль + ".";
			ДатаСобытия 	 = ДатаОснования + 950400; // Событие и задачу по второй рекомендации создаем через 11 дней
			ЗначениеСвойства = "Рекомендация 2";
			Содержание		 = "Процесс прошел этап ""Рекомендация 1""";
			
		ИначеЕсли Свойство = Неопределено И ЭтоРабочийЛистОП Тогда // Добавим ветку для Рабочего листа ОП
			Описание		 = "Связаться с клиентом по входящему трафику";	
			ДатаСобытия      = СтруктураДанных.ДатаПришел;
			ДатаОкончания    = СтруктураДанных.ДатаУшел;
			ЗначениеСвойства = "Входящий трафик";   
			//+Зиянгиров (18.04.2026) #btrx 15907
			ПримечаниеКарточки = "";
			АвторКарточки      = "";
			Если СтруктураДанных <> Неопределено Тогда
			    СтруктураДанных.Свойство("Примечание",    ПримечаниеКарточки);
			    СтруктураДанных.Свойство("АвторКарточки", АвторКарточки);
			КонецЕсли;

			Если ЗначениеЗаполнено(ПримечаниеКарточки) Тогда
			    АвторСтрока = ?(ЗначениеЗаполнено(АвторКарточки), Строка(АвторКарточки) + ": ", "");
			    Содержание = "Зарегистрирован входящий трафик" + Символы.ПС
			               + АвторСтрока + ПримечаниеКарточки;
			Иначе
			    Содержание = "Зарегистрирован входящий трафик";
			КонецЕсли;
			//-Зиянгиров 		
		ИначеЕсли Свойство = "Входящий трафик" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Входящий трафик";
			ДатаСобытия      = СтруктураДанных.Дата;
			Содержание       = СтруктураДанных.Результат + Символы.ПС + СтруктураДанных.Содержание;
		//+Бардашов Н.А. 16.03.2026 №12516	
		ИначеЕсли Свойство = "Тест драйв" Тогда
			Описание 		 = "Перенесенный контакт с клиентом по входящему трафику";
			ЗначениеСвойства = "Тест драйв";
			ДатаСобытия      = СтруктураДанных.Дата;
```

**Данный участок кода:**

```bsl
- 			Содержание		 = "Зарегистрирован входящий трафик";
- 		
+ 			//+Зиянгиров (18.04.2026) #btrx 15907
+ 			ПримечаниеКарточки = "";
+ 			АвторКарточки      = "";
+ 			Если СтруктураДанных <> Неопределено Тогда
+ 			    СтруктураДанных.Свойство("Примечание",    ПримечаниеКарточки);
+ 			    СтруктураДанных.Свойство("АвторКарточки", АвторКарточки);
+ 			КонецЕсли;
+ 
+ 			Если ЗначениеЗаполнено(ПримечаниеКарточки) Тогда
+ 			    АвторСтрока = ?(ЗначениеЗаполнено(АвторКарточки), Строка(АвторКарточки) + ": ", "");
+ 			    Содержание = "Зарегистрирован входящий трафик" + Символы.ПС
+ 			               + АвторСтрока + ПримечаниеКарточки;
+ 			Иначе
+ 			    Содержание = "Зарегистрирован входящий трафик";
+ 			КонецЕсли;
+ 			//-Зиянгиров
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `СоздатьОбновитьДокументСобытие`

**Код До:**

```bsl
		#Если Клиент Тогда
			глПрава = обПолучитьПраваИНастройкиПользователя(ПараметрыСеанса.Пользователь);
		#КонецЕсли
		// Каргина- (02.06.2024)
		
		// Запишем свойство Этапы CRM для определения данного этапа
		
		ОбновитьСвойствоДокументаСобытие(НовыйДокументСобытие.Ссылка, ЗначениеСвойства);
		
		Если ЭтоРеализация И НЕ СледущийЭтапЗапущен Тогда
			СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Обновить", Описание); // Если перепроведение реализации и следующий этап не запущен, то обновим данные в задаче
		ИначеЕсли ЭтоРабочийЛистОП Тогда
			
			Если Обращение Тогда
				// Вафина+ (28.02.24) добавлено условие
				// Вафина+ (07.06.24) скорректировала условие, т.к. сейчас параметр Обращение - Булево
				СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Входящий трафик", Описание, ,Описание, Обращение);
			Иначе
				СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Входящий трафик", Описание, ,Описание);
			КонецЕсли;
```

**Код После:**

```bsl
		#Если Клиент Тогда
			глПрава = обПолучитьПраваИНастройкиПользователя(ПараметрыСеанса.Пользователь);
		#КонецЕсли
		// Каргина- (02.06.2024)
		
		// Запишем свойство Этапы CRM для определения данного этапа
		
		ОбновитьСвойствоДокументаСобытие(НовыйДокументСобытие.Ссылка, ЗначениеСвойства);
		
		Если ЭтоРеализация И НЕ СледущийЭтапЗапущен Тогда
			СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Обновить", Описание,,ЗначениеСвойства); // Если перепроведение реализации и следующий этап не запущен, то обновим данные в задаче //+Бардашов Н.А. №13351 Добавлен параметр
		ИначеЕсли ЭтоРабочийЛистОП Тогда
			
			Если Обращение Тогда
				// Вафина+ (28.02.24) добавлено условие
				// Вафина+ (07.06.24) скорректировала условие, т.к. сейчас параметр Обращение - Булево
				СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Входящий трафик", Описание, ,Описание, Обращение);
			Иначе
				СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Входящий трафик", Описание, ,Описание);
			КонецЕсли;
```

**Данный участок кода:**

```bsl
- 			СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Обновить", Описание); // Если перепроведение реализации и следующий этап не запущен, то обновим данные в задаче
+ 			СоздатьЗакрытьЗадачуПроцессаCRM(НовыйДокументСобытие.Ссылка, "Обновить", Описание,,ЗначениеСвойства); // Если перепроведение реализации и следующий этап не запущен, то обновим данные в задаче //+Бардашов Н.А. №13351 Добавлен параметр
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `СоздатьЗакрытьЗадачуПроцессаCRM` — изменил: `Бардашов Н.А.`

**Код До:**

```bsl
	//+Бардашов Н.А. 02.04.2026 №14004 
	ПодразделенияИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьНастройки("ПодразделениеНеСоздаватьЗадачи"); 
	СоздаватьНаПользователя = обПраво(ПланыВидовХарактеристик.ПраваИНастройки.СоздаватьЗадачиПоПодразделению,,,,Пользователь); //+Бардашов Н.А. 13.04.2026 
	Если ПодразделенияИсключения.Количество() <> 0 И Не СоздаватьНаПользователя Тогда 
		ПодразделениеДокумента = ДокументСсылка.ПодразделениеКомпании;
		ЗначениеНастройки = ПодразделенияИсключения.Найти(ПодразделениеДокумента,"Параметр");
		Если ЗначениеНастройки <> Неопределено И (Этап = "Звонок заботы" 
			Или Этап = "Рекомендация 1"
			Или Этап = "Рекомендация 2"
			Или Этап = "Дружеские отношения" 
			Или Этап = "Входящий трафик") Тогда //+Бардашов Н.А. 26.03.2026 №13907
			Возврат;
		КонецЕсли;                                           
	КонецЕсли;	
	//-Бардашов Н.А. 02.04.2026 №14004
    ~ПередадимДиректору: 
	
	// Дьяконов+ (06.09.22) ORG-136 Создадим задачу менеджеру или завершим ее в зависимости от доп параметра
	Если ДопПараметр = "Создать" Тогда	
		НоваяЗадача = РегистрыСведений.НеобходимАкцептПоОбъектам.СоздатьМенеджерЗаписи();
```

**Код После:**

```bsl
	//+Бардашов Н.А. 02.04.2026 №14004 
	ПодразделенияИсключения = РегистрыСведений.АвтосетьНастройки.ПолучитьНастройки("ПодразделениеНеСоздаватьЗадачи"); 
	СоздаватьНаПользователя = обПраво(ПланыВидовХарактеристик.ПраваИНастройки.СоздаватьЗадачиПоПодразделению,,,,Пользователь); //+Бардашов Н.А. 13.04.2026 
	Если ПодразделенияИсключения.Количество() <> 0 И Не СоздаватьНаПользователя Тогда 
		ПодразделениеДокумента = ДокументСсылка.ПодразделениеКомпании;
		ЗначениеНастройки = ПодразделенияИсключения.Найти(ПодразделениеДокумента,"Параметр");
		Если ЗначениеНастройки <> Неопределено И (Этап = "Звонок заботы" 
			Или Этап = "Рекомендация 1"
			Или Этап = "Рекомендация 2"
			Или Этап = "Дружеские отношения" 
			Или Этап = "Входящий трафик"  //+Бардашов Н.А. 26.03.2026 №13907
			ИЛИ ДопПараметр = "Входящий трафик") Тогда //+Бардашов Н.А. 
			Возврат;
		КонецЕсли;                                           
	КонецЕсли;	
	//-Бардашов Н.А. 02.04.2026 №14004
    ~ПередадимДиректору: 
	
	// Дьяконов+ (06.09.22) ORG-136 Создадим задачу менеджеру или завершим ее в зависимости от доп параметра
	Если ДопПараметр = "Создать" Тогда	
		НоваяЗадача = РегистрыСведений.НеобходимАкцептПоОбъектам.СоздатьМенеджерЗаписи();
```

**Данный участок кода:**

```bsl
- 			Или Этап = "Входящий трафик") Тогда //+Бардашов Н.А. 26.03.2026 №13907
+ 			Или Этап = "Входящий трафик"  //+Бардашов Н.А. 26.03.2026 №13907
+ 			ИЛИ ДопПараметр = "Входящий трафик") Тогда //+Бардашов Н.А.
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `СоздатьОбновитьРабочийЛистОП`

**Код До:**

```bsl
		НовыйРабочийЛистОП.ВидКонтакта 				= СтрокаРегистра.ВидКонтакта;
		НовыйРабочийЛистОП.Пол 						= Перечисления.ПолФизическихЛиц.НеУказан;
		НовыйРабочийЛистОП.ПрограммныйЗапрос 		= Истина;
		НовыйРабочийЛистОП.КонтактноеЛицо 			= СтрокаРегистра.Клиент;
				
		СтруктураДанных = Новый Структура;
		СтруктураДанных.Вставить("ДатаПришел",  	Дата(ДатаПрихода + ВремяПрихода));
		СтруктураДанных.Вставить("ДатаУшел",    	Дата(ДатаПрихода + ВремяУхода));
		СтруктураДанных.Вставить("ВидКонтакта", 	СтрокаРегистра.ВидКонтакта);
		СтруктураДанных.Вставить("Контрагент",  	СтрокаРегистра.Клиент);
		СтруктураДанных.Вставить("Идентификация",   СтрокаРегистра.Идентификация);
		
		НачатьТранзакцию();
		// Каргина+ (04.06.2024) - все процедуры по созданию РЛ и событий в одной попытке и транзакции
		Попытка
			НовыйРабочийЛистОП.ПометкаУдаления = Ложь;
			НовыйРабочийЛистОП.Записать(РежимЗаписиДокумента.Проведение);
			//+Бардашов Н.А. 23.09.2025 №6104 Добавил проверку на Тест-драйв
			Если НЕ СтрокаРегистра.фТестДрайв Тогда 
				//Вафина+ (28.02.24) добавлено условие
				Если Обращение Тогда
```

**Код После:**

```bsl
		НовыйРабочийЛистОП.ВидКонтакта 				= СтрокаРегистра.ВидКонтакта;
		НовыйРабочийЛистОП.Пол 						= Перечисления.ПолФизическихЛиц.НеУказан;
		НовыйРабочийЛистОП.ПрограммныйЗапрос 		= Истина;
		НовыйРабочийЛистОП.КонтактноеЛицо 			= СтрокаРегистра.Клиент;
				
		СтруктураДанных = Новый Структура;
		СтруктураДанных.Вставить("ДатаПришел",  	Дата(ДатаПрихода + ВремяПрихода));
		СтруктураДанных.Вставить("ДатаУшел",    	Дата(ДатаПрихода + ВремяУхода));
		СтруктураДанных.Вставить("ВидКонтакта", 	СтрокаРегистра.ВидКонтакта);
		СтруктураДанных.Вставить("Контрагент",  	СтрокаРегистра.Клиент);
		СтруктураДанных.Вставить("Идентификация", СтрокаРегистра.Идентификация);
		//+Зиянгиров (18.04.2026) #btrx 15907
		СтруктураДанных.Вставить("Примечание",    СтрокаРегистра.Примечание);
		СтруктураДанных.Вставить("АвторКарточки", СтрокаРегистра.Автор.Сотрудник);
		//-Зиянгиров
		
		НачатьТранзакцию();
		// Каргина+ (04.06.2024) - все процедуры по созданию РЛ и событий в одной попытке и транзакции
		Попытка
			НовыйРабочийЛистОП.ПометкаУдаления = Ложь;
			НовыйРабочийЛистОП.Записать(РежимЗаписиДокумента.Проведение);
			//+Бардашов Н.А. 23.09.2025 №6104 Добавил проверку на Тест-драйв
			Если НЕ СтрокаРегистра.фТестДрайв Тогда 
				//Вафина+ (28.02.24) добавлено условие
				Если Обращение Тогда
```

**Данный участок кода:**

```bsl
- 		СтруктураДанных.Вставить("Идентификация",   СтрокаРегистра.Идентификация);
+ 		СтруктураДанных.Вставить("Идентификация", СтрокаРегистра.Идентификация);
+ 		//+Зиянгиров (18.04.2026) #btrx 15907
+ 		СтруктураДанных.Вставить("Примечание",    СтрокаРегистра.Примечание);
+ 		СтруктураДанных.Вставить("АвторКарточки", СтрокаРегистра.Автор.Сотрудник);
+ 		//-Зиянгиров
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 6) Общий модуль Шина1С — модуль

**Процедура:** `Глобальный контекст`

**Код До:**

```bsl
//Назначение области - отправка данных на сервер srvsql01
//Не имеет самостоятельного смысла без сервиса интеграции ШинаЗакамьеОсновнойПередатчикНаСервер
//Пока у финансового депортамента и BI-цев нет чёткого видения. На совещании были рассуждения в роде "Может надо, а может не надо? А что, пусть будет." 
//Поэтому приходится изменения делать мелкими шажками.
//Также стараюсь переносить изменения в рабочую базу, чтобы в sql не соседствовали актуальные данные с неактуальными.


//Муртазин И. Ф. 11-03-2026 - Добавил выгрузку реализаций в собственную таблицу на SQL dbo.Realizationz
//Муртазин И. Ф. 12-03-2026 - Добавил выгрузку доходов и расходов в dbo.fact_income_expense
//Муртазин И. Ф. 13-03-2026 - Добавил выгрузку доходов и расходов в dbo.dim_documents
Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину() Экспорт

	//1 - это srvsql01
	перИндексПриёмщика = 1;	
	
	 ТабЗначПодразделения = ЗапроситьПодразделенияДляОсновнойБазы();
	 
	 ОтправитьТаблицуНаСервер(ТабЗначПодразделения, перИндексПриёмщика, 1);
```

**Код После:**

```bsl
//Назначение области - отправка данных на сервер srvsql01
//Не имеет самостоятельного смысла без сервиса интеграции ШинаЗакамьеОсновнойПередатчикНаСервер
//Пока у финансового депортамента и BI-цев нет чёткого видения. На совещании были рассуждения в роде "Может надо, а может не надо? А что, пусть будет." 
//Поэтому приходится изменения делать мелкими шажками.
//Также стараюсь переносить изменения в рабочую базу, чтобы в sql не соседствовали актуальные данные с неактуальными.


//Муртазин И. Ф. 11-03-2026 - Добавил выгрузку реализаций в собственную таблицу на SQL dbo.Realizationz
//Муртазин И. Ф. 12-03-2026 - Добавил выгрузку доходов и расходов в dbo.fact_income_expense
//Муртазин И. Ф. 13-03-2026 - Добавил выгрузку доходов и расходов в dbo.dim_documents
//Муртазин И. Ф. 20-04-2026 - Добавил выгрузку контрагентов
Функция СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину() Экспорт

	//1 - это srvsql01
	перИндексПриёмщика = 1;	
	
	 ТабЗначПодразделения = ЗапроситьПодразделенияДляОсновнойБазы();
	 
	 ОтправитьТаблицуНаСервер(ТабЗначПодразделения, перИндексПриёмщика, 1);
```

**Данный участок кода:**

```bsl
+ //Муртазин И. Ф. 20-04-2026 - Добавил выгрузку контрагентов
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину`

**Код До:**

```bsl
	 
	 ТабЗначРегистраторыДоходовИРасходов = ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРегистраторыДоходовИРасходов, перИндексПриёмщика, 6);

	 
	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьСтрокиТаблицРегистраторовЗаПериод(датаНачало, датаКонец); 
	 
	 ОтправитьТаблицуНаСервер(ТабЗначСтрокиТаблицРегистраторовЗаПериод, перИндексПриёмщика, 7);
	 
	 
КонецФункции // СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину()


//Муртазин И. Ф. 11-03-2026 Изменений нет
Функция ОтправитьТаблицуНаСервер(аргТабЗначДанные, аргПерИндексПриёмщика, аргПерИндексТипаТаблицы) Экспорт
	
	 стрСобранныеJson = СериализоватьВJson(аргТабЗначДанные);
	 
	 СоотвПараметрыСообщения = Новый Соответствие;
```

**Код После:**

```bsl
	 
	 ТабЗначРегистраторыДоходовИРасходов = ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод(датаНачало, датаКонец);	
	 
	 ОтправитьТаблицуНаСервер(ТабЗначРегистраторыДоходовИРасходов, перИндексПриёмщика, 6);

	 
	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьСтрокиТаблицРегистраторовЗаПериод(датаНачало, датаКонец); 
	 
	 ОтправитьТаблицуНаСервер(ТабЗначСтрокиТаблицРегистраторовЗаПериод, перИндексПриёмщика, 7);
	 
	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьКонтрагентов(); 
	 
	 ОтправитьТаблицуНаСервер(ТабЗначСтрокиТаблицРегистраторовЗаПериод, перИндексПриёмщика, 8);
	 
	 
КонецФункции // СобратьИОтправитьСообщенияНаСерверДанныхЧерезШину()


//Муртазин И. Ф. 11-03-2026 Изменений нет
Функция ОтправитьТаблицуНаСервер(аргТабЗначДанные, аргПерИндексПриёмщика, аргПерИндексТипаТаблицы) Экспорт
	
	 стрСобранныеJson = СериализоватьВJson(аргТабЗначДанные);
	 
	 СоотвПараметрыСообщения = Новый Соответствие;
```

**Данный участок кода:**

```bsl
+ 	 ТабЗначСтрокиТаблицРегистраторовЗаПериод = ЗапроситьКонтрагентов(); 
+ 	 
+ 	 ОтправитьТаблицуНаСервер(ТабЗначСтрокиТаблицРегистраторовЗаПериод, перИндексПриёмщика, 8);
+
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ЗапроситьВсеРегистраторыДоходовИРасходовЗаПериод`

**Код До:**

```bsl
		|ВЫБРАТЬ
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Ссылка),
		|	РеализацияАвтомобилей.ХозОперация.Наименование,
		|	РеализацияАвтомобилей.Номер,
		|	РеализацияАвтомобилей.Дата,
		|	РеализацияАвтомобилей.Проведен,
		|	РеализацияАвтомобилей.ПометкаУдаления,
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Организация),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ПодразделениеКомпании),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Автор),
		|	NULL,
		|	NULL,
		|	NULL,
		|	0,
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ХозОперация),
		|	РеализацияАвтомобилей.Комментарий,
		|	РеализацияАвтомобилей.ДатаСоздания
		|ИЗ
		|	Документ.РеализацияАвтомобилей КАК РеализацияАвтомобилей
		|ГДЕ
		|	РеализацияАвтомобилей.Дата МЕЖДУ &датаНачало И &датаКонец
		|
		|ОБЪЕДИНИТЬ
		|
```

**Код После:**

```bsl
		|ВЫБРАТЬ
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Ссылка),
		|	РеализацияАвтомобилей.ХозОперация.Наименование,
		|	РеализацияАвтомобилей.Номер,
		|	РеализацияАвтомобилей.Дата,
		|	РеализацияАвтомобилей.Проведен,
		|	РеализацияАвтомобилей.ПометкаУдаления,
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Организация),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ПодразделениеКомпании),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Автор),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Контрагент),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ДоговорВзаиморасчетов),
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ВалютаДокумента),
		|	РеализацияАвтомобилей.СуммаДокумента,
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ХозОперация),
		|	РеализацияАвтомобилей.Комментарий,
		|	РеализацияАвтомобилей.ДатаСоздания
		|ИЗ
		|	Документ.РеализацияАвтомобилей КАК РеализацияАвтомобилей
		|ГДЕ
		|	РеализацияАвтомобилей.Дата МЕЖДУ &датаНачало И &датаКонец
		|
		|ОБЪЕДИНИТЬ
		|
```

**Данный участок кода:**

```bsl
- 		|	NULL,
- 		|	NULL,
- 		|	NULL,
- 		|	0,
+ 		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.Контрагент),
+ 		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ДоговорВзаиморасчетов),
+ 		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(РеализацияАвтомобилей.ВалютаДокумента),
+ 		|	РеализацияАвтомобилей.СуммаДокумента,
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ЗапроситьСтрокиТаблицРегистраторовЗаПериод`

**Код До:**

```bsl
	ЗапросСтрокТаблицРегистраторовЗаПериод.УстановитьПараметр("датаКонец", аргДатаКонец);
	
	
	
	
	Возврат ЗапросСтрокТаблицРегистраторовЗаПериод.Выполнить().Выгрузить();
	

КонецФункции // ЗапроситьСтрокиТаблицРегистраторов()





//Муртазин И. Ф. 11-03-2026 Изменений нет
Функция СобратьТабличныйДокумент() Экспорт
	
	ссОтчётПоСтатистике = Отчеты.СтатистикаПоРаботам.Создать();
	
	ТабДокРезультат = новый ТабличныйДокумент;
```

**Код После:**

```bsl
	ЗапросСтрокТаблицРегистраторовЗаПериод.УстановитьПараметр("датаКонец", аргДатаКонец);
	
	
	
	
	Возврат ЗапросСтрокТаблицРегистраторовЗаПериод.Выполнить().Выгрузить();
	

КонецФункции // ЗапроситьСтрокиТаблицРегистраторов()


//Муртазин И. Ф. 20-04-2026 Запрос контрагентов своих
Функция ЗапроситьКонтрагентов()
	
	ссПапкаНашиОрганизации = Справочники.Контрагенты.НайтиПоКоду("ЦБ000272");
	

	ЗапросКонтрагентов = Новый Запрос;
	ЗапросКонтрагентов.Текст = 
		"ВЫБРАТЬ
		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(Контрагенты.Ссылка) КАК GUID,
		|	Контрагенты.Наименование КАК name
		|ИЗ
		|	Справочник.Контрагенты КАК Контрагенты
		|ГДЕ
		|	НЕ Контрагенты.ПометкаУдаления
		|	И Контрагенты.Родитель В ИЕРАРХИИ(&нужныйРодитель)";
	
	ЗапросКонтрагентов.УстановитьПараметр("нужныйРодитель", ссПапкаНашиОрганизации);
	
	Возврат ЗапросКонтрагентов.Выполнить().Выгрузить();
	
	
КонецФункции // ЗапроситьКонтрагентов()




//Муртазин И. Ф. 11-03-2026 Изменений нет
Функция СобратьТабличныйДокумент() Экспорт
	
	ссОтчётПоСтатистике = Отчеты.СтатистикаПоРаботам.Создать();
	
	ТабДокРезультат = новый ТабличныйДокумент;
```

**Данный участок кода:**

```bsl
+ 
+ //Муртазин И. Ф. 20-04-2026 Запрос контрагентов своих
+ Функция ЗапроситьКонтрагентов()
+ 	
+ 	ссПапкаНашиОрганизации = Справочники.Контрагенты.НайтиПоКоду("ЦБ000272");
+ 	
+ 
+ 	ЗапросКонтрагентов = Новый Запрос;
+ 	ЗапросКонтрагентов.Текст = 
+ 		"ВЫБРАТЬ
+ 		|	УНИКАЛЬНЫЙИДЕНТИФИКАТОР(Контрагенты.Ссылка) КАК GUID,
+ 		|	Контрагенты.Наименование КАК name
+ 		|ИЗ
+ 		|	Справочник.Контрагенты КАК Контрагенты
+ 		|ГДЕ
+ 		|	НЕ Контрагенты.ПометкаУдаления
+ 		|	И Контрагенты.Родитель В ИЕРАРХИИ(&нужныйРодитель)";
+ 	
+ 	ЗапросКонтрагентов.УстановитьПараметр("нужныйРодитель", ссПапкаНашиОрганизации);
+ 	
+ 	Возврат ЗапросКонтрагентов.Выполнить().Выгрузить();
+ 	
+ 	
+ КонецФункции // ЗапроситьКонтрагентов()
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 7) Общий модуль обПроцедурыОбщегоНазначенияУФ — модуль

Изменений в модуле не обнаружено после сравнения.

## 8) Обработка Web_ЗагрузкаДанных — модуль объекта

**Процедура:** `ЗаписатьТаблицуОсмотра`

**Код До:**

```bsl
//+Бардашов Н.А. 27.10.2025 №840
Процедура ЗаписатьТаблицуОсмотра(ТаблицаОсмотра)  
	Если ТаблицаОсмотра.Количество() = 0 Тогда 
		Возврат;
	КонецЕсли;	
	Для каждого СтрокаТО Из ТаблицаОсмотра Цикл
		НоваяЗапись = РегистрыСведений.Web_ОсмотрАвтомобиля.СоздатьМенеджерЗаписи();
		НоваяЗапись.Автомобиль = Автомобиль;
		НоваяЗапись.ЭлементОсмотра = СтрокаТО.ЭлементОсмотра;
		НоваяЗапись.Категория = СтрокаТО.Категория;
		НоваяЗапись.ОкрашенныйЭлемент = СтрокаТО.ОкрашенныйЭлемент;
		НоваяЗапись.Целый = СтрокаТО.Целый;
		НоваяЗапись.Присутствует = СтрокаТО.Присутствует;
		НоваяЗапись.Оригинал = СтрокаТО.Оригинал;
		НоваяЗапись.Требуется = СтрокаТО.Требуется;
		НоваяЗапись.ТолщинаЛКП = СтрокаТО.ТолщинаЛКП;  
		НоваяЗапись.Повреждения = СтрокаТО.Повреждения;
		НоваяЗапись.СсылкаНаФото = СтрокаТО.ФотоСсылка;
		
		//+Немцев 11.02.2026 №12253
		НоваяЗапись.Комментарий = СтрокаТО.Комментарий;
```

**Код После:**

```bsl
//+Бардашов Н.А. 27.10.2025 №840
Процедура ЗаписатьТаблицуОсмотра(ТаблицаОсмотра)  
	Если ТаблицаОсмотра.Количество() = 0 Тогда 
		Возврат;
	КонецЕсли;	
	Для каждого СтрокаТО Из ТаблицаОсмотра Цикл
		НоваяЗапись = РегистрыСведений.Web_ОсмотрАвтомобиля.СоздатьМенеджерЗаписи();
		НоваяЗапись.Автомобиль = Автомобиль;
		НоваяЗапись.ЭлементОсмотра = СтрокаТО.ЭлементОсмотра;
		НоваяЗапись.Категория = СтрокаТО.Категория;
		НоваяЗапись.ОкрашенныйЭлемент = ?(СтрокаТО.ОкрашенныйЭлемент И НЕ СтрокаТО.Оригинал,Истина,Ложь);
		НоваяЗапись.Целый = СтрокаТО.Целый;
		НоваяЗапись.Присутствует = СтрокаТО.Присутствует;
		НоваяЗапись.Оригинал = СтрокаТО.Оригинал;
		НоваяЗапись.Требуется = СтрокаТО.Требуется;
		НоваяЗапись.ТолщинаЛКП = СтрокаТО.ТолщинаЛКП;  
		НоваяЗапись.Повреждения = СтрокаТО.Повреждения;
		НоваяЗапись.СсылкаНаФото = СтрокаТО.ФотоСсылка;
		
		//+Немцев 11.02.2026 №12253
		НоваяЗапись.Комментарий = СтрокаТО.Комментарий;
```

**Данный участок кода:**

```bsl
- 		НоваяЗапись.ОкрашенныйЭлемент = СтрокаТО.ОкрашенныйЭлемент;
+ 		НоваяЗапись.ОкрашенныйЭлемент = ?(СтрокаТО.ОкрашенныйЭлемент И НЕ СтрокаТО.Оригинал,Истина,Ложь);
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 9) Обработка АРМАдминистратор — модуль объекта

**Процедура:** `ЗаписатьПраваИНастройки`

**Код До:**

```bsl
				Рег=РегистрыСведений.ПраваИНастройки.СоздатьМенеджерЗаписи();
				Если ТипЗнч (ОбъектНастройкиПрав) = Тип("СправочникСсылка.Пользователи") Тогда
					Рег.Объект=?(обЗначениеНеЗаполнено(ОбъектНастройкиПрав.ШаблонПрав), ОбъектНастройкиПрав, ОбъектНастройкиПрав.ШаблонПрав);
				Иначе
					Рег.Объект=ОбъектНастройкиПрав;
				КонецЕсли;
				Рег.ПравоНастройка=Строка.ПравоНастройка;
				Рег.Значение=Строка.Значение;
				Рег.Записать();
				Строка.Изменено = Ложь;
			КонецЕсли;
				 //+Зиянгиров Артур (07.03.2026) В рамках улучшения технической поддержки добавил запись в регистр изменения ИзмененияПравШаблонов 
				ДатаЗаписи=ТекущаяДата();
			    //Чтение старого значения регистра
				СтруктураОтбора = Новый Структура("Объект,ПравоНастройка", ОбъектНастройкиПрав,Строка.ПравоНастройка);
				СтруктураСведений = РегистрыСведений.ИзмененияПравШаблонов.ПолучитьПоследнее(ДатаЗаписи,СтруктураОтбора);
				ЗначениеСтарое    = СтруктураСведений.Значение;
				Записывать        = Ложь;
				//Введено значение, а старое отсутствует
				Если НЕ обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое=Неопределено Тогда Записывать=Истина; КонецЕсли;
```

**Код После:**

```bsl
				Рег=РегистрыСведений.ПраваИНастройки.СоздатьМенеджерЗаписи();
				Если ТипЗнч (ОбъектНастройкиПрав) = Тип("СправочникСсылка.Пользователи") Тогда
					Рег.Объект=?(обЗначениеНеЗаполнено(ОбъектНастройкиПрав.ШаблонПрав), ОбъектНастройкиПрав, ОбъектНастройкиПрав.ШаблонПрав);
				Иначе
					Рег.Объект=ОбъектНастройкиПрав;
				КонецЕсли;
				Рег.ПравоНастройка=Строка.ПравоНастройка;
				Рег.Значение=Строка.Значение;
				Рег.Записать();
				Строка.Изменено = Ложь;
				//+Зиянгиров (20.04.2026). Меняю подход сохранения шаблона.
				// Определяем объект для аудита (для Пользователей — шаблон)
				ОбъектДляАудита = Рег.Объект;

				ДатаЗаписи = ТекущаяДата();
				СтруктураОтбора = Новый Структура("Объект,ПравоНастройка", ОбъектДляАудита, Строка.ПравоНастройка);
				СтруктураСведений = РегистрыСведений.ИзмененияПравШаблонов.ПолучитьПоследнее(ДатаЗаписи, СтруктураОтбора);
				ЗначениеСтарое = СтруктураСведений.Значение;

				// Пишем только если значение реально изменилось
				Если Строка.Значение <> ЗначениеСтарое Тогда
					Попытка
						ЗаписьРегСведений = РегистрыСведений.ИзмененияПравШаблонов.СоздатьМенеджерЗаписи();
						ЗаписьРегСведений.Период         = ДатаЗаписи;
						ЗаписьРегСведений.Объект         = ОбъектДляАудита;
						ЗаписьРегСведений.ПравоНастройка = Строка.ПравоНастройка;
						ЗаписьРегСведений.Значение       = Строка.Значение;
						ЗаписьРегСведений.КтоИзменил     = ПараметрыСеанса.ТекущийПользователь;
						ЗаписьРегСведений.Записать();
					Исключение
					КонецПопытки;
				КонецЕсли;
				//-Зиянгиров (20.04.2026). Меняю подход сохранения шаблона.
			КонецЕсли;
		КонецЦикла; 
		
		// В случае исключения вернем Ложь - запись не удалась
	Исключение 
		Возврат Ложь;
	КонецПопытки;
	
	Если ВключитьУтверждение Тогда
		// запишем ПраваИНастройки
```

**Данный участок кода:**

```bsl
+ 				//+Зиянгиров (20.04.2026). Меняю подход сохранения шаблона.
+ 				// Определяем объект для аудита (для Пользователей — шаблон)
+ 				ОбъектДляАудита = Рег.Объект;
+ 
+ 				ДатаЗаписи = ТекущаяДата();
+ 				СтруктураОтбора = Новый Структура("Объект,ПравоНастройка", ОбъектДляАудита, Строка.ПравоНастройка);
+ 				СтруктураСведений = РегистрыСведений.ИзмененияПравШаблонов.ПолучитьПоследнее(ДатаЗаписи, СтруктураОтбора);
+ 				ЗначениеСтарое = СтруктураСведений.Значение;
+ 
+ 				// Пишем только если значение реально изменилось
+ 				Если Строка.Значение <> ЗначениеСтарое Тогда
+ 					Попытка
+ 						ЗаписьРегСведений = РегистрыСведений.ИзмененияПравШаблонов.СоздатьМенеджерЗаписи();
+ 						ЗаписьРегСведений.Период         = ДатаЗаписи;
+ 						ЗаписьРегСведений.Объект         = ОбъектДляАудита;
+ 						ЗаписьРегСведений.ПравоНастройка = Строка.ПравоНастройка;
+ 						ЗаписьРегСведений.Значение       = Строка.Значение;
+ 						ЗаписьРегСведений.КтоИзменил     = ПараметрыСеанса.ТекущийПользователь;
+ 						ЗаписьРегСведений.Записать();
+ 					Исключение
+ 					КонецПопытки;
+ 				КонецЕсли;
+ 				//-Зиянгиров (20.04.2026). Меняю подход сохранения шаблона.
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ЗаписатьПраваИНастройки` — изменил: `Зиянгиров Артур`

**Код До:**

```bsl
				Если ТипЗнч (ОбъектНастройкиПрав) = Тип("СправочникСсылка.Пользователи") Тогда
					Рег.Объект=?(обЗначениеНеЗаполнено(ОбъектНастройкиПрав.ШаблонПрав), ОбъектНастройкиПрав, ОбъектНастройкиПрав.ШаблонПрав);
				Иначе
					Рег.Объект=ОбъектНастройкиПрав;
				КонецЕсли;
				Рег.ПравоНастройка=Строка.ПравоНастройка;
				Рег.Значение=Строка.Значение;
				Рег.Записать();
				Строка.Изменено = Ложь;
			КонецЕсли;
				 //+Зиянгиров Артур (07.03.2026) В рамках улучшения технической поддержки добавил запись в регистр изменения ИзмененияПравШаблонов 
				ДатаЗаписи=ТекущаяДата();
			    //Чтение старого значения регистра
				СтруктураОтбора = Новый Структура("Объект,ПравоНастройка", ОбъектНастройкиПрав,Строка.ПравоНастройка);
				СтруктураСведений = РегистрыСведений.ИзмененияПравШаблонов.ПолучитьПоследнее(ДатаЗаписи,СтруктураОтбора);
				ЗначениеСтарое    = СтруктураСведений.Значение;
				Записывать        = Ложь;
				//Введено значение, а старое отсутствует
				Если НЕ обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое=Неопределено Тогда Записывать=Истина; КонецЕсли; 
				//Значение стерто, а старое значение было введено
				Если обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое<>Неопределено Тогда Записывать=Истина; КонецЕсли; 
				//Введено значение и было введено старое
				Если НЕ обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое<>Неопределено Тогда
			
					Если Строка.Значение<>ЗначениеСтарое Тогда Записывать=Истина; КонецЕсли; 
					
				КонецЕсли;
				
				Если Записывать Тогда
				//Значение параметра изменилось
				ЗаписьРегСведений=РегистрыСведений.ИзмененияПравШаблонов.СоздатьМенеджерЗаписи();
				ЗаписьРегСведений.Объект  			= ОбъектНастройкиПрав;
				ЗаписьРегСведений.ПравоНастройка 	= Строка.ПравоНастройка;
				ЗаписьРегСведений.Значение    		= Строка.Значение;
				ЗаписьРегСведений.Период			= ДатаЗаписи;
				ЗаписьРегСведений.КтоИзменил    	= ПараметрыСеанса.ТекущийПользователь;
				ЗаписьРегСведений.Записать(); 
				//-Зиянгиров Артур (07.03.2026) В рамках улучшения технической поддержки добавил запись в регистр изменения ИзмененияПравШаблонов 
				КонецЕсли;
		КонецЦикла; 
		
		// В случае исключения вернем Ложь - запись не удалась
	Исключение 
		Возврат Ложь;
	КонецПопытки;
	
	Если ВключитьУтверждение Тогда
		// запишем ПраваИНастройки
		ТабПраваИНастройки = ТабУтверждениеДокументов.Скопировать();
```

**Код После:**

```bsl
						ЗаписьРегСведений.Объект         = ОбъектДляАудита;
						ЗаписьРегСведений.ПравоНастройка = Строка.ПравоНастройка;
						ЗаписьРегСведений.Значение       = Строка.Значение;
						ЗаписьРегСведений.КтоИзменил     = ПараметрыСеанса.ТекущийПользователь;
						ЗаписьРегСведений.Записать();
					Исключение
					КонецПопытки;
				КонецЕсли;
				//-Зиянгиров (20.04.2026). Меняю подход сохранения шаблона.
			КонецЕсли;
		КонецЦикла; 
		
		// В случае исключения вернем Ложь - запись не удалась
	Исключение 
		Возврат Ложь;
	КонецПопытки;
	
	Если ВключитьУтверждение Тогда
		// запишем ПраваИНастройки
		ТабПраваИНастройки = ТабУтверждениеДокументов.Скопировать();
```

**Данный участок кода:**

```bsl
- 				 //+Зиянгиров Артур (07.03.2026) В рамках улучшения технической поддержки добавил запись в регистр изменения ИзмененияПравШаблонов 
- 				ДатаЗаписи=ТекущаяДата();
- 			    //Чтение старого значения регистра
- 				СтруктураОтбора = Новый Структура("Объект,ПравоНастройка", ОбъектНастройкиПрав,Строка.ПравоНастройка);
- 				СтруктураСведений = РегистрыСведений.ИзмененияПравШаблонов.ПолучитьПоследнее(ДатаЗаписи,СтруктураОтбора);
- 				ЗначениеСтарое    = СтруктураСведений.Значение;
- 				Записывать        = Ложь;
- 				//Введено значение, а старое отсутствует
- 				Если НЕ обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое=Неопределено Тогда Записывать=Истина; КонецЕсли; 
- 				//Значение стерто, а старое значение было введено
- 				Если обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое<>Неопределено Тогда Записывать=Истина; КонецЕсли; 
- 				//Введено значение и было введено старое
- 				Если НЕ обЗначениеНеЗаполнено(Строка.Значение) И ЗначениеСтарое<>Неопределено Тогда
- 			
- 					Если Строка.Значение<>ЗначениеСтарое Тогда Записывать=Истина; КонецЕсли; 
- 					
- 				КонецЕсли;
- 				
- 				Если Записывать Тогда
- 				//Значение параметра изменилось
- 				ЗаписьРегСведений=РегистрыСведений.ИзмененияПравШаблонов.СоздатьМенеджерЗаписи();
- 				ЗаписьРегСведений.Объект  			= ОбъектНастройкиПрав;
- 				ЗаписьРегСведений.ПравоНастройка 	= Строка.ПравоНастройка;
- 				ЗаписьРегСведений.Значение    		= Строка.Значение;
- 				ЗаписьРегСведений.Период			= ДатаЗаписи;
- 				ЗаписьРегСведений.КтоИзменил    	= ПараметрыСеанса.ТекущийПользователь;
- 				ЗаписьРегСведений.Записать(); 
- 				//-Зиянгиров Артур (07.03.2026) В рамках улучшения технической поддержки добавил запись в регистр изменения ИзмененияПравШаблонов 
- 				КонецЕсли;
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 10) Обработка Обмен_с_ОЗОН — модуль формы ФормаОбмена

**Процедура:** `ПоВсемПлощадкам`

**Код До:**

```bsl
	
КонецПроцедуры


&НаКлиенте
Процедура ЗапросЗаказов(Команда)
	ЗапросЗаказовНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ПоВсемПлощадкам(Команда)  
	 ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры    

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда)  
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	|	ТорговыеПлощадки.Ссылка КАК Ссылка
```

**Код После:**

```bsl
	
КонецПроцедуры


&НаКлиенте
Процедура ЗапросЗаказов(Команда)
	ЗапросЗаказовНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ПоВсемПлощадкам(Команда) Экспорт 
	 ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры    

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда) Экспорт 
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	|	ТорговыеПлощадки.Ссылка КАК Ссылка
```

**Данный участок кода:**

```bsl
- Процедура ПоВсемПлощадкам(Команда)  
+ Процедура ПоВсемПлощадкам(Команда) Экспорт
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПоВсемПлощадкамНаСервере`

**Код До:**

```bsl
Процедура ЗапросЗаказов(Команда)
	ЗапросЗаказовНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ПоВсемПлощадкам(Команда)  
	 ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры    

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда)  
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	|	ТорговыеПлощадки.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ТорговыеПлощадки КАК ТорговыеПлощадки
	|ГДЕ
	|	ТорговыеПлощадки.УчаствуетВАвтообмене
	|	И НЕ ТорговыеПлощадки.ПометкаУдаления";
```

**Код После:**

```bsl
Процедура ЗапросЗаказов(Команда)
	ЗапросЗаказовНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ПоВсемПлощадкам(Команда) Экспорт 
	 ПоВсемПлощадкамНаСервере(Команда);
КонецПроцедуры    

&НаСервере
Процедура ПоВсемПлощадкамНаСервере(Команда) Экспорт 
	
	Запрос=Новый Запрос;
	
	Запрос.Текст="ВЫБРАТЬ
	|	ТорговыеПлощадки.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ТорговыеПлощадки КАК ТорговыеПлощадки
	|ГДЕ
	|	ТорговыеПлощадки.УчаствуетВАвтообмене
	|	И НЕ ТорговыеПлощадки.ПометкаУдаления";
```

**Данный участок кода:**

```bsl
- Процедура ПоВсемПлощадкамНаСервере(Команда)  
+ Процедура ПоВсемПлощадкамНаСервере(Команда) Экспорт
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 11) Обработка СобственныйОбработчикККТ — модуль объекта

**Процедура:** `Глобальный контекст`

**Код До:**

```bsl
Перем ОсновнаяОбработка Экспорт; // Объект обработки, из которой происходит печать

//ОПИСАНИЕ ФУНКЦИЙ И ДОРАБОТКИ ПРЕДСТАВЛЕНО ПО ССЫЛКЕ
//https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#для-программистов


//МАРКИРОВКА

//ДанныеМаркировки - Структура - Содержит 3 параметра ("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX")
//ТипИдентификатораТовара - это код обозначающий, какой именно штрихкод будет разобран
//Доступные значения: "КодТовараВФорматеEAN8", "КодТовараВФорматеEAN13", "КодТовараВФорматеITF14",
```

**Код После:**

```bsl
﻿Перем ОсновнаяОбработка Экспорт; // Объект обработки, из которой происходит печать

//ОПИСАНИЕ ФУНКЦИЙ И ДОРАБОТКИ ПРЕДСТАВЛЕНО ПО ССЫЛКЕ
//https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#для-программистов


//МАРКИРОВКА

//ДанныеМаркировки - Структура - Содержит 3 параметра ("ТипИдентификатораТовара, РеквизитКодаТовара, РеквизитКодаТовараHEX")
//ТипИдентификатораТовара - это код обозначающий, какой именно штрихкод будет разобран
//Доступные значения: "КодТовараВФорматеEAN8", "КодТовараВФорматеEAN13", "КодТовараВФорматеITF14",
```

**Данный участок кода:**

```bsl
- Перем ОсновнаяОбработка Экспорт; // Объект обработки, из которой происходит печать
+ ﻿Перем ОсновнаяОбработка Экспорт; // Объект обработки, из которой происходит печать
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ДоФискализацииЧека`

**Код До:**

```bsl
Процедура ДоФискализацииЧека(пПараметры, пОбщиеПараметры, пПараметрыПодключения, пДанныеЧека, ОтказатьсяОтПечати, НомерЧека, НомерСмены, ФискальныйПризнак) Экспорт
	//ПРИМЕР:
	//Вставка email клиента в будущий чек
	//Если Найти(пДанныеЧека, "CustomerEmail") = 0 Тогда
	//	пДанныеЧека = СтрЗаменить(пДанныеЧека, "<Parameters","<Parameters CustomerEmail=""test@gmail.com"""); 
	//КонецЕсли; 
	
	//Изменения xml данные чека
	//пОбщиеПараметры.Получатель = "Василий Иванович";
	//пДанныеЧека = ОсновнаяОбработка.ККТ_XMLПредставлениеЧека(пОбщиеПараметры, пПараметрыПодключения);

	// Для Штрих синхронизируем тег 1057 (шапка) и 1222 (строка), иначе возможен 33h/1057.
	Попытка
		Если пОбщиеПараметры.Свойство("ПризнакАгента")
			И ЗначениеЗаполнено(пОбщиеПараметры.ПризнакАгента)
			И пОбщиеПараметры.Свойство("ПозицииЧека")
			И пОбщиеПараметры.ПозицииЧека <> Неопределено Тогда
			
			ПризнакАгентаККТ = ПолучитьПризнакАгентаДляККТ(пОбщиеПараметры.ПризнакАгента);
			пОбщиеПараметры.ПризнакАгента = ПризнакАгентаККТ;
			
			Для Каждого ПозицияЧека Из пОбщиеПараметры.ПозицииЧека Цикл
				Если ПозицияЧека.Свойство("ФискальнаяСтрока") Тогда
					ПозицияЧека.ПризнакАгентаПоПредметуРасчета = ПризнакАгентаККТ;
				КонецЕсли;
			КонецЦикла;
			
			// После синхронизации формируем финальный XML для драйвера.
			пДанныеЧека = ОсновнаяОбработка.ККТ_XMLПредставлениеЧека(пОбщиеПараметры, пПараметрыПодключения);
		КонецЕсли;
	Исключение
	КонецПопытки;

	// Трассировка payload для разбора ошибки 1057 у Штрих.
	Попытка
		АгентШапка = "";
		Если пОбщиеПараметры.Свойство("ПризнакАгента") Тогда
			АгентШапка = Строка(пОбщиеПараметры.ПризнакАгента);
		КонецЕсли;
		
		АгентСтрока = "";
		Если пОбщиеПараметры.Свойство("ПозицииЧека")
			И пОбщиеПараметры.ПозицииЧека <> Неопределено
			И пОбщиеПараметры.ПозицииЧека.Количество() > 0 Тогда
			ПерваяПозиция = пОбщиеПараметры.ПозицииЧека[0];
			Если ПерваяПозиция.Свойство("ПризнакАгентаПоПредметуРасчета") Тогда
				АгентСтрока = Строка(ПерваяПозиция.ПризнакАгентаПоПредметуРасчета);
			КонецЕсли;
		КонецЕсли;
		
		ПоставщикИНН = "";
		ПоставщикТелефон = "";
		Если пОбщиеПараметры.Свойство("ДанныеПоставщика")
			И пОбщиеПараметры.ДанныеПоставщика <> Неопределено Тогда
			Если пОбщиеПараметры.ДанныеПоставщика.Свойство("ИНН") Тогда
				ПоставщикИНН = пОбщиеПараметры.ДанныеПоставщика.ИНН;
			КонецЕсли;
			Если пОбщиеПараметры.ДанныеПоставщика.Свойство("Телефон") Тогда
				ПоставщикТелефон = пОбщиеПараметры.ДанныеПоставщика.Телефон;
			КонецЕсли;
		КонецЕсли;
		
		AgentTypeXML = ПолучитьАтрибутXML(пДанныеЧека, "AgentType");
		CalcAgentXML = ПолучитьАтрибутXML(пДанныеЧека, "CalculationAgent");
		VendorINNXML = ПолучитьАтрибутXML(пДанныеЧека, "VendorINN");
		VendorPhoneXML = ПолучитьАтрибутXML(пДанныеЧека, "VendorPhone");
		PurveyorVATINXML = ПолучитьАтрибутXML(пДанныеЧека, "PurveyorVATIN");
		PurveyorPhoneXML = ПолучитьАтрибутXML(пДанныеЧека, "PurveyorPhone");
		
		ТекстЛога = СтрШаблон("KKT1057 TRACE: HeadAgent=%1; RowAgent=%2; SupplierINN=%3; SupplierPhone=%4; XML AgentType=%5; XML CalculationAgent=%6; XML VendorINN=%7; XML VendorPhone=%8; XML PurveyorVATIN=%9; XML PurveyorPhone=%10",
			АгентШапка, АгентСтрока, ПоставщикИНН, ПоставщикТелефон,
			AgentTypeXML, CalcAgentXML, VendorINNXML, VendorPhoneXML, PurveyorVATINXML, PurveyorPhoneXML);
		ЗаписьЖурналаРегистрации("KKT1057", УровеньЖурналаРегистрации.Информация, , пОбщиеПараметры.ДокументОснование, ТекстЛога);
	Исключение
	КонецПопытки;
	
КонецПроцедуры

Функция ПолучитьАтрибутXML(ТекстXML, ИмяАтрибута)
	Шаблон = ИмяАтрибута + "=" + """";
	Позиция = Найти(ТекстXML, Шаблон);
	Если Позиция = 0 Тогда
		Возврат "";
	КонецЕсли;
	
	НачалоЗначения = Позиция + СтрДлина(Шаблон);
	Хвост = Сред(ТекстXML, НачалоЗначения);
	КонецОтносительный = Найти(Хвост, """");
	Если КонецОтносительный = 0 Тогда
		Возврат "";
	КонецЕсли;
	
	Возврат Сред(ТекстXML, НачалоЗначения, КонецОтносительный - 1);
КонецФункции

//Выполняется операции печати чека
//Можно реализовать собственную рассылку чеков
//Или сохранить данные о напечатанном чеке
// 
//Параметры:
//  пПараметры - Тип Структура, настройки Обработки, см. мПараметрыУстройства
//  пОбщиеПараметры - Тип Структура, Структура текущего чека, см. мОбщиеПараметры.
//  пТекстЭлектронногоЧека - Тип Строка, Текст электронного чека, который может быть отправлен по SMS, или будет сохранен в базе.
//  НомерЧека - Тип Число, номер напечатанного чека
```

**Код После:**

```bsl
Процедура ДоФискализацииЧека(пПараметры, пОбщиеПараметры, пПараметрыПодключения, пДанныеЧека, ОтказатьсяОтПечати, НомерЧека, НомерСмены, ФискальныйПризнак) Экспорт
	//ПРИМЕР:
	//Вставка email клиента в будущий чек
	//Если Найти(пДанныеЧека, "CustomerEmail") = 0 Тогда
	//	пДанныеЧека = СтрЗаменить(пДанныеЧека, "<Parameters","<Parameters CustomerEmail=""test@gmail.com"""); 
	//КонецЕсли; 
	
	//Изменения xml данные чека
	//пОбщиеПараметры.Получатель = "Василий Иванович";
	//пДанныеЧека = ОсновнаяОбработка.ККТ_XMLПредставлениеЧека(пОбщиеПараметры, пПараметрыПодключения);
	
КонецПроцедуры

//Выполняется операции печати чека
//Можно реализовать собственную рассылку чеков
//Или сохранить данные о напечатанном чеке
// 
//Параметры:
//  пПараметры - Тип Структура, настройки Обработки, см. мПараметрыУстройства
//  пОбщиеПараметры - Тип Структура, Структура текущего чека, см. мОбщиеПараметры.
//  пТекстЭлектронногоЧека - Тип Строка, Текст электронного чека, который может быть отправлен по SMS, или будет сохранен в базе.
//  НомерЧека - Тип Число, номер напечатанного чека
```

**Данный участок кода:**

```bsl
- 
- 	// Для Штрих синхронизируем тег 1057 (шапка) и 1222 (строка), иначе возможен 33h/1057.
- 	Попытка
- 		Если пОбщиеПараметры.Свойство("ПризнакАгента")
- 			И ЗначениеЗаполнено(пОбщиеПараметры.ПризнакАгента)
- 			И пОбщиеПараметры.Свойство("ПозицииЧека")
- 			И пОбщиеПараметры.ПозицииЧека <> Неопределено Тогда
- 			
- 			ПризнакАгентаККТ = ПолучитьПризнакАгентаДляККТ(пОбщиеПараметры.ПризнакАгента);
- 			пОбщиеПараметры.ПризнакАгента = ПризнакАгентаККТ;
- 			
- 			Для Каждого ПозицияЧека Из пОбщиеПараметры.ПозицииЧека Цикл
- 				Если ПозицияЧека.Свойство("ФискальнаяСтрока") Тогда
- 					ПозицияЧека.ПризнакАгентаПоПредметуРасчета = ПризнакАгентаККТ;
- 				КонецЕсли;
- 			КонецЦикла;
- 			
- 			// После синхронизации формируем финальный XML для драйвера.
- 			пДанныеЧека = ОсновнаяОбработка.ККТ_XMLПредставлениеЧека(пОбщиеПараметры, пПараметрыПодключения);
- 		КонецЕсли;
- 	Исключение
- 	КонецПопытки;
- 
- 	// Трассировка payload для разбора ошибки 1057 у Штрих.
- 	Попытка
- 		АгентШапка = "";
- 		Если пОбщиеПараметры.Свойство("ПризнакАгента") Тогда
- 			АгентШапка = Строка(пОбщиеПараметры.ПризнакАгента);
- 		КонецЕсли;
- 		
- 		АгентСтрока = "";
- 		Если пОбщиеПараметры.Свойство("ПозицииЧека")
- 			И пОбщиеПараметры.ПозицииЧека <> Неопределено
- 			И пОбщиеПараметры.ПозицииЧека.Количество() > 0 Тогда
- 			ПерваяПозиция = пОбщиеПараметры.ПозицииЧека[0];
- 			Если ПерваяПозиция.Свойство("ПризнакАгентаПоПредметуРасчета") Тогда
- 				АгентСтрока = Строка(ПерваяПозиция.ПризнакАгентаПоПредметуРасчета);
- 			КонецЕсли;
- 		КонецЕсли;
- 		
- 		ПоставщикИНН = "";
- 		ПоставщикТелефон = "";
- 		Если пОбщиеПараметры.Свойство("ДанныеПоставщика")
- 			И пОбщиеПараметры.ДанныеПоставщика <> Неопределено Тогда
- 			Если пОбщиеПараметры.ДанныеПоставщика.Свойство("ИНН") Тогда
- 				ПоставщикИНН = пОбщиеПараметры.ДанныеПоставщика.ИНН;
- 			КонецЕсли;
- 			Если пОбщиеПараметры.ДанныеПоставщика.Свойство("Телефон") Тогда
- 				ПоставщикТелефон = пОбщиеПараметры.ДанныеПоставщика.Телефон;
- 			КонецЕсли;
- 		КонецЕсли;
- 		
- 		AgentTypeXML = ПолучитьАтрибутXML(пДанныеЧека, "AgentType");
- 		CalcAgentXML = ПолучитьАтрибутXML(пДанныеЧека, "CalculationAgent");
- 		VendorINNXML = ПолучитьАтрибутXML(пДанныеЧека, "VendorINN");
- 		VendorPhoneXML = ПолучитьАтрибутXML(пДанныеЧека, "VendorPhone");
- 		PurveyorVATINXML = ПолучитьАтрибутXML(пДанныеЧека, "PurveyorVATIN");
- 		PurveyorPhoneXML = ПолучитьАтрибутXML(пДанныеЧека, "PurveyorPhone");
- 		
- 		ТекстЛога = СтрШаблон("KKT1057 TRACE: HeadAgent=%1; RowAgent=%2; SupplierINN=%3; SupplierPhone=%4; XML AgentType=%5; XML CalculationAgent=%6; XML VendorINN=%7; XML VendorPhone=%8; XML PurveyorVATIN=%9; XML PurveyorPhone=%10",
- 			АгентШапка, АгентСтрока, ПоставщикИНН, ПоставщикТелефон,
- 			AgentTypeXML, CalcAgentXML, VendorINNXML, VendorPhoneXML, PurveyorVATINXML, PurveyorPhoneXML);
- 		ЗаписьЖурналаРегистрации("KKT1057", УровеньЖурналаРегистрации.Информация, , пОбщиеПараметры.ДокументОснование, ТекстЛога);
- 	Исключение
- 	КонецПопытки;
- 	
- КонецПроцедуры
- 
- Функция ПолучитьАтрибутXML(ТекстXML, ИмяАтрибута)
- 	Шаблон = ИмяАтрибута + "=" + """";
- 	Позиция = Найти(ТекстXML, Шаблон);
- 	Если Позиция = 0 Тогда
- 		Возврат "";
- 	КонецЕсли;
- 	
- 	НачалоЗначения = Позиция + СтрДлина(Шаблон);
- 	Хвост = Сред(ТекстXML, НачалоЗначения);
- 	КонецОтносительный = Найти(Хвост, """");
- 	Если КонецОтносительный = 0 Тогда
- 		Возврат "";
// ... сокращено, всего строк: 86
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПослеФормированииТаблицыЧека` — изменил: `Зиянгиров Артур`

**Код До:**

```bsl
//Вы можете реализовать собственный алгоритм распределения чека, меняя ключевые поля.
//Объединять позиции в одну строку, или добавлять собственные. И многое другое.
// 
//Параметры:
//  пТаблицаЧека - Тип ТаблицаЗначений, содержит позиции фискальной строки чека. 
// 		См. ОсновнаяОбработка.мМодульККТОбщий.ККТ_ПолучитьПустуюТаблицуСДанными();
//  пПараметры - Тип Структура, настройки обработки, см. мПараметрыУстройства
//
Процедура ПослеФормированииТаблицыЧека(пТаблицаЧека, пПараметры) Экспорт
	//ОПИСАНИЕ: https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#послеформированиитаблицычека
	// + Зиянгиров Артур (27.03.2026) Автозаполнение агентских данных и поставщика из договора взаиморасчетов.
	// + Зиянгиров Артур (30.03.2026) Ключи агента/поставщика формируются по Коду договора (с резервом на ссылку).
	// + Зиянгиров Артур (26.03.2026) Добавлены пояснения к ранним выходам при отсутствии договора и признака агента.
	
	ДокументОснование = ОсновнаяОбработка.мОбщиеПараметры.ДокументОснование; //Текущий документ, если что.
	ДоговорВзаиморасчетов = ПолучитьДоговорВзаиморасчетовДляАгента(пТаблицаЧека, ДокументОснование);
	// Если договор не найден, заполнять агентские реквизиты нельзя — выходим без изменений.
	Если НЕ ЗначениеЗаполнено(ДоговорВзаиморасчетов) Тогда
		Возврат;
	КонецЕсли;
	
	// Если в договоре нет признака агента, структура агента будет некорректной — выходим.
	Если НЕ ЗначениеЗаполнено(ДоговорВзаиморасчетов.ПризнакАгента) Тогда
		Возврат;
	КонецЕсли;
	
	// Передаем реквизиты напрямую в строки чека, как это делает фронт кассира:
	// Признак агента (перечисление) + данные поставщика.
	НаименованиеПоставщика = "";
	ИННПоставщика = "";
	ТелефонПоставщика = "";
	Попытка НаименованиеПоставщика = ДоговорВзаиморасчетов.НаименованиеПоставщика; Исключение КонецПопытки;
	Попытка ИННПоставщика = ДоговорВзаиморасчетов.ИННПоставщика; Исключение КонецПопытки;
	Попытка ТелефонПоставщика = ДоговорВзаиморасчетов.ТелефонПоставщика; Исключение КонецПопытки;
	ТелефонПоставщика = НормализоватьТелефонДляККТ(ТелефонПоставщика);
	ПризнакАгентаККТ = ПолучитьПризнакАгентаДляККТ(ДоговорВзаиморасчетов.ПризнакАгента);
	
	Для Каждого СтрокаЧека Из пТаблицаЧека Цикл
		Попытка СтрокаЧека.ПризнакАгента = ПризнакАгентаККТ; Исключение КонецПопытки;
		Попытка СтрокаЧека.ПризнакАгентаПоПредметуРасчета = ПризнакАгентаККТ; Исключение КонецПопытки;
		Попытка СтрокаЧека.Наименование = НаименованиеПоставщика; Исключение КонецПопытки;
		Попытка СтрокаЧека.ИНН = ИННПоставщика; Исключение КонецПопытки;
		Попытка СтрокаЧека.Телефон = ТелефонПоставщика; Исключение КонецПопытки;
		Попытка СтрокаЧека.ДоговорАгента = ""; Исключение КонецПопытки;
		Попытка СтрокаЧека.ДоговорАгентаПоПозициям = ""; Исключение КонецПопытки;
		Попытка СтрокаЧека.ПоставщикАгента = ""; Исключение КонецПопытки;
		Попытка СтрокаЧека.ПоставщикАгентаПоПозициям = ""; Исключение КонецПопытки;
	КонецЦикла;
КонецПроцедуры

Функция ПолучитьПризнакАгентаДляККТ(ПризнакАгентаИсходный)
	Если НЕ ЗначениеЗаполнено(ПризнакАгентаИсходный) Тогда
		Возврат ПризнакАгентаИсходный;
	КонецЕсли;
	
	ПризнакАгента = ПризнакАгентаИсходный;
	КодПризнакаСтрокой = "";
	Попытка
		КодПризнакаСтрокой = ВРег(СокрЛП(Строка(ПризнакАгентаИсходный)));
	Исключение
	КонецПопытки;
	
	// Если пришел строковый код/наименование, приводим к перечислению.
	Если ТипЗнч(ПризнакАгентаИсходный) = Тип("Строка") Тогда
		Если КодПризнакаСтрокой = "0" ИЛИ КодПризнакаСтрокой = "БАНКОВСКИЙПЛАТЕЖНЫЙАГЕНТ" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.БанковскийПлатежныйАгент");
		ИначеЕсли КодПризнакаСтрокой = "1" ИЛИ КодПризнакаСтрокой = "БАНКОВСКИЙПЛАТЕЖНЫЙСУБАГЕНТ" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.БанковскийПлатежныйСубагент");
		ИначеЕсли КодПризнакаСтрокой = "2" ИЛИ КодПризнакаСтрокой = "ПЛАТЕЖНЫЙАГЕНТ" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.ПлатежныйАгент");
		ИначеЕсли КодПризнакаСтрокой = "3" ИЛИ КодПризнакаСтрокой = "ПЛАТЕЖНЫЙСУБАГЕНТ" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.ПлатежныйСубагент");
		ИначеЕсли КодПризнакаСтрокой = "4" ИЛИ КодПризнакаСтрокой = "ПОВЕРЕННЫЙ" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Поверенный");
		ИначеЕсли КодПризнакаСтрокой = "5" ИЛИ КодПризнакаСтрокой = "КОМИССИОНЕР" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Комиссионер");
		ИначеЕсли КодПризнакаСтрокой = "6" ИЛИ КодПризнакаСтрокой = "АГЕНТ" Тогда
			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Агент");
		КонецЕсли;
	КонецЕсли;
	
	// Для Штрих значение "Агент" часто приводит к 33h/1057 при пробитии;
	// для комиссионной схемы передаем "Комиссионер".
	Если ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Агент") Тогда
		ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Комиссионер");
	КонецЕсли;
	
	Возврат ПризнакАгента;
КонецФункции

Функция НормализоватьТелефонДляККТ(ТелефонИсходный)
	Телефон = "";
	Попытка
		Телефон = СокрЛП(Строка(ТелефонИсходный));
	Исключение
		Возврат "";
	КонецПопытки;
	
	Если ПустаяСтрока(Телефон) Тогда
		Возврат "";
	КонецЕсли;
	
	Телефон = СтрЗаменить(Телефон, " ", "");
	Телефон = СтрЗаменить(Телефон, "-", "");
	Телефон = СтрЗаменить(Телефон, "(", "");
	Телефон = СтрЗаменить(Телефон, ")", "");
	
	ТолькоЦифры = "";
	Для ИндексСимвола = 1 По СтрДлина(Телефон) Цикл
		Символ = Сред(Телефон, ИндексСимвола, 1);
		Если Символ >= "0" И Символ <= "9" Тогда
			ТолькоЦифры = ТолькоЦифры + Символ;
		КонецЕсли;
	КонецЦикла;
	
	Если СтрДлина(ТолькоЦифры) < 10 Тогда
		Возврат "";
	КонецЕсли;
	
	Если СтрДлина(ТолькоЦифры) = 11 И Лев(ТолькоЦифры, 1) = "8" Тогда
// ... сокращено, всего строк: 273
```

**Код После:**

```bsl
//Вы можете реализовать собственный алгоритм распределения чека, меняя ключевые поля.
//Объединять позиции в одну строку, или добавлять собственные. И многое другое.
// 
//Параметры:
//  пТаблицаЧека - Тип ТаблицаЗначений, содержит позиции фискальной строки чека. 
// 		См. ОсновнаяОбработка.мМодульККТОбщий.ККТ_ПолучитьПустуюТаблицуСДанными();
//  пПараметры - Тип Структура, настройки обработки, см. мПараметрыУстройства
//
Процедура ПослеФормированииТаблицыЧека(пТаблицаЧека, пПараметры) Экспорт
	//ОПИСАНИЕ: https://github.com/andrei-karpov/1c_kkt_54fz/blob/main/for_programmers.md#послеформированиитаблицычека
КонецПроцедуры

//Выполняется после фискализации чека, 
//и перед отправкой чека по SMS или EMAIL средствами обработки
//
//Параметры:
//  СсылкаНаДокумент - Тип Документ, Ссылка на документ, из которого происходит печать 
//  ДанныеОтправкиЧека - Тип Структура, Параметры отправки чека, может быть либо SMS, либо EMAIL
//  ОтказатьОтОтправки - Тип Булево, Если установлен Истина, то чек средствами обработки не будет отправлен. 
Процедура ПередОтправкойЧека(СсылкаНаДокумент, ДанныеОтправкиЧека, ОтказатьОтОтправки = Ложь) Экспорт
```

**Данный участок кода:**

```bsl
- 	// + Зиянгиров Артур (27.03.2026) Автозаполнение агентских данных и поставщика из договора взаиморасчетов.
- 	// + Зиянгиров Артур (30.03.2026) Ключи агента/поставщика формируются по Коду договора (с резервом на ссылку).
- 	// + Зиянгиров Артур (26.03.2026) Добавлены пояснения к ранним выходам при отсутствии договора и признака агента.
- 	
- 	ДокументОснование = ОсновнаяОбработка.мОбщиеПараметры.ДокументОснование; //Текущий документ, если что.
- 	ДоговорВзаиморасчетов = ПолучитьДоговорВзаиморасчетовДляАгента(пТаблицаЧека, ДокументОснование);
- 	// Если договор не найден, заполнять агентские реквизиты нельзя — выходим без изменений.
- 	Если НЕ ЗначениеЗаполнено(ДоговорВзаиморасчетов) Тогда
- 		Возврат;
- 	КонецЕсли;
- 	
- 	// Если в договоре нет признака агента, структура агента будет некорректной — выходим.
- 	Если НЕ ЗначениеЗаполнено(ДоговорВзаиморасчетов.ПризнакАгента) Тогда
- 		Возврат;
- 	КонецЕсли;
- 	
- 	// Передаем реквизиты напрямую в строки чека, как это делает фронт кассира:
- 	// Признак агента (перечисление) + данные поставщика.
- 	НаименованиеПоставщика = "";
- 	ИННПоставщика = "";
- 	ТелефонПоставщика = "";
- 	Попытка НаименованиеПоставщика = ДоговорВзаиморасчетов.НаименованиеПоставщика; Исключение КонецПопытки;
- 	Попытка ИННПоставщика = ДоговорВзаиморасчетов.ИННПоставщика; Исключение КонецПопытки;
- 	Попытка ТелефонПоставщика = ДоговорВзаиморасчетов.ТелефонПоставщика; Исключение КонецПопытки;
- 	ТелефонПоставщика = НормализоватьТелефонДляККТ(ТелефонПоставщика);
- 	ПризнакАгентаККТ = ПолучитьПризнакАгентаДляККТ(ДоговорВзаиморасчетов.ПризнакАгента);
- 	
- 	Для Каждого СтрокаЧека Из пТаблицаЧека Цикл
- 		Попытка СтрокаЧека.ПризнакАгента = ПризнакАгентаККТ; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.ПризнакАгентаПоПредметуРасчета = ПризнакАгентаККТ; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.Наименование = НаименованиеПоставщика; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.ИНН = ИННПоставщика; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.Телефон = ТелефонПоставщика; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.ДоговорАгента = ""; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.ДоговорАгентаПоПозициям = ""; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.ПоставщикАгента = ""; Исключение КонецПопытки;
- 		Попытка СтрокаЧека.ПоставщикАгентаПоПозициям = ""; Исключение КонецПопытки;
- 	КонецЦикла;
- КонецПроцедуры
- 
- Функция ПолучитьПризнакАгентаДляККТ(ПризнакАгентаИсходный)
- 	Если НЕ ЗначениеЗаполнено(ПризнакАгентаИсходный) Тогда
- 		Возврат ПризнакАгентаИсходный;
- 	КонецЕсли;
- 	
- 	ПризнакАгента = ПризнакАгентаИсходный;
- 	КодПризнакаСтрокой = "";
- 	Попытка
- 		КодПризнакаСтрокой = ВРег(СокрЛП(Строка(ПризнакАгентаИсходный)));
- 	Исключение
- 	КонецПопытки;
- 	
- 	// Если пришел строковый код/наименование, приводим к перечислению.
- 	Если ТипЗнч(ПризнакАгентаИсходный) = Тип("Строка") Тогда
- 		Если КодПризнакаСтрокой = "0" ИЛИ КодПризнакаСтрокой = "БАНКОВСКИЙПЛАТЕЖНЫЙАГЕНТ" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.БанковскийПлатежныйАгент");
- 		ИначеЕсли КодПризнакаСтрокой = "1" ИЛИ КодПризнакаСтрокой = "БАНКОВСКИЙПЛАТЕЖНЫЙСУБАГЕНТ" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.БанковскийПлатежныйСубагент");
- 		ИначеЕсли КодПризнакаСтрокой = "2" ИЛИ КодПризнакаСтрокой = "ПЛАТЕЖНЫЙАГЕНТ" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.ПлатежныйАгент");
- 		ИначеЕсли КодПризнакаСтрокой = "3" ИЛИ КодПризнакаСтрокой = "ПЛАТЕЖНЫЙСУБАГЕНТ" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.ПлатежныйСубагент");
- 		ИначеЕсли КодПризнакаСтрокой = "4" ИЛИ КодПризнакаСтрокой = "ПОВЕРЕННЫЙ" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Поверенный");
- 		ИначеЕсли КодПризнакаСтрокой = "5" ИЛИ КодПризнакаСтрокой = "КОМИССИОНЕР" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Комиссионер");
- 		ИначеЕсли КодПризнакаСтрокой = "6" ИЛИ КодПризнакаСтрокой = "АГЕНТ" Тогда
- 			ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Агент");
- 		КонецЕсли;
- 	КонецЕсли;
- 	
- 	// Для Штрих значение "Агент" часто приводит к 33h/1057 при пробитии;
- 	// для комиссионной схемы передаем "Комиссионер".
- 	Если ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Агент") Тогда
- 		ПризнакАгента = ПредопределенноеЗначение("Перечисление.ПризнакиАгента.Комиссионер");
- 	КонецЕсли;
- 	
- 	Возврат ПризнакАгента;
- КонецФункции
- 
// ... сокращено, всего строк: 254
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 12) Документ ЗаказНаряд — модуль объекта

**Процедура:** `Глобальный контекст`

**Код До:**

```bsl
Перем ИмяРеквизитаКода Экспорт;     // Кэш права "РежимВыводаКодаВДокументах". Служит для настройки вывода кода/артикула в ТЧ

Перем КэшПакетов;
Перем ТоварыБезНДС;
Перем РаботыБезНДС;
Перем ЗакрыватьИнтервалыРабот Экспорт; //Флаг необходимости закрытия интервалов выполнения работ при закрытии заказ-наряда
Перем ПрограммныйЗапрос Экспорт; // odin+ (10.11.17) БИЗНЕС-ПРОЦЕСС "ПСО" Для определения программного запроса к документу (отключает часть проверок)

Перем УдалитьАвтоСозданныеДокументы;		// Дьяконов+ (11.12.24) ORG-716






////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ОБЩЕГО НАЗНАЧЕНИЯ

Функция ПолучитьДатуДокумента(СостояниеНаряда = Неопределено) Экспорт
	
	Если СостояниеНаряда = Неопределено Тогда
		СостояниеНаряда = Состояние;
```

**Код После:**

```bsl
Перем ИмяРеквизитаКода Экспорт;     // Кэш права "РежимВыводаКодаВДокументах". Служит для настройки вывода кода/артикула в ТЧ

Перем КэшПакетов;
Перем ТоварыБезНДС;
Перем РаботыБезНДС;
Перем ЗакрыватьИнтервалыРабот Экспорт; //Флаг необходимости закрытия интервалов выполнения работ при закрытии заказ-наряда
Перем ПрограммныйЗапрос Экспорт; // odin+ (10.11.17) БИЗНЕС-ПРОЦЕСС "ПСО" Для определения программного запроса к документу (отключает часть проверок)

Перем УдалитьАвтоСозданныеДокументы;		// Дьяконов+ (11.12.24) ORG-716

Перем флПлательщикИзменён Экспорт; //Муртазин И. Ф. ORANGE start 1/4 13-04-2026 Задача 11281
//Обновление сопутствующих документов (ПоступлениеТоваров и СчётФактураПолученный) если отличается контрагент или дата



////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ОБЩЕГО НАЗНАЧЕНИЯ

Функция ПолучитьДатуДокумента(СостояниеНаряда = Неопределено) Экспорт
	
	Если СостояниеНаряда = Неопределено Тогда
		СостояниеНаряда = Состояние;
```

**Данный участок кода:**

```bsl
- 
- 
+ Перем флПлательщикИзменён Экспорт; //Муртазин И. Ф. ORANGE start 1/4 13-04-2026 Задача 11281
+ //Обновление сопутствующих документов (ПоступлениеТоваров и СчётФактураПолученный) если отличается контрагент или дата
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПриЗаписи`

**Код До:**

```bsl
		
		ДатаСтарта = РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение("СОПД DNM", "ДатаСтарта");
		Если ЗначениеЗаполнено(ДатаСтарта) И ДатаСтарта < ТекущаяДата() Тогда
			#Если Клиент Тогда
				ПроверкаСОПД_DNM()
			#КонецЕсли
		КонецЕсли;
	КонецЕсли;
	//-Фомин Сергей 24.06.2025 btrx#2331
	
КонецПроцедуры

Процедура ПередУдалением(Отказ)
	Если НЕ дкПередУдалением(ЭтотОбъект, Отказ) Тогда Возврат; КонецЕсли;

КонецПроцедуры

Процедура ОбработкаУдаленияПроведения(Отказ)
	Если НЕ дкОбработкаУдаленияПроведения(ЭтотОбъект, Отказ) Тогда Возврат; КонецЕсли;
```

**Код После:**

```bsl
		
		ДатаСтарта = РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение("СОПД DNM", "ДатаСтарта");
		Если ЗначениеЗаполнено(ДатаСтарта) И ДатаСтарта < ТекущаяДата() Тогда
			#Если Клиент Тогда
				ПроверкаСОПД_DNM()
			#КонецЕсли
		КонецЕсли;
	КонецЕсли;
	//-Фомин Сергей 24.06.2025 btrx#2331
	
	  
	//Муртазин И. Ф. ORANGE start 2/4 13-04-2026 Задача 11281
	//Обновление сопутствующих документов (ПоступлениеТоваров и СчётФактураПолученный) если отличается контрагент или дата
	//Основная логика:
	//Если контрагент изменён то ПоступлениеТоваров и СчётФактураПолученный удалаяются
	//	
	
	
	Если РольДоступна("ПолныеПрава") Тогда
		
		Если Не Отказ И НЕ ЭтоНовый() И Проведен Тогда
			
			ЗапросПоступленияТоваровИСчётаФактурыПолученного = Новый Запрос;
			ЗапросПоступленияТоваровИСчётаФактурыПолученного.Текст = 
			"ВЫБРАТЬ ПЕРВЫЕ 1
			|	ПодчиненныеДокументы.Ссылка КАК Ссылка
			|ИЗ
			|	КритерийОтбора.ПодчиненныеДокументы(&документОснование) КАК ПодчиненныеДокументы
			|ГДЕ
			|	ТИПЗНАЧЕНИЯ(ПодчиненныеДокументы.Ссылка) = ТИП(Документ.ПоступлениеТоваров)
			|	И ПодчиненныеДокументы.Ссылка.ХозОперация = ЗНАЧЕНИЕ(Справочник.ХозОперации.УслугиСтороннихОрганизаций)
			|	И НЕ ПодчиненныеДокументы.Ссылка.ПометкаУдаления";
			
			ЗапросПоступленияТоваровИСчётаФактурыПолученного.УстановитьПараметр("ДокументОснование", Ссылка);
			
			ВыбРезультат = ЗапросПоступленияТоваровИСчётаФактурыПолученного.Выполнить().Выбрать();
			
			обПоступлениеТоваров = Неопределено;
			обСчётФактураПолученный = Неопределено;
			
			Если ВыбРезультат.Следующий() Тогда
				
				обПоступлениеТоваров = ВыбРезультат.Ссылка.ПолучитьОбъект();
				
			КонецЕсли;
			
			ЗапросПоступленияТоваровИСчётаФактурыПолученного = Новый Запрос;
			ЗапросПоступленияТоваровИСчётаФактурыПолученного.Текст = 
			"ВЫБРАТЬ ПЕРВЫЕ 1
			|	ПодчиненныеДокументы.Ссылка КАК Ссылка
			|ИЗ
			|	КритерийОтбора.ПодчиненныеДокументы(&документОснование) КАК ПодчиненныеДокументы
			|ГДЕ
			|	ТИПЗНАЧЕНИЯ(ПодчиненныеДокументы.Ссылка) = ТИП(Документ.СчетФактураПолученный)
			|	И ПодчиненныеДокументы.Ссылка.ХозОперация = ЗНАЧЕНИЕ(Справочник.ХозОперации.УслугиСтороннихОрганизаций)
			|	И НЕ ПодчиненныеДокументы.Ссылка.ПометкаУдаления";
			
			ЗапросПоступленияТоваровИСчётаФактурыПолученного.УстановитьПараметр("ДокументОснование", Ссылка);
			
			ВыбРезультат = ЗапросПоступленияТоваровИСчётаФактурыПолученного.Выполнить().Выбрать();
			
			
			Если ВыбРезультат.Следующий() Тогда
				
				обСчётФактураПолученный = ВыбРезультат.Ссылка.ПолучитьОбъект();
				
			КонецЕсли;
			
			
			Если флПлательщикИзменён Тогда
				
				Если Не обПоступлениеТоваров = Неопределено Тогда
					
					обПоступлениеТоваров.Записать(РежимЗаписиДокумента.ОтменаПроведения);
					обПоступлениеТоваров.УстановитьПометкуУдаления(Истина);
					
				КонецЕсли; 
				
				Если Не обСчётФактураПолученный = Неопределено Тогда
					
					обСчётФактураПолученный.Записать(РежимЗаписиДокумента.ОтменаПроведения);
					обСчётФактураПолученный.УстановитьПометкуУдаления(Истина);
					
				КонецЕсли;
				
			Иначе
				
				Если Не обПоступлениеТоваров = Неопределено Тогда
					
					ссНоменклатураУслугаРемонт = Справочники.Номенклатура.НайтиПоКоду("ЦБ017091"); // Ремонт, комплектация  автомобиля
					
					
					флЕстьРасхожденияДаты = (НЕ Ссылка.ДатаЗакрытия = обПоступлениеТоваров.Ссылка.Дата);
					
					флЕстьРасхожденияТабличнойЧастиТоваров = ЕстьРасхождения(обПоступлениеТоваров.Ссылка.Товары);
					
					флЕстьРасхождения = (флЕстьРасхожденияДаты ИЛИ флЕстьРасхожденияТабличнойЧастиТоваров); 
					
					Если флЕстьРасхождения Тогда
						
						обПоступлениеТоваров.Заполнить(Ссылка);
						
						Если флЕстьРасхожденияДаты Тогда
							
							обПоступлениеТоваров.Дата = Ссылка.ДатаЗакрытия;
							
						КонецЕсли;
						
						Если флЕстьРасхожденияТабличнойЧастиТоваров Тогда
							
							обПоступлениеТоваров.Товары.Очистить();
							
							Если Работы.Количество() > 0 Тогда 
								
								добавленнаяСтрока = обПоступлениеТоваров.Товары.Добавить();
								
								добавленнаяСтрока.Номенклатура = ссНоменклатураУслугаРемонт;
								обПоступлениеТоваров.ОбработкаРеквизита("Товары.Номенклатура", добавленнаяСтрока);
								
								добавленнаяСтрока.Цена = Товары.Итог("СуммаВсего") + Работы.Итог("СуммаВсего");
// ... сокращено, всего строк: 161
```

**Данный участок кода:**

```bsl
+ 	  
+ 	//Муртазин И. Ф. ORANGE start 2/4 13-04-2026 Задача 11281
+ 	//Обновление сопутствующих документов (ПоступлениеТоваров и СчётФактураПолученный) если отличается контрагент или дата
+ 	//Основная логика:
+ 	//Если контрагент изменён то ПоступлениеТоваров и СчётФактураПолученный удалаяются
+ 	//	
+ 	
+ 	
+ 	Если РольДоступна("ПолныеПрава") Тогда
+ 		
+ 		Если Не Отказ И НЕ ЭтоНовый() И Проведен Тогда
+ 			
+ 			ЗапросПоступленияТоваровИСчётаФактурыПолученного = Новый Запрос;
+ 			ЗапросПоступленияТоваровИСчётаФактурыПолученного.Текст = 
+ 			"ВЫБРАТЬ ПЕРВЫЕ 1
+ 			|	ПодчиненныеДокументы.Ссылка КАК Ссылка
+ 			|ИЗ
+ 			|	КритерийОтбора.ПодчиненныеДокументы(&документОснование) КАК ПодчиненныеДокументы
+ 			|ГДЕ
+ 			|	ТИПЗНАЧЕНИЯ(ПодчиненныеДокументы.Ссылка) = ТИП(Документ.ПоступлениеТоваров)
+ 			|	И ПодчиненныеДокументы.Ссылка.ХозОперация = ЗНАЧЕНИЕ(Справочник.ХозОперации.УслугиСтороннихОрганизаций)
+ 			|	И НЕ ПодчиненныеДокументы.Ссылка.ПометкаУдаления";
+ 			
+ 			ЗапросПоступленияТоваровИСчётаФактурыПолученного.УстановитьПараметр("ДокументОснование", Ссылка);
+ 			
+ 			ВыбРезультат = ЗапросПоступленияТоваровИСчётаФактурыПолученного.Выполнить().Выбрать();
+ 			
+ 			обПоступлениеТоваров = Неопределено;
+ 			обСчётФактураПолученный = Неопределено;
+ 			
+ 			Если ВыбРезультат.Следующий() Тогда
+ 				
+ 				обПоступлениеТоваров = ВыбРезультат.Ссылка.ПолучитьОбъект();
+ 				
+ 			КонецЕсли;
+ 			
+ 			ЗапросПоступленияТоваровИСчётаФактурыПолученного = Новый Запрос;
+ 			ЗапросПоступленияТоваровИСчётаФактурыПолученного.Текст = 
+ 			"ВЫБРАТЬ ПЕРВЫЕ 1
+ 			|	ПодчиненныеДокументы.Ссылка КАК Ссылка
+ 			|ИЗ
+ 			|	КритерийОтбора.ПодчиненныеДокументы(&документОснование) КАК ПодчиненныеДокументы
+ 			|ГДЕ
+ 			|	ТИПЗНАЧЕНИЯ(ПодчиненныеДокументы.Ссылка) = ТИП(Документ.СчетФактураПолученный)
+ 			|	И ПодчиненныеДокументы.Ссылка.ХозОперация = ЗНАЧЕНИЕ(Справочник.ХозОперации.УслугиСтороннихОрганизаций)
+ 			|	И НЕ ПодчиненныеДокументы.Ссылка.ПометкаУдаления";
+ 			
+ 			ЗапросПоступленияТоваровИСчётаФактурыПолученного.УстановитьПараметр("ДокументОснование", Ссылка);
+ 			
+ 			ВыбРезультат = ЗапросПоступленияТоваровИСчётаФактурыПолученного.Выполнить().Выбрать();
+ 			
+ 			
+ 			Если ВыбРезультат.Следующий() Тогда
+ 				
+ 				обСчётФактураПолученный = ВыбРезультат.Ссылка.ПолучитьОбъект();
+ 				
+ 			КонецЕсли;
+ 			
+ 			
+ 			Если флПлательщикИзменён Тогда
+ 				
+ 				Если Не обПоступлениеТоваров = Неопределено Тогда
+ 					
+ 					обПоступлениеТоваров.Записать(РежимЗаписиДокумента.ОтменаПроведения);
+ 					обПоступлениеТоваров.УстановитьПометкуУдаления(Истина);
+ 					
+ 				КонецЕсли; 
+ 				
+ 				Если Не обСчётФактураПолученный = Неопределено Тогда
+ 					
+ 					обСчётФактураПолученный.Записать(РежимЗаписиДокумента.ОтменаПроведения);
+ 					обСчётФактураПолученный.УстановитьПометкуУдаления(Истина);
+ 					
+ 				КонецЕсли;
+ 				
+ 			Иначе
+ 				
+ 				Если Не обПоступлениеТоваров = Неопределено Тогда
+ 					
+ 					ссНоменклатураУслугаРемонт = Справочники.Номенклатура.НайтиПоКоду("ЦБ017091"); // Ремонт, комплектация  автомобиля
// ... сокращено, всего строк: 141
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПриЗаписи`

**Код До:**

```bsl
		ДатаСтарта = РегистрыСведений.АвтосетьНастройки.ПолучитьЗначение("СОПД DNM", "ДатаСтарта");
		Если ЗначениеЗаполнено(ДатаСтарта) И ДатаСтарта < ТекущаяДата() Тогда
			#Если Клиент Тогда
				ПроверкаСОПД_DNM()
			#КонецЕсли
		КонецЕсли;
	КонецЕсли;
	//-Фомин Сергей 24.06.2025 btrx#2331
	
КонецПроцедуры

Процедура ПередУдалением(Отказ)
	Если НЕ дкПередУдалением(ЭтотОбъект, Отказ) Тогда Возврат; КонецЕсли;

КонецПроцедуры

Процедура ОбработкаУдаленияПроведения(Отказ)
	Если НЕ дкОбработкаУдаленияПроведения(ЭтотОбъект, Отказ) Тогда Возврат; КонецЕсли;
```

**Код После:**

```bsl
		
		
		
		
	КонецЕсли;
	
	//Муртазин И. Ф. ORANGE end
	
	
КонецПроцедуры


//Муртазин И. Ф. ORANGE start 3/4 13-04-2026 Задача 11281
//Проверяет отличия номенклатур, цен и количества.
Функция ЕстьРасхождения(аргТабЧПоступлениеТоваров)
		
	Если НЕ Товары.Количество() = аргТабЧПоступлениеТоваров.Количество() Тогда 
		Возврат Истина;
	КонецЕсли;
	
	Для каждого стрТабЧ Из аргТабЧПоступлениеТоваров Цикл
		
		найденнаяСтрока = Товары.Найти(стрТабЧ.Номенклатура, "Номенклатура");

		Если найденнаяСтрока = Неопределено Тогда
			
			Возврат Истина;
			
		Иначе
			
			Если (Не найденнаяСтрока.Количество = стрТабЧ.Количество) ИЛИ (Не найденнаяСтрока.Цена = стрТабЧ.Цена) Тогда
			
				Возврат Истина;
			
			КонецЕсли; 	
		
		КонецЕсли;
	
	КонецЦикла;
	

	Возврат Ложь;

КонецФункции // ЕстьРасхождения()



//Муртазин И. Ф. ORANGE end


Процедура ПередУдалением(Отказ)
	Если НЕ дкПередУдалением(ЭтотОбъект, Отказ) Тогда Возврат; КонецЕсли;

КонецПроцедуры

Процедура ОбработкаУдаленияПроведения(Отказ)
	Если НЕ дкОбработкаУдаленияПроведения(ЭтотОбъект, Отказ) Тогда Возврат; КонецЕсли;
```

**Данный участок кода:**

```bsl
+ 
+ 
+ //Муртазин И. Ф. ORANGE start 3/4 13-04-2026 Задача 11281
+ //Проверяет отличия номенклатур, цен и количества.
+ Функция ЕстьРасхождения(аргТабЧПоступлениеТоваров)
+ 		
+ 	Если НЕ Товары.Количество() = аргТабЧПоступлениеТоваров.Количество() Тогда 
+ 		Возврат Истина;
+ 	КонецЕсли;
+ 	
+ 	Для каждого стрТабЧ Из аргТабЧПоступлениеТоваров Цикл
+ 		
+ 		найденнаяСтрока = Товары.Найти(стрТабЧ.Номенклатура, "Номенклатура");
+ 
+ 		Если найденнаяСтрока = Неопределено Тогда
+ 			
+ 			Возврат Истина;
+ 			
+ 		Иначе
+ 			
+ 			Если (Не найденнаяСтрока.Количество = стрТабЧ.Количество) ИЛИ (Не найденнаяСтрока.Цена = стрТабЧ.Цена) Тогда
+ 			
+ 				Возврат Истина;
+ 			
+ 			КонецЕсли; 	
+ 		
+ 		КонецЕсли;
+ 	
+ 	КонецЦикла;
+ 	
+ 
+ 	Возврат Ложь;
+ 
+ КонецФункции // ЕстьРасхождения()
+ 
+ 
+ 
+ //Муртазин И. Ф. ORANGE end
+
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `СозданиеЗаказаПокупателяИПоставщику`

**Код До:**

```bsl
КэшОстатковНоменклатурыНаСкладе.Колонки.Добавить("ХарактеристикаНоменклатуры",ОписаниеТиповХарактеристикиНоменклатуры);
КэшОстатковНоменклатурыНаСкладе.Колонки.Добавить("Остаток",ОписаниеТиповЧисло);

ТоварыБезНДС = Ложь;
РаботыБезНДС = Ложь;
ЗакрыватьИнтервалыРабот = Ложь;
КэшПакетов = Неопределено;
```

**Код После:**

```bsl
КэшОстатковНоменклатурыНаСкладе.Колонки.Добавить("ХарактеристикаНоменклатуры",ОписаниеТиповХарактеристикиНоменклатуры);
КэшОстатковНоменклатурыНаСкладе.Колонки.Добавить("Остаток",ОписаниеТиповЧисло);

ТоварыБезНДС = Ложь;
РаботыБезНДС = Ложь;
ЗакрыватьИнтервалыРабот = Ложь;
КэшПакетов = Неопределено;        



флПлательщикИзменён = Ложь; //Муртазин И. Ф. ORANGE start 4/4 15-04-2026 Задача 11281
//Обновление сопутствующих документов (ПоступлениеТоваров и СчётФактураПолученный) если отличается контрагент или дата
```

**Данный участок кода:**

```bsl
- 
- 
+ флПлательщикИзменён = Ложь; //Муртазин И. Ф. ORANGE start 4/4 15-04-2026 Задача 11281
+ //Обновление сопутствующих документов (ПоступлениеТоваров и СчётФактураПолученный) если отличается контрагент или дата
+
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 13) Документ ИзвлечениеТоваровИзПроизводства — модуль объекта

Изменений в модуле не обнаружено после сравнения.

## 14) Документ ПоступлениеТоваров — модуль объекта

**Процедура:** `ОбработкаПроведения`

**Код До:**

```bsl
			Сообщить("Ошибка изменения заявки, " + ЗаявкаНаПлатеж.Номер + ": " + ОписаниеОшибки());
			Отказ = Истина;
		КонецПопытки;
	КонецЕсли;
	// - Виталий Немцев
	
КонецПроцедуры // ОбработкаУдаленияПроведения()

Процедура ОбработкаПроведения(Отказ, Режим)
	
	//Муртазин И. Ф. ORANGE start 25-03-2026 Задача 13759
	//Запрет проведения когда распределения между центрами финансовой ответственности (ЦФО) не сходятся.
	//смотри ПоступлениеТоваров.ФормаДокумента.ПанельРаспределениеИзЗаявокНаОплату(...) стр 1034
	
	Если РаспределениеПоЦФО.Количество() > 0 Тогда
		
		перСуммаЗаявок = РаспределениеПоЦФО.Итог("Сумма");
		
		перРазница = ?(СуммаДокумента > перСуммаЗаявок, СуммаДокумента - перСуммаЗаявок, перСуммаЗаявок - СуммаДокумента);
		
		Если перРазница > 0.001 Тогда
			
			//Предупреждение("Сумма документа и сумма заявок не сходятся. " + СуммаДокумента + " <> " + перСуммаЗаявок + " .", , "Внимание"); 
			Сообщить("Сумма документа и сумма заявок (вкладка ""Распределение ЦФО"") не сходятся. " + СуммаДокумента + " <> " + перСуммаЗаявок + " .");//Луковников 01.04.2026
			
			Отказ = Истина;
			
			Возврат;
			
		КонецЕсли;
		
		перСуммаПроцентов = РаспределениеПоЦФО.Итог("ПроцентРаспределения");
		
		Если Не перСуммаПроцентов = 100 Тогда
			
			//Предупреждение("Количество процентов в Распределении не равно 100!", , "Внимание");	
			Сообщить("Количество процентов в Распределении не равно 100!");//Луковников 01.04.2026	  
			
			Отказ = Истина;
			
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
	//Муртазин И. Ф. ORANGE end
	
	
	Если НЕ дкОбработкаПроведения(ЭтотОбъект, Отказ, Режим) Тогда Возврат; КонецЕсли;
	
	// определим необходимость формирования корректирующих проводок
	ВедетсяБалансПоПодразделению = (Константы.СпособВеденияБаланса.Получить() = Перечисления.СпособВеденияБаланса.ПоПодразделению);
	
	ПодразделениеСклад = обПолучитьБалансовоеПодразделение(СкладКомпании.Подразделение);

	ПодразделениеДоговорВзаиморасчетов = обПолучитьБалансовоеПодразделение(ДоговорВзаиморасчетов.Подразделение);
```

**Код После:**

```bsl
			Сообщить("Ошибка изменения заявки, " + ЗаявкаНаПлатеж.Номер + ": " + ОписаниеОшибки());
			Отказ = Истина;
		КонецПопытки;
	КонецЕсли;
	// - Виталий Немцев
	
КонецПроцедуры // ОбработкаУдаленияПроведения()

Процедура ОбработкаПроведения(Отказ, Режим)
	
	//Муртазин И. Ф. ORANGE comment 15-04-2026 Переместил свою проверку из обработки проведения в Форму поступления товаров ПередЗаписью(...) стр 933. 
	//Так как считаю это более правильным. Проверка корректности введённых полей должна проходить на форме. 
	//Плюс ещё дали задачу сделать менее жёсткой проверку на проценты.
	//Данила, если ты это читаешь, этот коммент специально для тебя. Можешь его удалить, так как он не несёт особой ценности.
	
	Если НЕ дкОбработкаПроведения(ЭтотОбъект, Отказ, Режим) Тогда Возврат; КонецЕсли;
	
	// определим необходимость формирования корректирующих проводок
	ВедетсяБалансПоПодразделению = (Константы.СпособВеденияБаланса.Получить() = Перечисления.СпособВеденияБаланса.ПоПодразделению);
	
	ПодразделениеСклад = обПолучитьБалансовоеПодразделение(СкладКомпании.Подразделение);

	ПодразделениеДоговорВзаиморасчетов = обПолучитьБалансовоеПодразделение(ДоговорВзаиморасчетов.Подразделение);
```

**Данный участок кода:**

```bsl
- 	//Муртазин И. Ф. ORANGE start 25-03-2026 Задача 13759
- 	//Запрет проведения когда распределения между центрами финансовой ответственности (ЦФО) не сходятся.
- 	//смотри ПоступлениеТоваров.ФормаДокумента.ПанельРаспределениеИзЗаявокНаОплату(...) стр 1034
- 	
- 	Если РаспределениеПоЦФО.Количество() > 0 Тогда
- 		
- 		перСуммаЗаявок = РаспределениеПоЦФО.Итог("Сумма");
- 		
- 		перРазница = ?(СуммаДокумента > перСуммаЗаявок, СуммаДокумента - перСуммаЗаявок, перСуммаЗаявок - СуммаДокумента);
- 		
- 		Если перРазница > 0.001 Тогда
- 			
- 			//Предупреждение("Сумма документа и сумма заявок не сходятся. " + СуммаДокумента + " <> " + перСуммаЗаявок + " .", , "Внимание"); 
- 			Сообщить("Сумма документа и сумма заявок (вкладка ""Распределение ЦФО"") не сходятся. " + СуммаДокумента + " <> " + перСуммаЗаявок + " .");//Луковников 01.04.2026
- 			
- 			Отказ = Истина;
- 			
- 			Возврат;
- 			
- 		КонецЕсли;
- 		
- 		перСуммаПроцентов = РаспределениеПоЦФО.Итог("ПроцентРаспределения");
- 		
- 		Если Не перСуммаПроцентов = 100 Тогда
- 			
- 			//Предупреждение("Количество процентов в Распределении не равно 100!", , "Внимание");	
- 			Сообщить("Количество процентов в Распределении не равно 100!");//Луковников 01.04.2026	  
- 			
- 			Отказ = Истина;
- 			
- 			Возврат;
- 			
- 		КонецЕсли;
- 		
- 	КонецЕсли;
- 	
- 	//Муртазин И. Ф. ORANGE end
- 	
+ 	//Муртазин И. Ф. ORANGE comment 15-04-2026 Переместил свою проверку из обработки проведения в Форму поступления товаров ПередЗаписью(...) стр 933. 
+ 	//Так как считаю это более правильным. Проверка корректности введённых полей должна проходить на форме. 
+ 	//Плюс ещё дали задачу сделать менее жёсткой проверку на проценты.
+ 	//Данила, если ты это читаешь, этот коммент специально для тебя. Можешь его удалить, так как он не несёт особой ценности.
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

## 15) Отчет СводныйАнализПродажАвтомобилей — модуль объекта

**Процедура:** `ПриКомпоновкеРезультата`

**Код До:**

```bsl
		
	//Муртазин И. Ф. ORANGE end	

	
	
	Суффикс = ПолучитьСуффиксЭлемента(ДокументРезультат.Имя);

	Если Суффикс = "_ОФУ" Тогда
		ВывестиМакет_ОФУ(ДокументРезультат);
		
	Иначе
		ВывестиМакет(ДокументРезультат);
	КонецЕсли;
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

Процедура ПолучитьОсновныеПараметры()
	Если НЕ Ручной Тогда
```

**Код После:**

```bsl
		
	//Муртазин И. Ф. ORANGE end	

	
	
	Суффикс = ПолучитьСуффиксЭлемента(ДокументРезультат.Имя);

	Если Суффикс = "_ОФУ" Тогда
		ВывестиМакет_ОФУ(ДокументРезультат);
		
	Иначе     
		ВывестиМакет(ДокументРезультат);
	КонецЕсли;
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

Процедура ПолучитьОсновныеПараметры()
	Если НЕ Ручной Тогда
```

**Данный участок кода:**

```bsl
- 	Иначе
+ 	Иначе
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|			ТОГДА 0
		|		ИНАЧЕ ЕСТЬNULL(ТабБонусныхВозмещений.СуммаБонусныхВозмещений, 0)
		|	КОНЕЦ КАК СуммаБонусныхВозмещений,
		|	КатегорияАвтомобиля.Значение КАК КатегорияАвтомобиля,
		|	Автомобили.ПокупательФормаСобственности КАК ПокупательФормаСобственности,
		|	ТабДопов.ЗаказНарядКонтрагент КАК ЗаказНарядКонтрагент,
		|	Автомобили.НоменклатураДопРасходов КАК НоменклатураДопРасходов,
		|	Автомобили.СуммаДопРасходов КАК СуммаДопРасходов,
		|	ТабКВЛизинг.СуммаКВЛизингПлан КАК СуммаКВЛизингПлан,
		|	ТабКВЛизинг.СуммаКВЛизингФакт КАК СуммаКВЛизингФакт,
		|	ЛОЖЬ КАК РазницаПрошлогоПериода
		|ПОМЕСТИТЬ ГотовыеДанные
		|ИЗ
		|	Автомобили КАК Автомобили
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			Автомобили.ДокументПродажи КАК ДокументПродажи,
		|			СУММА(ОстаткиАвтомобилей.Сумма * -1 * Автомобили.КоличествоПродажи) КАК СуммаКорректировокВозмещенийФакт,
		|			Автомобили.Автомобиль КАК Автомобиль
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрНакопления.ОстаткиАвтомобилей КАК ОстаткиАвтомобилей
```

**Код После:**

```bsl
		|			ТОГДА 0
		|		ИНАЧЕ ЕСТЬNULL(ТабБонусныхВозмещений.СуммаБонусныхВозмещений, 0)
		|	КОНЕЦ КАК СуммаБонусныхВозмещений,
		|	КатегорияАвтомобиля.Значение КАК КатегорияАвтомобиля,
		|	Автомобили.ПокупательФормаСобственности КАК ПокупательФормаСобственности,
		|	ТабДопов.ЗаказНарядКонтрагент КАК ЗаказНарядКонтрагент,
		|	Автомобили.НоменклатураДопРасходов КАК НоменклатураДопРасходов,
		|	Автомобили.СуммаДопРасходов КАК СуммаДопРасходов,
		|	ТабКВЛизинг.СуммаКВЛизингПлан КАК СуммаКВЛизингПлан,
		|	ТабКВЛизинг.СуммаКВЛизингФакт КАК СуммаКВЛизингФакт,
		|	ЛОЖЬ КАК РазницаПрошлогоПериода,
		|	ТабДопов.ЭтоНаши КАК ЭтоНаши
		|ПОМЕСТИТЬ ГотовыеДанные
		|ИЗ
		|	Автомобили КАК Автомобили
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			Автомобили.ДокументПродажи КАК ДокументПродажи,
		|			СУММА(ОстаткиАвтомобилей.Сумма * -1 * Автомобили.КоличествоПродажи) КАК СуммаКорректировокВозмещенийФакт,
		|			Автомобили.Автомобиль КАК Автомобиль
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрНакопления.ОстаткиАвтомобилей КАК ОстаткиАвтомобилей
```

**Данный участок кода:**

```bsl
- 		|	ЛОЖЬ КАК РазницаПрошлогоПериода
+ 		|	ЛОЖЬ КАК РазницаПрошлогоПериода,
+ 		|	ТабДопов.ЭтоНаши КАК ЭтоНаши
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|		ПО Автомобили.ДокументПродажи = ТабПодарков.ДокументПродажи
		|			И Автомобили.Автомобиль = ТабПодарков.Автомобиль
		|		ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ЕСТЬNULL(Автомобили.ДокументПродажи, ТабДоповОбщая.ДокументПродажи) КАК ДокументПродажи,
		|			ЕСТЬNULL(Автомобили.Автомобиль, ТабДоповОбщая.Автомобиль) КАК Автомобиль,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.МаржаСДопов, 0)) КАК МаржаСДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СуммаДопов, 0)) КАК СуммаДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|			МАКСИМУМ(ЕСТЬNULL(ТабДоповОбщая.ДатаЗакрытияЗаказНаряда, Автомобили.ДатаПродажи)) КАК ДатаПродажи,
		|			Автомобили.РабочийЛистОП.ТимЛидер КАК РабочийЛистОПТимЛидер,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|					ТабТоваров.ДокументПродажи КАК ДокументПродажи,
		|					ТабТоваров.Автомобиль КАК Автомобиль,
		|					СУММА(ТабТоваров.СуммаДопов - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
		|					СУММА(ТабТоваров.СуммаДопов) КАК СуммаДопов,
		|					СУММА(ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|					ТабТоваров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|					ТабТоваров.ЗаказНарядКонтрагент КАК ЗаказНарядКонтрагент
```

**Код После:**

```bsl
		|		ПО Автомобили.ДокументПродажи = ТабПодарков.ДокументПродажи
		|			И Автомобили.Автомобиль = ТабПодарков.Автомобиль
		|		ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ЕСТЬNULL(Автомобили.ДокументПродажи, ТабДоповОбщая.ДокументПродажи) КАК ДокументПродажи,
		|			ЕСТЬNULL(Автомобили.Автомобиль, ТабДоповОбщая.Автомобиль) КАК Автомобиль,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.МаржаСДопов, 0)) КАК МаржаСДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СуммаДопов, 0)) КАК СуммаДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|			МАКСИМУМ(ЕСТЬNULL(ТабДоповОбщая.ДатаЗакрытияЗаказНаряда, Автомобили.ДатаПродажи)) КАК ДатаПродажи,
		|			Автомобили.РабочийЛистОП.ТимЛидер КАК РабочийЛистОПТимЛидер,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент.Родитель = &НашиОрганизации) КАК ЭтоНаши
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|					ВЫБОР
		|						КОГДА ТабТоваров.ДокументПродажи ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.ДокументПродажи
		|						ИНАЧЕ ТабТоваров.ДокументПродажи
		|					КОНЕЦ КАК ДокументПродажи,
		|					ВЫБОР
		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
```

**Данный участок кода:**

```bsl
- 		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент
+ 		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент,
+ 		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент.Родитель = &НашиОрганизации) КАК ЭтоНаши
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|			ЕСТЬNULL(Автомобили.Автомобиль, ТабДоповОбщая.Автомобиль) КАК Автомобиль,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.МаржаСДопов, 0)) КАК МаржаСДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СуммаДопов, 0)) КАК СуммаДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|			МАКСИМУМ(ЕСТЬNULL(ТабДоповОбщая.ДатаЗакрытияЗаказНаряда, Автомобили.ДатаПродажи)) КАК ДатаПродажи,
		|			Автомобили.РабочийЛистОП.ТимЛидер КАК РабочийЛистОПТимЛидер,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|					ТабТоваров.ДокументПродажи КАК ДокументПродажи,
		|					ТабТоваров.Автомобиль КАК Автомобиль,
		|					СУММА(ТабТоваров.СуммаДопов - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
		|					СУММА(ТабТоваров.СуммаДопов) КАК СуммаДопов,
		|					СУММА(ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|					ТабТоваров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|					ТабТоваров.ЗаказНарядКонтрагент КАК ЗаказНарядКонтрагент
		|				ИЗ
		|					(ВЫБРАТЬ
		|						ВыработкаДиспетчеров.ЗаказНаряд КАК ДокументПродажи,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|						СУММА(ВыработкаДиспетчеров.СуммаЗаказНаряда) КАК СуммаДопов,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент КАК ЗаказНарядКонтрагент
```

**Код После:**

```bsl
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.МаржаСДопов, 0)) КАК МаржаСДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СуммаДопов, 0)) КАК СуммаДопов,
		|			СУММА(ЕСТЬNULL(ТабДоповОбщая.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|			МАКСИМУМ(ЕСТЬNULL(ТабДоповОбщая.ДатаЗакрытияЗаказНаряда, Автомобили.ДатаПродажи)) КАК ДатаПродажи,
		|			Автомобили.РабочийЛистОП.ТимЛидер КАК РабочийЛистОПТимЛидер,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент.Родитель = &НашиОрганизации) КАК ЭтоНаши
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|					ВЫБОР
		|						КОГДА ТабТоваров.ДокументПродажи ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.ДокументПродажи
		|						ИНАЧЕ ТабТоваров.ДокументПродажи
		|					КОНЕЦ КАК ДокументПродажи,
		|					ВЫБОР
		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.Автомобиль
		|						ИНАЧЕ ТабТоваров.Автомобиль
		|					КОНЕЦ КАК Автомобиль,
		|					СУММА(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0) - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
		|					СУММА(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0)) КАК СуммаДопов,
		|					СУММА(ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|					ЕСТЬNULL(ТабТоваров.ДатаЗакрытияЗаказНаряда, ТабСебестоимости.ДокументПродажи.Дата) КАК ДатаЗакрытияЗаказНаряда,
		|					ЕСТЬNULL(ТабТоваров.ЗаказНарядКонтрагент, ТабСебестоимости.ДокументПродажи.Контрагент) КАК ЗаказНарядКонтрагент
		|				ИЗ
		|					(ВЫБРАТЬ
		|						ВыработкаДиспетчеров.ЗаказНаряд КАК ДокументПродажи,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|						СУММА(ВыработкаДиспетчеров.СуммаЗаказНаряда) КАК СуммаДопов,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент КАК ЗаказНарядКонтрагент
```

**Данный участок кода:**

```bsl
- 		|					ТабТоваров.ДокументПродажи КАК ДокументПродажи,
- 		|					ТабТоваров.Автомобиль КАК Автомобиль,
- 		|					СУММА(ТабТоваров.СуммаДопов - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
- 		|					СУММА(ТабТоваров.СуммаДопов) КАК СуммаДопов,
+ 		|					ВЫБОР
+ 		|						КОГДА ТабТоваров.ДокументПродажи ЕСТЬ NULL
+ 		|							ТОГДА ТабСебестоимости.ДокументПродажи
+ 		|						ИНАЧЕ ТабТоваров.ДокументПродажи
+ 		|					КОНЕЦ КАК ДокументПродажи,
+ 		|					ВЫБОР
+ 		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
+ 		|							ТОГДА ТабСебестоимости.Автомобиль
+ 		|						ИНАЧЕ ТабТоваров.Автомобиль
+ 		|					КОНЕЦ КАК Автомобиль,
+ 		|					СУММА(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0) - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
+ 		|					СУММА(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0)) КАК СуммаДопов,
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|			Автомобили.РабочийЛистОП.ТимЛидер КАК РабочийЛистОПТимЛидер,
		|			МИНИМУМ(ТабДоповОбщая.ЗаказНарядКонтрагент) КАК ЗаказНарядКонтрагент
		|		ИЗ
		|			Автомобили КАК Автомобили
		|				ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|					ТабТоваров.ДокументПродажи КАК ДокументПродажи,
		|					ТабТоваров.Автомобиль КАК Автомобиль,
		|					СУММА(ТабТоваров.СуммаДопов - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
		|					СУММА(ТабТоваров.СуммаДопов) КАК СуммаДопов,
		|					СУММА(ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|					ТабТоваров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|					ТабТоваров.ЗаказНарядКонтрагент КАК ЗаказНарядКонтрагент
		|				ИЗ
		|					(ВЫБРАТЬ
		|						ВыработкаДиспетчеров.ЗаказНаряд КАК ДокументПродажи,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|						СУММА(ВыработкаДиспетчеров.СуммаЗаказНаряда) КАК СуммаДопов,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент КАК ЗаказНарядКонтрагент
		|					ИЗ
		|						РегистрНакопления.ВыработкаДиспетчеров КАК ВыработкаДиспетчеров
		|					ГДЕ
```

**Код После:**

```bsl
		|						ИНАЧЕ ТабТоваров.ДокументПродажи
		|					КОНЕЦ КАК ДокументПродажи,
		|					ВЫБОР
		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.Автомобиль
		|						ИНАЧЕ ТабТоваров.Автомобиль
		|					КОНЕЦ КАК Автомобиль,
		|					СУММА(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0) - ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК МаржаСДопов,
		|					СУММА(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0)) КАК СуммаДопов,
		|					СУММА(ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0)) КАК СебестоимостьДопов,
		|					ЕСТЬNULL(ТабТоваров.ДатаЗакрытияЗаказНаряда, ТабСебестоимости.ДокументПродажи.Дата) КАК ДатаЗакрытияЗаказНаряда,
		|					ЕСТЬNULL(ТабТоваров.ЗаказНарядКонтрагент, ТабСебестоимости.ДокументПродажи.Контрагент) КАК ЗаказНарядКонтрагент
		|				ИЗ
		|					(ВЫБРАТЬ
		|						ВыработкаДиспетчеров.ЗаказНаряд КАК ДокументПродажи,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
		|						СУММА(ВыработкаДиспетчеров.СуммаЗаказНаряда) КАК СуммаДопов,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент КАК ЗаказНарядКонтрагент
		|					ИЗ
		|						РегистрНакопления.ВыработкаДиспетчеров КАК ВыработкаДиспетчеров
		|					ГДЕ
```

**Данный участок кода:**

```bsl
- 		|					ТабТоваров.ДатаЗакрытияЗаказНаряда КАК ДатаЗакрытияЗаказНаряда,
- 		|					ТабТоваров.ЗаказНарядКонтрагент КАК ЗаказНарядКонтрагент
+ 		|					ЕСТЬNULL(ТабТоваров.ДатаЗакрытияЗаказНаряда, ТабСебестоимости.ДокументПродажи.Дата) КАК ДатаЗакрытияЗаказНаряда,
+ 		|					ЕСТЬNULL(ТабТоваров.ЗаказНарядКонтрагент, ТабСебестоимости.ДокументПродажи.Контрагент) КАК ЗаказНарядКонтрагент
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|								КОГДА &СкладыПродажиПустые
		|									ТОГДА ИСТИНА
		|								ИНАЧЕ ВыработкаДиспетчеров.ЗаказНаряд.СкладРеализацииАвтомобиля В ИЕРАРХИИ (&СкладыПродажи)
		|							КОНЕЦ
		|						И НЕ ВыработкаДиспетчеров.ЗаказНаряд.Контрагент.НеВключатьВОтчет
		|						И ВЫБОР
		|								КОГДА &АвтомобилиПустые
		|									ТОГДА ИСТИНА
		|								ИНАЧЕ ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль В (&Автомобили)
		|							КОНЕЦ
		|					
		|					СГРУППИРОВАТЬ ПО
		|						ВыработкаДиспетчеров.ЗаказНаряд,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент) КАК ТабТоваров
		|						ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|							ТоварыВПроизводстве.ЗаказНаряд КАК ДокументПродажи,
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|							СУММА(ТоварыВПроизводстве.Сумма) КАК СебестоимостьДопов
```

**Код После:**

```bsl
		|								КОГДА &СкладыПродажиПустые
		|									ТОГДА ИСТИНА
		|								ИНАЧЕ ВыработкаДиспетчеров.ЗаказНаряд.СкладРеализацииАвтомобиля В ИЕРАРХИИ (&СкладыПродажи)
		|							КОНЕЦ
		|						И НЕ ВыработкаДиспетчеров.ЗаказНаряд.Контрагент.НеВключатьВОтчет
		|						И ВЫБОР
		|								КОГДА &АвтомобилиПустые
		|									ТОГДА ИСТИНА
		|								ИНАЧЕ ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль В (&Автомобили)
		|							КОНЕЦ
		|						И НЕ ВыработкаДиспетчеров.ЗаказНаряд.Контрагент В ИЕРАРХИИ (&НашиОрганизации)
		|					
		|					СГРУППИРОВАТЬ ПО
		|						ВыработкаДиспетчеров.ЗаказНаряд,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент) КАК ТабТоваров
		|						ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|							ТоварыВПроизводстве.ЗаказНаряд КАК ДокументПродажи,
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|							СУММА(ТоварыВПроизводстве.Сумма) КАК СебестоимостьДопов
```

**Данный участок кода:**

```bsl
+ 		|						И НЕ ВыработкаДиспетчеров.ЗаказНаряд.Контрагент В ИЕРАРХИИ (&НашиОрганизации)
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|								КОГДА &АвтомобилиПустые
		|									ТОГДА ИСТИНА
		|								ИНАЧЕ ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль В (&Автомобили)
		|							КОНЕЦ
		|					
		|					СГРУППИРОВАТЬ ПО
		|						ВыработкаДиспетчеров.ЗаказНаряд,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент) КАК ТабТоваров
		|						ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|							ТоварыВПроизводстве.ЗаказНаряд КАК ДокументПродажи,
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|							СУММА(ТоварыВПроизводстве.Сумма) КАК СебестоимостьДопов
		|						ИЗ
		|							РегистрНакопления.ТоварыВПроизводстве КАК ТоварыВПроизводстве
		|						ГДЕ
		|							ТоварыВПроизводстве.Период МЕЖДУ &ДатаНачала И &ДатаОкончания
		|							И ТоварыВПроизводстве.ЗаказНаряд.ВидРемонта = ЗНАЧЕНИЕ(Справочник.ВидыРемонта.ДополнительноеОборудование)
		|							И ТИПЗНАЧЕНИЯ(ТоварыВПроизводстве.Регистратор) = ТИП(Документ.ЗаказНаряд)
		|							И НЕ ТоварыВПроизводстве.ЗаказНаряд.Контрагент.НеВключатьВОтчет
```

**Код После:**

```bsl
		|									ТОГДА ИСТИНА
		|								ИНАЧЕ ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль В (&Автомобили)
		|							КОНЕЦ
		|						И НЕ ВыработкаДиспетчеров.ЗаказНаряд.Контрагент В ИЕРАРХИИ (&НашиОрганизации)
		|					
		|					СГРУППИРОВАТЬ ПО
		|						ВыработкаДиспетчеров.ЗаказНаряд,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Автомобиль,
		|						ВыработкаДиспетчеров.ДатаЗакрытияЗаказНаряда,
		|						ВыработкаДиспетчеров.ЗаказНаряд.Контрагент) КАК ТабТоваров
		|						ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|							ТоварыВПроизводстве.ЗаказНаряд КАК ДокументПродажи,
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль КАК Автомобиль,
		|							СУММА(ТоварыВПроизводстве.Сумма) КАК СебестоимостьДопов
		|						ИЗ
		|							РегистрНакопления.ТоварыВПроизводстве КАК ТоварыВПроизводстве
		|						ГДЕ
		|							ТоварыВПроизводстве.Период МЕЖДУ &ДатаНачала И &ДатаОкончания
		|							И ТоварыВПроизводстве.ЗаказНаряд.ВидРемонта = ЗНАЧЕНИЕ(Справочник.ВидыРемонта.ДополнительноеОборудование)
		|							И ТИПЗНАЧЕНИЯ(ТоварыВПроизводстве.Регистратор) = ТИП(Документ.ЗаказНаряд)
		|							И НЕ ТоварыВПроизводстве.ЗаказНаряд.Контрагент.НеВключатьВОтчет
```

**Данный участок кода:**

```bsl
- 		|						ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
+ 		|						ПОЛНОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|									КОГДА &СкладыПродажиПустые
		|										ТОГДА ИСТИНА
		|									ИНАЧЕ ТоварыВПроизводстве.ЗаказНаряд.СкладРеализацииАвтомобиля В ИЕРАРХИИ (&СкладыПродажи)
		|								КОНЕЦ
		|						
		|						СГРУППИРОВАТЬ ПО
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль,
		|							ТоварыВПроизводстве.ЗаказНаряд) КАК ТабСебестоимости
		|						ПО ТабТоваров.ДокументПродажи = ТабСебестоимости.ДокументПродажи
		|				ГДЕ
		|					ТабТоваров.СуммаДопов <> 0
		|				
		|				СГРУППИРОВАТЬ ПО
		|					ТабТоваров.ДокументПродажи,
		|					ТабТоваров.Автомобиль,
		|					ТабТоваров.ДатаЗакрытияЗаказНаряда,
		|					ТабТоваров.ЗаказНарядКонтрагент) КАК ТабДоповОбщая
		|				ПО Автомобили.Автомобиль = ТабДоповОбщая.Автомобиль
		|		
		|		СГРУППИРОВАТЬ ПО
		|			ЕСТЬNULL(Автомобили.ДокументПродажи, ТабДоповОбщая.ДокументПродажи),
```

**Код После:**

```bsl
		|									КОГДА &СкладыПродажиПустые
		|										ТОГДА ИСТИНА
		|									ИНАЧЕ ТоварыВПроизводстве.ЗаказНаряд.СкладРеализацииАвтомобиля В ИЕРАРХИИ (&СкладыПродажи)
		|								КОНЕЦ
		|						
		|						СГРУППИРОВАТЬ ПО
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль,
		|							ТоварыВПроизводстве.ЗаказНаряд) КАК ТабСебестоимости
		|						ПО ТабТоваров.ДокументПродажи = ТабСебестоимости.ДокументПродажи
		|				ГДЕ
		|					(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0) <> 0
		|							ИЛИ ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0) <> 0)
		|				
		|				СГРУППИРОВАТЬ ПО
		|					ВЫБОР
		|						КОГДА ТабТоваров.ДокументПродажи ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.ДокументПродажи
		|						ИНАЧЕ ТабТоваров.ДокументПродажи
		|					КОНЕЦ,
		|					ВЫБОР
		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.Автомобиль
```

**Данный участок кода:**

```bsl
- 		|					ТабТоваров.СуммаДопов <> 0
+ 		|					(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0) <> 0
+ 		|							ИЛИ ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0) <> 0)
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|								КОНЕЦ
		|						
		|						СГРУППИРОВАТЬ ПО
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль,
		|							ТоварыВПроизводстве.ЗаказНаряд) КАК ТабСебестоимости
		|						ПО ТабТоваров.ДокументПродажи = ТабСебестоимости.ДокументПродажи
		|				ГДЕ
		|					ТабТоваров.СуммаДопов <> 0
		|				
		|				СГРУППИРОВАТЬ ПО
		|					ТабТоваров.ДокументПродажи,
		|					ТабТоваров.Автомобиль,
		|					ТабТоваров.ДатаЗакрытияЗаказНаряда,
		|					ТабТоваров.ЗаказНарядКонтрагент) КАК ТабДоповОбщая
		|				ПО Автомобили.Автомобиль = ТабДоповОбщая.Автомобиль
		|		
		|		СГРУППИРОВАТЬ ПО
		|			ЕСТЬNULL(Автомобили.ДокументПродажи, ТабДоповОбщая.ДокументПродажи),
		|			ЕСТЬNULL(Автомобили.Автомобиль, ТабДоповОбщая.Автомобиль),
		|			Автомобили.РабочийЛистОП.ТимЛидер) КАК ТабДопов
		|		ПО Автомобили.ДокументПродажи = ТабДопов.ДокументПродажи
		|			И Автомобили.Автомобиль = ТабДопов.Автомобиль
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			Автомобили.ДокументПродажи КАК ДокументПродажи,
```

**Код После:**

```bsl
		|						
		|						СГРУППИРОВАТЬ ПО
		|							ТоварыВПроизводстве.ЗаказНаряд.Автомобиль,
		|							ТоварыВПроизводстве.ЗаказНаряд) КАК ТабСебестоимости
		|						ПО ТабТоваров.ДокументПродажи = ТабСебестоимости.ДокументПродажи
		|				ГДЕ
		|					(ЕСТЬNULL(ТабТоваров.СуммаДопов, 0) <> 0
		|							ИЛИ ЕСТЬNULL(ТабСебестоимости.СебестоимостьДопов, 0) <> 0)
		|				
		|				СГРУППИРОВАТЬ ПО
		|					ВЫБОР
		|						КОГДА ТабТоваров.ДокументПродажи ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.ДокументПродажи
		|						ИНАЧЕ ТабТоваров.ДокументПродажи
		|					КОНЕЦ,
		|					ВЫБОР
		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
		|							ТОГДА ТабСебестоимости.Автомобиль
		|						ИНАЧЕ ТабТоваров.Автомобиль
		|					КОНЕЦ,
		|					ЕСТЬNULL(ТабТоваров.ЗаказНарядКонтрагент, ТабСебестоимости.ДокументПродажи.Контрагент),
		|					ЕСТЬNULL(ТабТоваров.ДатаЗакрытияЗаказНаряда, ТабСебестоимости.ДокументПродажи.Дата)) КАК ТабДоповОбщая
		|				ПО Автомобили.Автомобиль = ТабДоповОбщая.Автомобиль
		|		
		|		СГРУППИРОВАТЬ ПО
		|			ЕСТЬNULL(Автомобили.ДокументПродажи, ТабДоповОбщая.ДокументПродажи),
		|			ЕСТЬNULL(Автомобили.Автомобиль, ТабДоповОбщая.Автомобиль),
		|			Автомобили.РабочийЛистОП.ТимЛидер) КАК ТабДопов
		|		ПО Автомобили.ДокументПродажи = ТабДопов.ДокументПродажи
		|			И Автомобили.Автомобиль = ТабДопов.Автомобиль
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			Автомобили.ДокументПродажи КАК ДокументПродажи,
```

**Данный участок кода:**

```bsl
- 		|					ТабТоваров.ДокументПродажи,
- 		|					ТабТоваров.Автомобиль,
- 		|					ТабТоваров.ДатаЗакрытияЗаказНаряда,
- 		|					ТабТоваров.ЗаказНарядКонтрагент) КАК ТабДоповОбщая
+ 		|					ВЫБОР
+ 		|						КОГДА ТабТоваров.ДокументПродажи ЕСТЬ NULL
+ 		|							ТОГДА ТабСебестоимости.ДокументПродажи
+ 		|						ИНАЧЕ ТабТоваров.ДокументПродажи
+ 		|					КОНЕЦ,
+ 		|					ВЫБОР
+ 		|						КОГДА ТабТоваров.Автомобиль ЕСТЬ NULL
+ 		|							ТОГДА ТабСебестоимости.Автомобиль
+ 		|						ИНАЧЕ ТабТоваров.Автомобиль
+ 		|					КОНЕЦ,
+ 		|					ЕСТЬNULL(ТабТоваров.ЗаказНарядКонтрагент, ТабСебестоимости.ДокументПродажи.Контрагент),
+ 		|					ЕСТЬNULL(ТабТоваров.ДатаЗакрытияЗаказНаряда, ТабСебестоимости.ДокументПродажи.Дата)) КАК ТабДоповОбщая
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|	КредитыКВ.Агент,
		|	0,
		|	0,
		|	NULL,
		|	ВЫРАЗИТЬ(ПроданныеЗаПрошлыйМесяцПолныеДанные.ДокументПродажи КАК Документ.РеализацияАвтомобилей).Контрагент.ФормаСобственности,
		|	NULL,
		|	0,
		|	0,
		|	0,
		|	0,
		|	ИСТИНА
		|ИЗ
		|	ПроданныеЗаПрошлыйМесяцПолныеДанные КАК ПроданныеЗаПрошлыйМесяцПолныеДанные
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ПроданныеЗаПрошлыйМесяцПолныеДанные.Автомобиль КАК Автомобиль,
		|			ПроданныеЗаПрошлыйМесяцПолныеДанные.ДокументПродажи КАК ДокументПродажи,
		|			СУММА(ЕСТЬNULL(РабочийЛистКредитногоОтдела.СуммаКомиссииФакт - РабочийЛистКредитногоОтдела.СуммаКомиссии, 0)) КАК РазницаКВ,
		|			РабочийЛистКредитногоОтдела.Банк КАК Банк,
		|			РабочийЛистКредитногоОтдела.Автор КАК Агент
		|		ИЗ
		|			ПроданныеЗаПрошлыйМесяцПолныеДанные КАК ПроданныеЗаПрошлыйМесяцПолныеДанные
```

**Код После:**

```bsl
		|	КредитыКВ.Агент,
		|	0,
		|	0,
		|	NULL,
		|	ВЫРАЗИТЬ(ПроданныеЗаПрошлыйМесяцПолныеДанные.ДокументПродажи КАК Документ.РеализацияАвтомобилей).Контрагент.ФормаСобственности,
		|	NULL,
		|	0,
		|	0,
		|	0,
		|	0,
		|	ИСТИНА,
		|	NULL
		|ИЗ
		|	ПроданныеЗаПрошлыйМесяцПолныеДанные КАК ПроданныеЗаПрошлыйМесяцПолныеДанные
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ПроданныеЗаПрошлыйМесяцПолныеДанные.Автомобиль КАК Автомобиль,
		|			ПроданныеЗаПрошлыйМесяцПолныеДанные.ДокументПродажи КАК ДокументПродажи,
		|			СУММА(ЕСТЬNULL(РабочийЛистКредитногоОтдела.СуммаКомиссииФакт - РабочийЛистКредитногоОтдела.СуммаКомиссии, 0)) КАК РазницаКВ,
		|			РабочийЛистКредитногоОтдела.Банк КАК Банк,
		|			РабочийЛистКредитногоОтдела.Автор КАК Агент
		|		ИЗ
		|			ПроданныеЗаПрошлыйМесяцПолныеДанные КАК ПроданныеЗаПрошлыйМесяцПолныеДанные
```

**Данный участок кода:**

```bsl
- 		|	ИСТИНА
+ 		|	ИСТИНА,
+ 		|	NULL
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК НаценкаСПлановымВозмещением,
		|	ГотовыеДанные.СуммаКВКредитПлан КАК СуммаКВКредитПлан,
		|	ГотовыеДанные.СуммаКВКредитФакт КАК СуммаКВКредитФакт,
		|	ГотовыеДанные.СуммаКВСтрахованияПлан КАК СуммаКВСтрахованияПлан,
		|	ГотовыеДанные.СуммаКВСтрахованияФакт КАК СуммаКВСтрахованияФакт,
		|	ГотовыеДанные.СуммаКВКредитПлан + ГотовыеДанные.СуммаКВСтрахованияПлан КАК СуммаКВИтогоПлан,
		|	ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт КАК СуммаКВИтогоФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЗаказНарядКонтрагент В ИЕРАРХИИ (&НашиОрганизации)
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|		ИНАЧЕ ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаКВКредитПлан + ГотовыеДанные.СуммаКВСтрахованияПлан + ГотовыеДанные.ПрочиеДоходы + ГотовыеДанные.СуммаБонусныхВозмещений - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|	КОНЕЦ КАК МаржаПлан,
		|	ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийФакт + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингФакт, 0) КАК МаржаФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЗаказНарядКонтрагент В ИЕРАРХИИ (&НашиОрганизации)
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)
		|		ИНАЧЕ ВЫБОР
		|				КОГДА ГотовыеДанные.ТимЛидер.Наименование ЕСТЬ NULL
```

**Код После:**

```bsl
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК НаценкаСПлановымВозмещением,
		|	ГотовыеДанные.СуммаКВКредитПлан КАК СуммаКВКредитПлан,
		|	ГотовыеДанные.СуммаКВКредитФакт КАК СуммаКВКредитФакт,
		|	ГотовыеДанные.СуммаКВСтрахованияПлан КАК СуммаКВСтрахованияПлан,
		|	ГотовыеДанные.СуммаКВСтрахованияФакт КАК СуммаКВСтрахованияФакт,
		|	ГотовыеДанные.СуммаКВКредитПлан + ГотовыеДанные.СуммаКВСтрахованияПлан КАК СуммаКВИтогоПлан,
		|	ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт КАК СуммаКВИтогоФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЭтоНаши
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|		ИНАЧЕ ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаКВКредитПлан + ГотовыеДанные.СуммаКВСтрахованияПлан + ГотовыеДанные.ПрочиеДоходы + ГотовыеДанные.СуммаБонусныхВозмещений - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|	КОНЕЦ КАК МаржаПлан,
		|	ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийФакт + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингФакт, 0) КАК МаржаФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЭтоНаши
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)
		|		ИНАЧЕ ВЫБОР
		|				КОГДА ГотовыеДанные.ТимЛидер.Наименование ЕСТЬ NULL
```

**Данный участок кода:**

```bsl
- 		|				И ГотовыеДанные.ЗаказНарядКонтрагент В ИЕРАРХИИ (&НашиОрганизации)
+ 		|				И ГотовыеДанные.ЭтоНаши
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.

**Процедура:** `ПолучитьТекстЗапроса`

**Код До:**

```bsl
		|	ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт КАК СуммаКВИтогоФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЗаказНарядКонтрагент В ИЕРАРХИИ (&НашиОрганизации)
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|		ИНАЧЕ ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаКВКредитПлан + ГотовыеДанные.СуммаКВСтрахованияПлан + ГотовыеДанные.ПрочиеДоходы + ГотовыеДанные.СуммаБонусныхВозмещений - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|	КОНЕЦ КАК МаржаПлан,
		|	ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийФакт + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингФакт, 0) КАК МаржаФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЗаказНарядКонтрагент В ИЕРАРХИИ (&НашиОрганизации)
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)
		|		ИНАЧЕ ВЫБОР
		|				КОГДА ГотовыеДанные.ТимЛидер.Наименование ЕСТЬ NULL
		|					ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаБонусныхВозмещений + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)
		|				ИНАЧЕ (ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаБонусныхВозмещений + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)) / 2
		|			КОНЕЦ
		|	КОНЕЦ КАК МаржаДляЗП,
		|	ГотовыеДанные.ОсновнойTradeIn КАК ОсновнойTradeIn,
		|	ГотовыеДанные.ВидВыкупа КАК ВидВыкупа,
		|	ГотовыеДанные.Клиент.РекламныйИсточник КАК ИсточникКлиента,
```

**Код После:**

```bsl
		|	ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт КАК СуммаКВИтогоФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЭтоНаши
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|		ИНАЧЕ ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаКВКредитПлан + ГотовыеДанные.СуммаКВСтрахованияПлан + ГотовыеДанные.ПрочиеДоходы + ГотовыеДанные.СуммаБонусныхВозмещений - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингПлан, 0)
		|	КОНЕЦ КАК МаржаПлан,
		|	ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийФакт + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0) + ЕСТЬNULL(ГотовыеДанные.СуммаКВЛизингФакт, 0) КАК МаржаФакт,
		|	ВЫБОР
		|		КОГДА ГотовыеДанные.ПокупательФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЮридическоеЛицо)
		|				И ГотовыеДанные.ЭтоНаши
		|			ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаРасходовПСО - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СебестоимостьДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)
		|		ИНАЧЕ ВЫБОР
		|				КОГДА ГотовыеДанные.ТимЛидер.Наименование ЕСТЬ NULL
		|					ТОГДА ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаБонусныхВозмещений + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)
		|				ИНАЧЕ (ГотовыеДанные.ЦенаПродажи - ГотовыеДанные.ЦенаВхода - ГотовыеДанные.СуммаПодарка - ГотовыеДанные.СуммаРасходовПСО + ГотовыеДанные.МаржаСДопов + ГотовыеДанные.СуммаКорректировокВозмещенийПлан + ГотовыеДанные.СуммаБонусныхВозмещений + ГотовыеДанные.СуммаКВКредитФакт + ГотовыеДанные.СуммаКВСтрахованияФакт - ЕСТЬNULL(ГотовыеДанные.СуммаДопРасходов, 0)) / 2
		|			КОНЕЦ
		|	КОНЕЦ КАК МаржаДляЗП,
		|	ГотовыеДанные.ОсновнойTradeIn КАК ОсновнойTradeIn,
		|	ГотовыеДанные.ВидВыкупа КАК ВидВыкупа,
		|	ГотовыеДанные.Клиент.РекламныйИсточник КАК ИсточникКлиента,
```

**Данный участок кода:**

```bsl
- 		|				И ГотовыеДанные.ЗаказНарядКонтрагент В ИЕРАРХИИ (&НашиОрганизации)
+ 		|				И ГотовыеДанные.ЭтоНаши
```

**Суть:** изменена логика/структура участка в текущем фрагменте.
**Эффект:** поведение модуля соответствует новой выгрузке `New/2104`.
